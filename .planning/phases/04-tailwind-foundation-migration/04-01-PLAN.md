---
phase: 04-tailwind-foundation-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - src/index.css
autonomous: false
requirements: [STYLE-01, STYLE-03]
user_setup: []

must_haves:
  truths:
    - "@tailwindcss/vite is registered in vite.config.ts plugins and vite dev/build compiles Tailwind"
    - "src/index.css imports Tailwind (@import tailwindcss) and its @theme block emits the design tokens to :root as real CSS custom properties"
    - "The four TSX-referenced tokens (--color-chart-historical, --color-chart-mean, --color-chart-actual, --color-muted) exist under @theme with byte-for-byte identical names and values"
    - "An animate-location-spin utility exists (from --animate-location-spin + @keyframes location-display-spin) reproducing the 0.8s linear infinite spin"
    - "Preflight is active globally from the entry import (basis for the Leaflet interaction verified in plan 04)"
  artifacts:
    - "package.json / package-lock.json with tailwindcss@4.3.2 and @tailwindcss/vite@4.3.2"
    - "vite.config.ts with tailwindcss() plugin"
    - "src/index.css rewritten as the Tailwind entry (@import + @theme + @keyframes + base)"
  key_links:
    - "@theme token emission to :root -> inline var(--color-chart-*)/var(--color-muted) SVG props in TrendDayChart.tsx / TrendLegend.tsx keep resolving"
    - "--animate-location-spin -> animate-location-spin utility consumed by the spinner spans in plan 02"
  prohibitions:
    - text: "MUST NOT introduce a tailwind.config.js; Tailwind v4 CSS-first only (@import tailwindcss + @theme)"
      status: flagged
    - text: "MUST NOT rename or alter the token names --color-chart-historical, --color-chart-mean, --color-chart-actual, --color-muted (referenced as literal strings from TSX)"
      status: flagged
    - text: "MUST NOT re-theme the trend chart tokens (change their color VALUES); chart re-theming is Phase 5 / DESIGN-05"
      status: flagged
    - text: "MUST NOT make any visual/aesthetic change; the bar is visual equivalence to v1.0"
      status: flagged
    - text: "MUST NOT change app feature/behavior of any kind"
      status: flagged
---

<objective>
Stand up the Tailwind CSS v4 foundation: install the two packages (behind a supply-chain checkpoint), register `@tailwindcss/vite` in the build, and rewrite `src/index.css` into the single Tailwind entry file — importing Tailwind, porting the design tokens into `@theme`, and preserving the spinner keyframe as a reusable `animate-*` utility. This is the base every component migration (plans 02, 03) builds on.

Purpose: satisfy STYLE-01 (styling implemented with Tailwind v4, CSS-first, no config file) at the tooling/token layer, and land Preflight globally so the Leaflet interaction (STYLE-03) can be verified in plan 04. Token names must survive so the inline `var(--color-chart-*)` / `var(--color-muted)` SVG props keep resolving untouched (D-04).

Output: `tailwindcss@4.3.2` + `@tailwindcss/vite@4.3.2` installed, `vite.config.ts` wired, `src/index.css` as the Tailwind entry.

Edge-probe note: the deterministic edge-probe over STYLE-01..04 found no input-driven edge cases — these are mechanical/structural requirements, not input-boundary behaviors. The real risk surface is migration completeness + visual equivalence, captured in `must_haves` (truths + prohibitions), not invented edge predicates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tailwind-foundation-migration/04-CONTEXT.md
@.planning/phases/04-tailwind-foundation-migration/04-RESEARCH.md
@.planning/phases/04-tailwind-foundation-migration/04-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint (supply-chain gate)</name>
  <files>(none — human gate, no files touched)</files>
  <read_first>
    - .planning/phases/04-tailwind-foundation-migration/04-RESEARCH.md (## Package Legitimacy Audit — both packages flagged SUS only by the too-new recency heuristic; verified false positives)
  </read_first>
  <action>
    STOP before running any install. This is a blocking supply-chain gate that precedes the `npm install` in Task 2 — do NOT install until a human approves. Present the two packages (`tailwindcss@4.3.2`, `@tailwindcss/vite@4.3.2`) and RESEARCH.md's legitimacy findings to the reviewer, and wait for explicit approval. This gate is never auto-approved (legitimacy gates ignore auto_advance/yolo mode).
  </action>
  <what-built>
    Nothing yet — this gate precedes the `npm install` in Task 2. Two packages will be installed: `tailwindcss@4.3.2` and `@tailwindcss/vite@4.3.2`. RESEARCH.md's audit records both as SUS(`too-new`) false positives: canonical `github.com/tailwindlabs/tailwindcss` repo, ~95M/wk and ~38M/wk downloads, no postinstall script, already the exact package pinned in CLAUDE.md's stack table.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/tailwindcss — confirm latest is 4.3.2, repo is github.com/tailwindlabs/tailwindcss, weekly downloads in the tens of millions.
    2. Open https://www.npmjs.com/package/@tailwindcss/vite — confirm 4.3.2, same tailwindlabs monorepo, no postinstall script.
    3. Confirm neither is a slopsquat lookalike (exact names, official publisher).
  </how-to-verify>
  <verify>
    <human-check>Reviewer confirms both packages resolve to the official tailwindlabs project at version 4.3.2 on npmjs.com, then approves the install.</human-check>
  </verify>
  <acceptance_criteria>
    - Reviewer confirms both packages resolve to the official tailwindlabs project at version 4.3.2.
    - This checkpoint is never auto-approved (legitimacy gates ignore auto_advance/yolo).
  </acceptance_criteria>
  <done>The supply-chain gate is explicitly approved by a human; installation may proceed in Task 2.</done>
  <resume-signal>Type "approved" to proceed to install, or describe the discrepancy.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install Tailwind v4 and register the Vite plugin</name>
  <files>package.json, package-lock.json, vite.config.ts</files>
  <read_first>
    - vite.config.ts (current 11-line config — react() plugin + vitest test.environment jsdom)
    - .planning/phases/04-tailwind-foundation-migration/04-PATTERNS.md (§ A. Tooling wiring — the exact plugin-array change)
    - .planning/phases/04-tailwind-foundation-migration/04-RESEARCH.md (Pattern 2: vite.config.ts plugin wiring)
  </read_first>
  <action>
    Run `npm install tailwindcss@4.3.2 @tailwindcss/vite@4.3.2` (exact pins — do not float to a newer patch). Then edit `vite.config.ts`: add `import tailwindcss from '@tailwindcss/vite'` alongside the existing imports, and insert `tailwindcss()` into the `plugins` array next to `react()` (order `[react(), tailwindcss()]`). Change nothing else — keep the `/// <reference types="vitest/config" />` line, the `test: { environment: 'jsdom' }` block, and the defineConfig shape exactly as-is. Do NOT create a tailwind.config.js or postcss.config.js — this is CSS-first v4, config lives in the entry CSS (@theme), not a JS config file.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};if(d['tailwindcss']!=='4.3.2'||d['@tailwindcss/vite']!=='4.3.2'){console.error('bad versions',d['tailwindcss'],d['@tailwindcss/vite']);process.exit(1)}" && grep -q "@tailwindcss/vite" vite.config.ts && grep -Eq "tailwindcss\(\)" vite.config.ts && test ! -f tailwind.config.js && test ! -f tailwind.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` pins `tailwindcss` and `@tailwindcss/vite` at exactly `4.3.2`.
    - `vite.config.ts` imports `@tailwindcss/vite` and calls `tailwindcss()` inside `plugins`.
    - No `tailwind.config.js` / `tailwind.config.ts` / `postcss.config.js` exists.
    - `react()` and the vitest `test` block remain unchanged.
  </acceptance_criteria>
  <done>Both packages installed at 4.3.2 and the Vite plugin is active; no JS config file introduced.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite src/index.css as the Tailwind entry (tokens + keyframe)</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (current :root token block — the source of truth for the @theme port; lines 1-75)
    - .planning/phases/04-tailwind-foundation-migration/04-PATTERNS.md (§ B design-token port + § C spinner keyframe — verbatim token list and the HARD-REQUIREMENT name list)
    - .planning/phases/04-tailwind-foundation-migration/04-RESEARCH.md (Pattern 1: CSS-first entry with ported tokens — the recommended @theme namespace remap)
  </read_first>
  <action>
    Replace the entire contents of `src/index.css` with the Tailwind entry, in this order:

    1. First line: `@import "tailwindcss";`

    2. An `@theme { ... }` block porting every current `:root` token:
       - Color tokens — copy values 1:1; KEEP these four names byte-for-byte (they are read as literal strings from TSX): `--color-chart-historical: rgba(37, 99, 235, 0.22);`, `--color-chart-mean: var(--color-accent);`, `--color-chart-actual: #ea580c;`, `--color-muted: #4b5563;`. Also include `--color-dominant: #ffffff;`, `--color-secondary: #f4f5f7;`, `--color-accent: #2563eb;`, `--color-destructive: #dc2626;`, `--color-border-subtle: #e5e7eb;` (these stay in the --color-* namespace so they generate bg-/text-/border- utilities).
       - Spacing — remap `--space-*` to Tailwind's native `--spacing-*` namespace (same px values): `--spacing-xs: 4px; --spacing-sm: 8px; --spacing-md: 16px; --spacing-lg: 24px; --spacing-xl: 32px; --spacing-2xl: 48px; --spacing-3xl: 64px;` (generates gap-/p-/m-/w-/h- utilities keyed by name).
       - Font family — `--font-sans:` with the exact stack currently in `--font-family-base` (`-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif`).
       - Text sizes with paired line-heights: `--text-body: 16px; --text-body--line-height: 1.5; --text-label: 14px; --text-label--line-height: 1.4; --text-heading: 20px; --text-heading--line-height: 1.2; --text-display: 28px; --text-display--line-height: 1.2;`.
       - Font weights: `--font-weight-body: 400; --font-weight-label: 400; --font-weight-heading: 600; --font-weight-display: 600;`.
       - Animation: `--animate-location-spin: location-display-spin 0.8s linear infinite;` (exposes the spinner as the `animate-location-spin` utility for plan 02).

    3. After the @theme block, the irreducible keyframe (D-03) as plain CSS: `@keyframes location-display-spin { to { transform: rotate(360deg); } }`.

    4. Minimal global base as plain CSS (not expressible as utilities, kept verbatim from v1.0): `:root { color-scheme: light; }`, `html, body, #root { height: 100%; margin: 0; }`, and a `body` rule setting `font-family: var(--font-sans); font-size: var(--text-body); font-weight: var(--font-weight-body); line-height: 1.5; color: #08060d; background: var(--color-dominant);`.

    Drop the old universal `box-sizing: border-box` rule — Tailwind Preflight already sets it. Do not add any component/BEM rules here and do not add speculative `.leaflet-container` overrides (RESEARCH.md: none are needed). `src/main.tsx` already imports `./index.css` after `leaflet/dist/leaflet.css`; leave that import line untouched (import order is not the deciding factor for cascade layers — verified in RESEARCH.md).
  </action>
  <verify>
    <automated>grep -q '@import "tailwindcss"' src/index.css && grep -q "@theme" src/index.css && for t in --color-chart-historical --color-chart-mean --color-chart-actual --color-muted --animate-location-spin; do grep -q -- "$t" src/index.css || { echo "missing $t"; exit 1; }; done && grep -q "@keyframes location-display-spin" src/index.css && npx tsc -b && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.css` begins with `@import "tailwindcss";` and contains an `@theme` block.
    - The four load-bearing token names are present verbatim: `--color-chart-historical`, `--color-chart-mean`, `--color-chart-actual`, `--color-muted`.
    - `--animate-location-spin` and `@keyframes location-display-spin` are present.
    - `npx tsc -b && npx vite build` exits 0 (Tailwind compiles; entry CSS is valid).
    - No `tailwind.config.js` was created and no `.leaflet-container` override rule was added.
  </acceptance_criteria>
  <done>`src/index.css` is the Tailwind entry: imports Tailwind, ports all tokens into @theme (four color names preserved), keeps the spinner keyframe as the `animate-location-spin` utility, and the production build compiles.</done>
</task>

</tasks>

<threat_model>
Security enforcement: ASVS L1, block_on: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local build | Untrusted package tarballs (tailwindcss, @tailwindcss/vite) cross into the build at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | npm installs (tailwindcss + @tailwindcss/vite @ 4.3.2) | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) before install; both verified on npmjs.com; RESEARCH.md audit confirms canonical tailwindlabs repo, ~95M/38M weekly downloads, and no postinstall script; versions pinned exactly to 4.3.2 |

No other threats at or above `high` in this plan's scope — it adds build-time CSS tooling only, with no auth, session, network-input, or data-handling surface.
</threat_model>

<verification>
- `npx tsc -b && npx vite build` exits 0 with the plugin registered and the new entry CSS.
- `grep` confirms the four `--color-chart-*`/`--color-muted` token names and the `animate-location-spin` machinery are present in `src/index.css`.
- No `tailwind.config.js`/`tailwind.config.ts`/`postcss.config.js` exists.
- The supply-chain checkpoint was explicitly approved before install.
</verification>

<success_criteria>
- Tailwind v4 (`tailwindcss` + `@tailwindcss/vite`, both 4.3.2) installed and wired via `vite.config.ts` with no JS config file (STYLE-01, foundation).
- `src/index.css` is the CSS-first Tailwind entry with all design tokens ported into `@theme`, the four TSX-referenced token names preserved byte-for-byte, and the spinner keyframe exposed as `animate-location-spin`.
- Preflight is active globally, ready for the Leaflet interaction check in plan 04 (STYLE-03).
- Production build compiles.
</success_criteria>

<artifacts_this_phase_produces>
Symbols/files created in Phase 4 (excluded from later drift verification):
- `src/index.css` rewritten as the Tailwind entry (`@import "tailwindcss";` + `@theme { ... }` token block + `@keyframes location-display-spin` + minimal global base).
- New `@theme` design-token block (colors, `--spacing-*`, `--font-sans`, `--text-*`, `--font-weight-*`).
- New `@keyframes location-display-spin` + `--animate-location-spin` → the `animate-location-spin` utility class.
- `@tailwindcss/vite` plugin registered in `vite.config.ts` (`tailwindcss()`).
- New package.json deps: `tailwindcss@4.3.2`, `@tailwindcss/vite@4.3.2`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-tailwind-foundation-migration/04-01-SUMMARY.md` when done.
</output>
