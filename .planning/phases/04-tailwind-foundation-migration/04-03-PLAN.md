---
phase: 04-tailwind-foundation-migration
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/app/TrendRow.tsx
  - src/app/TrendDayChart.tsx
  - src/app/TrendLegend.tsx
autonomous: true
requirements: [STYLE-01]
user_setup: []

must_haves:
  truths:
    - "TrendRow (heading, shared Y-axis column, charts row), TrendDayChart (day tile, label, today variant, placeholder), and TrendLegend render visually equivalent to v1.0 from Tailwind utilities"
    - "The inline SVG color props var(--color-chart-historical), var(--color-chart-actual), var(--color-chart-mean), var(--color-muted) are UNTOUCHED and resolve from plan 01's @theme, so the historical dots, mean line, actual-value marker, and axis ticks keep their exact colors"
    - "The existing TrendDayChart and TrendLegend Vitest suites still pass"
  artifacts:
    - "src/app/TrendRow.tsx, src/app/TrendDayChart.tsx, src/app/TrendLegend.tsx with utility-class className strings"
  key_links:
    - "@theme --color-chart-*/--color-muted tokens (plan 01) -> unchanged inline SVG fill/stroke/tick props -> chart marks render with their v1.0 colors"
  prohibitions:
    - text: "MUST NOT modify the inline SVG fill/stroke/tick props that read var(--color-chart-*) / var(--color-muted) — className strings only"
      status: flagged
    - text: "MUST NOT re-theme the trend visuals (change any chart color, shape, or the legend wording); chart re-theming is Phase 5 / DESIGN-05"
      status: flagged
    - text: "MUST NOT rename any design token"
      status: flagged
    - text: "MUST NOT make any visual/aesthetic change; render visually equivalent to v1.0"
      status: flagged
    - text: "MUST NOT change component markup, props, or behavior — className strings only"
      status: flagged
---

<objective>
Migrate the trend visuals to Tailwind utilities: the trend row layout (TrendRow.tsx — heading, shared Y-axis column, charts row), the per-day chart tile (TrendDayChart.tsx — day container, label with its today variant, and the not-enough-data placeholder), and the persistent legend (TrendLegend.tsx). Rewrite `className` strings only; the inline SVG `var(--color-*)` props that color the Recharts marks are left exactly as-is.

Purpose: STYLE-01 (Tailwind utilities in JSX) for the three trend components, while D-04's hard constraint holds — the four `var(--color-chart-*)`/`var(--color-muted)` token references stay byte-for-byte so the dots, mean line, actual marker, and tick labels keep resolving from plan 01's `@theme` untouched. Chart re-theming is explicitly out of scope (Phase 5 / DESIGN-05).

Output: three trend components rewritten to utility classes with all SVG color props preserved.

Edge-probe note: STYLE-01..04 are mechanical requirements with no input-driven edge cases; the risk here is (a) faithful visual translation and (b) NOT disturbing the SVG color props — both captured in `must_haves`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-tailwind-foundation-migration/04-CONTEXT.md
@.planning/phases/04-tailwind-foundation-migration/04-PATTERNS.md
@.planning/phases/04-tailwind-foundation-migration/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: TrendRow to utilities (heading, shared axis, charts row)</name>
  <files>src/app/TrendRow.tsx</files>
  <read_first>
    - src/app/TrendRow.tsx (six className sites on lines 37-44)
    - .planning/phases/04-tailwind-foundation-migration/04-PATTERNS.md (§ TrendRow.tsx — exact source classes + CSS)
  </read_first>
  <action>
    Translate all six className sites (source classes + CSS in PATTERNS.md § TrendRow.tsx):
    - The `<section>` row container → `flex flex-col gap-sm`.
    - The heading → `m-0 text-label font-semibold text-muted uppercase tracking-[0.05em]` (uppercase muted eyebrow with 0.05em tracking; weight 600).
    - The body wrapper (axis + charts) → `flex flex-row items-start gap-sm`.
    - The Y-axis column → `flex flex-col flex-none gap-xs`.
    - The axis spacer `<span>` (aria-hidden) → `block h-4` (16px, matching the label-row height so ticks align with the tiles below).
    - The charts row → `flex flex-row items-start gap-sm`.
    Do not change markup, props, or the aria-hidden attribute — className strings only.
  </action>
  <verify>
    <automated>! grep -q "trend-row" src/app/TrendRow.tsx && grep -q "className=" src/app/TrendRow.tsx && grep -q "aria-hidden" src/app/TrendRow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "trend-row" src/app/TrendRow.tsx` returns nothing (no BEM classes remain).
    - The file still has `className=` utility strings and the `aria-hidden` spacer.
    - The row keeps its heading + shared axis column + charts row layout (visual equivalence).
  </acceptance_criteria>
  <done>TrendRow renders from utilities with the shared Y-axis column layout intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TrendDayChart className to utilities (SVG color props untouched)</name>
  <files>src/app/TrendDayChart.tsx</files>
  <read_first>
    - src/app/TrendDayChart.tsx (className sites incl. the labelClassName ternary near lines 127-129 and the day/placeholder sites near lines 138-146; SVG color props on lines 89/109/170/179/222 — DO NOT TOUCH)
    - .planning/phases/04-tailwind-foundation-migration/04-PATTERNS.md (§ TrendDayChart.tsx — exact source classes, CSS, and the DO-NOT-TOUCH line list)
  </read_first>
  <action>
    Translate the className sites only (source classes + CSS in PATTERNS.md § TrendDayChart.tsx):
    - The day tile container → `flex flex-col w-[88px] gap-xs`.
    - The base day label → `m-0 h-4 text-label font-normal text-muted text-center` (fixed 16px height, centered, muted, weight 400).
    - The today label variant → keep the existing ternary that builds `labelClassName`, but output utilities: today = `m-0 h-4 text-label text-center font-semibold text-accent`; non-today = `m-0 h-4 text-label text-center font-normal text-muted`.
    - The not-enough-data placeholder card → `w-[88px] h-[120px] flex items-center justify-center text-center p-xs bg-dominant border border-border-subtle rounded-[8px] text-muted text-label box-border`.
    HARD CONSTRAINT (D-04): do NOT modify any inline SVG prop. The props reading `var(--color-chart-historical)`, `var(--color-chart-actual)`, `var(--color-chart-mean)`, and `var(--color-muted)` (in `fill`, `stroke`, and `tick={{ fill: ... }}`) must stay byte-for-byte identical — they are plain strings TypeScript cannot type-check, and a rename silently blanks the chart colors. Change className strings only; leave all Recharts/SVG props, data logic, and the defensive guards untouched.
  </action>
  <verify>
    <automated>! grep -q "trend-day" src/app/TrendDayChart.tsx && grep -Fq "var(--color-chart-historical)" src/app/TrendDayChart.tsx && grep -Fq "var(--color-chart-actual)" src/app/TrendDayChart.tsx && grep -Fq "var(--color-chart-mean)" src/app/TrendDayChart.tsx && grep -Fq "var(--color-muted)" src/app/TrendDayChart.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "trend-day" src/app/TrendDayChart.tsx` returns nothing (no BEM classes remain).
    - All four SVG token references are still present verbatim: `var(--color-chart-historical)`, `var(--color-chart-actual)`, `var(--color-chart-mean)`, `var(--color-muted)`.
    - The labelClassName ternary still distinguishes today vs non-today.
    - The day tile, label, and placeholder render visually equivalent to v1.0.
  </acceptance_criteria>
  <done>TrendDayChart's className strings are utilities; every SVG color prop is preserved so the chart marks keep their colors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: TrendLegend className to utilities (SVG color props untouched); run trend tests</name>
  <files>src/app/TrendLegend.tsx</files>
  <read_first>
    - src/app/TrendLegend.tsx (four className sites on lines 23-33 and 40; SVG fill props on lines 48/60/69 — DO NOT TOUCH)
    - .planning/phases/04-tailwind-foundation-migration/04-PATTERNS.md (§ TrendLegend.tsx — exact source classes, CSS, and the DO-NOT-TOUCH line list)
  </read_first>
  <action>
    Translate the four className sites only (source classes + CSS in PATTERNS.md § TrendLegend.tsx):
    - The legend container `<div>` (role="list") → `flex flex-row flex-wrap gap-md`.
    - Each legend item → `flex flex-row items-center gap-xs`.
    - The swatch `<svg>` → `flex-none w-[14px] h-[14px]`.
    - The legend label `<span>` → `text-label font-label text-muted`.
    HARD CONSTRAINT (D-04): do NOT modify the inline SVG `fill` props reading `var(--color-chart-historical)`, `var(--color-chart-mean)`, `var(--color-chart-actual)` — className strings only. Keep `role="list"`, `aria-label`, and the legend wording exactly as-is (the reviewer-exact copy is locked). After the rewrite, run the two trend Vitest files to confirm the SVG structure the tests assert still holds.
  </action>
  <verify>
    <automated>! grep -q "trend-legend" src/app/TrendLegend.tsx && grep -Fq "var(--color-chart-historical)" src/app/TrendLegend.tsx && grep -Fq "var(--color-chart-mean)" src/app/TrendLegend.tsx && grep -Fq "var(--color-chart-actual)" src/app/TrendLegend.tsx && npx vitest run src/app/TrendDayChart.test.tsx src/app/TrendLegend.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "trend-legend" src/app/TrendLegend.tsx` returns nothing (no BEM classes remain).
    - The three SVG token references are still present verbatim: `var(--color-chart-historical)`, `var(--color-chart-mean)`, `var(--color-chart-actual)`.
    - `role="list"`, `aria-label`, and the legend copy are unchanged.
    - `npx vitest run src/app/TrendDayChart.test.tsx src/app/TrendLegend.test.tsx` exits 0.
  </acceptance_criteria>
  <done>TrendLegend renders from utilities with SVG color props and legend copy preserved; the trend test suites pass.</done>
</task>

</tasks>

<threat_model>
Security enforcement: ASVS L1, block_on: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side className rewrites only — no new inputs, network calls, auth, or data handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | client JSX className rewrite (TrendRow/TrendDayChart/TrendLegend) | low | accept | Styling migration with no new attack surface; SVG data/color props and behavior unchanged. Supply-chain threat T-04-SC is handled by plan 01's install checkpoint. |

No threats at or above `high` in this plan's scope.
</threat_model>

<verification>
- Per-task grep gates confirm no BEM classes remain and every `var(--color-chart-*)`/`var(--color-muted)` SVG prop survives.
- `npx vitest run` on the two trend test files (Task 3) confirms the asserted SVG structure still renders.
- Definitive full build+test+visual verification is plan 04.
</verification>

<success_criteria>
- TrendRow, TrendDayChart, and TrendLegend styled entirely via Tailwind utilities (STYLE-01).
- All four `var(--color-chart-*)`/`var(--color-muted)` SVG color props preserved byte-for-byte (D-04); no chart re-theme.
- The trend Vitest suites pass.
</success_criteria>

<artifacts_this_phase_produces>
Phase-4 artifacts produced here (see plan 01 for token/plugin artifacts):
- `src/app/TrendRow.tsx`, `src/app/TrendDayChart.tsx`, `src/app/TrendLegend.tsx` rewritten to utility classes.
- No new symbols/exports and no new tokens — className strings only; SVG color props unchanged.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-tailwind-foundation-migration/04-03-SUMMARY.md` when done.
</output>
