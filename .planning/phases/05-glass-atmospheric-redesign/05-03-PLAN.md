---
phase: 05-glass-atmospheric-redesign
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/app/AnomalyCard.tsx
  - src/app/TrendRow.tsx
  - src/app/TrendDayChart.tsx
autonomous: true
requirements: [DESIGN-01, DESIGN-03, DESIGN-04, DESIGN-05, DESIGN-06]
must_haves:
  truths:
    - "each AnomalyCard state branch (empty / loading / error / resolved) renders on the hero glass card (bg rgba(255,255,255,0.72), border-glass-border, rounded-glass-lg, shadow-glass, backdrop-blur-lg, p-lg) so the treatment never flickers between states (DESIGN-03, DESIGN-04)"
    - "the hero delta <p> is color-coded via color: var(--anomaly-color) with a motion-safe transition; at 700-weight / ~48px on the highest-opacity surface it is the unmistakable focal point (DESIGN-04, D-06)"
    - "the z-score chip keeps its neutral bg-secondary / text-muted styling and takes NO anomaly hue, staying subordinate to the hero (D-06)"
    - "a zero / near-normal delta renders big '0°C' immediately followed by 'Right on the 30-year average' — never a bare, ambiguous 0 (DESIGN-06, D-07)"
    - "the TrendRow section root is a translucent glass card matching the panel system (DESIGN-03)"
    - "the recharts trend re-themes to the new palette purely via the --color-chart-historical token value (Plan 01); TrendYAxisColumn layout and TrendLegend copy are untouched (DESIGN-05, D-08)"
    - "the 'Not enough data' placeholder tile uses glass tokens (bg-glass-surface, border-glass-border, rounded-glass-sm) with NO nested backdrop-blur (it already sits inside the blurred TrendRow card) (DESIGN-05, D-08)"
    - "the actual-value marker keeps its reserved standout hue #ea580c (var(--color-chart-actual)), clearly distinguishable from the recolored historical dots and the mean line (D-09)"
    - "AnomalyCard empty ('Drop a pin to see today's current conditions.') renders on the hero glass card, copy unchanged (E2 empty)"
    - "AnomalyCard loading spinner (--color-accent ring) renders on the hero glass card (E2 loading)"
    - "AnomalyCard resolved hero shows the color-coded delta; the zero-delta case renders the framed copy (E2 populated)"
    - "AnomalyCard null zScore resolves to the normal anchor via anomalyColor(null) inherited on the hero — no blank/undefined hero (E2 partial)"
    - "the hero delta text (whole-number +/- format, <= ~3 chars incl. sign) fits the 760px hero card at any anomalyColor value — no truncation (E2 overflow)"
    - "TrendRow returns null for null/empty days — no dangling empty glass card renders (E3 empty)"
    - "TrendRow loading branch renders on the glass card (E3 loading)"
    - "per-day chart failure is inherited from Phase 3 and a fully-absent trend collapses to null — no new error surface (E3 error)"
    - "the full 7-day happy path keeps the locked layout; only token values / finish change (E3 populated)"
    - "mixed usable + 'not enough data' tiles restyle to the glass treatment; placeholder tiles stay neutral with no anomaly-hue coloring (E3 partial)"
    - "the fixed shared-Y-domain layout introduces no reflow this phase (E3 overflow)"
    - statement: "text-destructive (#dc2626) error copy meets legible contrast against the neutral-anchor translucent glass surface (rgba(255,255,255,0.72) over the #57534e-tinted gradient), not just the old flat bg-secondary"
      verification: backstop
  artifacts:
    - src/app/AnomalyCard.tsx
    - src/app/TrendRow.tsx
    - src/app/TrendDayChart.tsx
  key_links:
    - "the hero <p> reads the inherited var(--anomaly-color) set on LocationPanel (Plan 02) — one shared color source for backdrop and hero; the @property initial-value #57534e is the fallback if no bridge is present"
    - "DESIGN-05 re-theme flows entirely from the src/index.css --color-chart-historical token value (Plan 01); TrendDayChart/TrendLegend var(--color-chart-*) call sites are unchanged"
  prohibitions:
    - "must NOT let glass translucency reduce the hero delta number or the trend chart below legible contrast (D-04 / D-06 hard line)"
    - "must NOT alter the locked Phase-3 trend layout (TrendYAxisColumn, shared Y-domain) or the reviewer-exact TrendLegend copy — the re-theme is token-value / finish only (D-08)"
    - "must NOT recolor the z-score chip or change the actual-value marker hue #ea580c (D-06 / D-09)"
    - "must NOT introduce any animation loop or growth of the hero font size (DESIGN-04 hierarchy comes from color + opacity + weight, not a larger font)"
---

<objective>
Deliver the content-surface half of the redesign: elevate the anomaly hero as the unmistakable focal point (color-coded delta on the highest-opacity glass card, with the framed zero-delta case), and re-theme the recharts trend to sit in the same visual system — all without disturbing the locked Phase-3 trend layout or legend copy.

Purpose: DESIGN-04 (hero hierarchy) + DESIGN-06 (zero-delta clarity) + DESIGN-05 (chart re-theme) + DESIGN-03 (glass surfaces). The hero reads the inherited --anomaly-color from the panel (Plan 02), so backdrop and hero are one color system (D-06). The chart recolor is token-value-only (Plan 01) plus the placeholder-tile glass finish.
Output: AnomalyCard glass cards + color-coded hero; TrendRow glass card; TrendDayChart placeholder-tile glass finish. No feature/behavior change; layout and legend copy locked.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-glass-atmospheric-redesign/05-UI-SPEC.md
@.planning/phases/05-glass-atmospheric-redesign/05-PATTERNS.md
@.planning/phases/05-glass-atmospheric-redesign/05-01-PLAN.md
</context>

<artifacts_produced>
This plan creates NO new exported symbols, tokens, or props — it restyles existing components.
- Consumes (does not create): var(--anomaly-color) inherited from LocationPanel (Plan 02); glass tokens + --color-chart-historical re-value (Plan 01, src/index.css); VERDICT_LABEL.typical framed copy (Plan 01, src/anomaly/anomaly.ts)
- The only sanctioned inline style added here is the hero <p>'s color: var(--anomaly-color) (the second and last inline-style exception for the phase)
- No new files, no new function/type/prop names.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Elevate the anomaly hero — glass cards on all branches + color-coded delta</name>
  <files>src/app/AnomalyCard.tsx</files>
  <read_first>
    - src/app/AnomalyCard.tsx (analog: the 4-branch early-return structure — empty lines 31-39, combined loading 44-56, error 58-66, resolved hero 68-92; the hero delta <p> line 84; the z-score chip lines 86-90)
    - 05-UI-SPEC.md "Layout & Composition (Glass System)" hero-elevation paragraph (rgba(255,255,255,0.72), p-lg), the "Color" section (hero reads var(--anomaly-color); z-score chip stays neutral), "Motion & Performance Contract", and "Copywriting Contract" (zero-delta framed copy comes from VERDICT_LABEL, not from this file)
    - 05-PATTERNS.md "src/app/AnomalyCard.tsx" section (branch structure to preserve exactly; the two sanctioned inline-style points)
    - src/index.css glass tokens + @property --anomaly-color (Plan 01); src/anomaly/anomaly.ts VERDICT_LABEL.typical (Plan 01)
  </read_first>
  <action>
    Apply the hero glass card to the ROOT element of all four branches (empty, combined-loading, error, resolved) so the treatment never resets between states. Each root className gets `bg-[rgba(255,255,255,0.72)] border border-glass-border rounded-glass-lg shadow-glass backdrop-blur-lg p-lg` — the hero uses the higher 0.72 opacity (the strongest surface, best legibility for D-06's hard contrast requirement) rather than the shared bg-glass-surface. Preserve each branch's existing inner structure exactly: keep the empty/loading/error copy (locked), the role="status" + aria-hidden + --color-accent spinner ring on the loading branch, the text-destructive error line, the current-temperature <p> (text-display font-display) and the info "i" button in the resolved branch. On the resolved hero delta <p> (currently `m-0 text-[calc(var(--text-display)*1.7)] font-bold leading-[1.1]`) add the inline style={{ color: 'var(--anomaly-color)' }} plus `motion-safe:transition-colors motion-safe:duration-300 motion-safe:ease-out` — do NOT grow the font size (hierarchy comes from color + opacity + weight, not size). This is the second and final sanctioned inline style of the phase; add no others. Leave the verdict label <p> as-is — it renders verdictLabel(anomaly.verdictTier), which now returns 'Right on the 30-year average' for the typical tier via Plan 01's copy change. Leave the z-score chip (bg-secondary text-muted pill) completely unchanged — it must not take any anomaly hue.
  </action>
  <verify>
    <automated>npx vitest run && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` (full suite) exits 0 and `npx vite build` exits 0
    - Source assertion: hero card on every branch root — `grep -c "rgba(255,255,255,0.72)" src/app/AnomalyCard.tsx` returns >= 4
    - Source assertion: `grep -q "backdrop-blur-lg" src/app/AnomalyCard.tsx` and `grep -q "shadow-glass" src/app/AnomalyCard.tsx`
    - Hero color-coding: `grep -q "color: 'var(--anomaly-color)'" src/app/AnomalyCard.tsx` and `grep -q "motion-safe:transition-colors" src/app/AnomalyCard.tsx`
    - Hero size unchanged: `grep -q "text-\[calc(var(--text-display)\*1.7)\]" src/app/AnomalyCard.tsx`
    - z-score chip unchanged: the pill still carries `text-muted bg-secondary` (`grep -q "text-muted bg-secondary" src/app/AnomalyCard.tsx`)
    - Behavior (DESIGN-06): a zero delta renders '0' (from formatDelta) paired with 'Right on the 30-year average' (from verdictLabel) — the two-part framed zero
    - <human-check>Backstop (E2 error contrast): force the AnomalyCard error branch and confirm the #dc2626 "Couldn't compute an anomaly here." text stays legibly high-contrast against the neutral-anchor translucent glass surface, not just against the old flat gray.</human-check>
  </acceptance_criteria>
  <done>All four AnomalyCard branches render on the higher-opacity hero glass card; the resolved delta is color-coded via the inherited --anomaly-color with a motion-safe transition and unchanged size; the z-score chip is untouched; the zero-delta framed pair renders; suite + build pass.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the TrendRow root onto the translucent glass card</name>
  <files>src/app/TrendRow.tsx</files>
  <read_first>
    - src/app/TrendRow.tsx (analog: the <section className="flex flex-col gap-sm"> root line 37 and the null-guard early return lines 30-32)
    - 05-UI-SPEC.md "Layout & Composition (Glass System)" (three glass cards; TrendRow root is the third; smaller cards get px-md py-md) and "Recharts Re-theme" (layout locked — TrendYAxisColumn / TrendLegend untouched)
    - 05-PATTERNS.md "src/app/TrendRow.tsx" is not restructured; the shared-Y-domain composition is locked (D-06)
    - src/index.css glass tokens (Plan 01)
  </read_first>
  <action>
    Add the shared glass-card treatment to the TrendRow <section> root className: `bg-glass-surface border border-glass-border rounded-glass-lg shadow-glass backdrop-blur-lg px-md py-md`, preserving the existing `flex flex-col gap-sm`. Do NOT touch anything else in this component: the null/empty early return stays (so no empty glass card renders when days is null/empty), the "Last 7 Days" eyebrow heading, the computeSharedYDomain call and the single TrendYAxisColumn column, the 7x TrendDayChart mapping with showYAxis={false}, and the persistent <TrendLegend /> all remain exactly as they are (layout + legend copy locked, D-08). This is the outer blurred card; the inner day tiles must NOT add their own backdrop-blur (handled in Task 3).
  </action>
  <verify>
    <automated>npx vitest run && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` (full suite) exits 0 and `npx vite build` exits 0
    - Source assertion: `grep -q "bg-glass-surface" src/app/TrendRow.tsx` and `grep -q "backdrop-blur-lg" src/app/TrendRow.tsx` and `grep -q "rounded-glass-lg" src/app/TrendRow.tsx`
    - Layout locked: `grep -q "TrendYAxisColumn" src/app/TrendRow.tsx` and `grep -q "TrendLegend" src/app/TrendRow.tsx` and `grep -q "computeSharedYDomain" src/app/TrendRow.tsx` (all still present, unchanged)
    - Null-guard preserved: `grep -q "days === null || days.length === 0" src/app/TrendRow.tsx`
  </acceptance_criteria>
  <done>The TrendRow section root is a translucent glass card; the null guard, shared Y-axis layout, 7 tiles, and the legend are untouched; suite + build pass.</done>
</task>

<task type="auto">
  <name>Task 3: Give the TrendDayChart "not enough data" placeholder tile the glass finish (no nested blur)</name>
  <files>src/app/TrendDayChart.tsx</files>
  <read_first>
    - src/app/TrendDayChart.tsx (analog: the "not enough data" placeholder box lines 136-148, currently `bg-dominant border border-border-subtle rounded-[8px]`; the var(--color-chart-*) SVG props at lines 170, 179, 184-185 which stay unchanged)
    - 05-UI-SPEC.md "Recharts Re-theme" table (last row: placeholder box -> glass tokens; historical token value already changed in Plan 01; mean/actual unchanged) and the "do NOT stack blur on the small tiles" note
    - 05-PATTERNS.md "src/app/TrendDayChart.tsx" section (re-theme is token-value-only; the placeholder box is the ONE className change in this file)
    - src/index.css glass tokens + re-valued --color-chart-historical (Plan 01)
  </read_first>
  <action>
    Change ONLY the "Not enough data" placeholder box className (the `!day.usable` branch): replace its `bg-dominant border border-border-subtle rounded-[8px]` fill/border/radius with the glass tokens `bg-glass-surface border border-glass-border rounded-glass-sm`, keeping every other class on that element (w-[88px], h-[120px], flex, items-center, justify-center, text-center, p-xs, text-muted, text-label, leading-[1.5], box-border) and the title / aria-label / "Not enough data" copy unchanged. Do NOT add backdrop-blur here — the tile already sits inside the blurred TrendRow card (stacking blur wastes paint for no visible gain). Do NOT change any var(--color-chart-historical|mean|actual) SVG prop, the ComposedChart/Scatter/ReferenceLine markup, the r={3} historical dot radius, the actual-value polygon (stays fill="var(--color-chart-actual)" = #ea580c, D-09), or the shared yDomain/axis — the historical recolor is delivered entirely by Plan 01's token-value edit. If the UI-SPEC pins no additional finish numeric tweak, make none.
  </action>
  <verify>
    <automated>npx vitest run src/app/TrendDayChart.test.tsx && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` (full suite, incl. TrendDayChart.test.tsx and TrendLegend.test.tsx) exits 0 and `npx vite build` exits 0
    - Source assertion: placeholder box restyled — `grep -q "bg-glass-surface" src/app/TrendDayChart.tsx` and `grep -q "rounded-glass-sm" src/app/TrendDayChart.tsx`
    - No nested blur on tiles: `grep -c "backdrop-blur" src/app/TrendDayChart.tsx` returns 0
    - Marks unchanged: `grep -q "var(--color-chart-actual)" src/app/TrendDayChart.tsx` and `grep -q "var(--color-chart-mean)" src/app/TrendDayChart.tsx` and `grep -q "var(--color-chart-historical)" src/app/TrendDayChart.tsx` (all call sites intact — recolor is token-value-only)
    - Copy unchanged: `grep -q "Not enough data" src/app/TrendDayChart.tsx`
  </acceptance_criteria>
  <done>The placeholder tile carries the glass finish (small radius, no nested blur); all recharts marks and the SVG var() call sites are untouched; the trend suite and build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inherited CSS var -> hero text color | the hero <p> reads var(--anomaly-color), set numeric-derived on LocationPanel (Plan 02) |
| verdict tier -> rendered copy | verdictLabel(tier) returns a fixed curated string, rendered as a plain JSX text node |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | hero delta color: var(--anomaly-color) | low | mitigate | The hero color resolves only to anomalyColor()'s computed #rrggbb (or the @property initial-value fallback) — never a user-controlled string reaching a style context |
| T-05-06 | Information disclosure | verdict / delta / z-score copy | low | mitigate | All dynamic text (delta, verdict label, z-score) renders as ordinary JSX text nodes — never dangerouslySetInnerHTML — preserving the established discipline; the DESIGN-06 copy change is a static curated string |

No new inputs, network calls, persistence, or auth — no ASVS L1 "high" threat to block on.
</threat_model>

<flagged_assumptions>
Spec-less edge probe items that are NOT genuine data-shape boundaries for this plan (surfaced explicitly, not silently dropped):
- DESIGN-04 "unclassified" probe: hero-hierarchy strength is a visual property (opacity + weight + color), not a discrete data boundary — verified via the human walkthrough, not a unit boundary.
- DESIGN-05 "unclassified" probe: the recharts re-theme is a token-value / finish change with no data-shape edge; covered by the re-theme truths above and the trend test suite staying green.
</flagged_assumptions>

<verification>
- `npx vitest run` (full suite) and `npx vite build` pass — trend tests query by SVG element type, so the recolor/finish changes keep them green.
- Layout + legend copy locked (TrendYAxisColumn / TrendLegend untouched).
- Only two inline styles exist across the whole phase: the LocationPanel --anomaly-color bridge (Plan 02) and this plan's hero color: var(--anomaly-color).
</verification>

<success_criteria>
The anomaly delta reads as the unmistakable, color-coded focal point with the zero-delta case clearly framed; the recharts trend and the panel surfaces read as one glassy visual system with the Phase-3 layout and legend copy intact; full suite and production build green, no behavior change.
</success_criteria>

<output>
Create `.planning/phases/05-glass-atmospheric-redesign/05-03-SUMMARY.md` when done.
</output>
