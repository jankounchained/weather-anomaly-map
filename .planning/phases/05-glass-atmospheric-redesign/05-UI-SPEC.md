---
phase: 5
slug: glass-atmospheric-redesign
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-16
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none |
| Icon library | none (CSS-drawn glyphs/spinner only — the "i" info glyph and the location spinner are both plain markup + `@theme` keyframe, no icon package) |
| Font | `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif` (unchanged system stack from Phase 4 — no new webfont this phase) |

**Tool: none — reasoning.** No `components.json` exists and no component library (radix/shadcn/etc.) is referenced anywhere in the codebase or in `CLAUDE.md`'s stack table. Phases 1–4 already established a hand-rolled Tailwind v4 `@theme` token system (`src/index.css`) as the project's design-system convention. REQUIREMENTS.md scopes v1.1 as "styling and visual design only" with no new dependencies implied — introducing shadcn now would contradict that framing and the already-locked Phase 4 foundation. Continuing the hand-rolled token convention is the correct, lowest-risk choice; the shadcn gate is answered **N** by inference from established project convention.

---

## Spacing Scale

Declared values (multiples of 4, unchanged from Phase 4 — `src/index.css`):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing, panel card gap |
| lg | 24px | Section padding (panel outer padding) |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none — this phase reuses the existing scale for all padding/gap decisions. New **non-spacing** tokens (radius, blur, shadow, glass surface alpha) are introduced in [Layout & Composition](#layout--composition-glass-system) below; they are a separate token family, not spacing exceptions.

---

## Typography

Declared roles (unchanged from Phase 4 — `src/index.css` `@theme`):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 | 1.5 |
| Label | 14px | 400 (also used at 600 for the trend "today" tile label and legend caption — existing, unchanged) | 1.4 |
| Heading | 20px | 600 | 1.2 |
| Display | 28px | 600 | 1.2 |

**Declared weight set: 400 (regular) + 600 (semibold).**

**Hero weight exception (DESIGN-04):** the anomaly-delta hero number is a **third, explicitly reserved weight — 700 (bold)** at size `calc(var(--text-display) * 1.7)` ≈ 48px, line-height 1.1. This already exists in code (`AnomalyCard.tsx`: `text-[calc(var(--text-display)*1.7)] font-bold leading-[1.1]`) and is **kept unchanged by size** — DESIGN-04's "strengthened hierarchy" is delivered through color-coding + elevated glass surface (see below), not by growing the font further (avoids overflow risk in the 760px panel). 700 is reserved **exclusively** for this one element — do not reuse bold 700 anywhere else; if a future need for bold arises elsewhere, treat it as a new discretion item, not a silent reuse of this exception.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#ffffff` (`--color-dominant`, unchanged) | Map region background, base page — untouched by this phase (PERF-01: the map surface receives no glass/gradient treatment at all) |
| Secondary (30%) | `#f4f5f7` (`--color-secondary`, unchanged token, **new role**: no-pin/no-data fallback fill — see [Layout & Composition](#layout--composition-glass-system)) | Docked panel background **before** any anomaly value exists; superseded by the computed anomaly gradient once a pin+data context exists |
| Accent (10%) | `#2563eb` (`--color-accent`, unchanged) | Reserved for: location/anomaly loading-spinner ring, the "i" data-quality info button border+glyph+focus treatment, the trend chart's mean `ReferenceLine`. **Not** reused for anomaly-hue coloring (hero/backdrop) — kept semantically separate from the new data-driven palette so "accent = interactive chrome" stays unambiguous |
| Destructive | `#dc2626` (`--color-destructive`, unchanged) | Reserved for the AnomalyCard fetch-error text only (`text-destructive`, "Couldn't compute an anomaly here."). **Not** reused for "hot" anomaly semantics — a hot-but-successful reading must never visually read as an error state |

Accent reserved for: spinner ring (LocationDisplay + AnomalyCard loading states), info-button border/glyph, trend chart mean line. Never for anomaly-hero or backdrop coloring.

### New tokens — Anomaly Color System (DESIGN-02, DESIGN-04, D-02, D-06)

These are **computed/continuous**, not fixed-frequency 60/30/10 colors — documented separately because they don't fit that static model:

| Token | RGB | Hex | Role |
|-------|-----|-----|------|
| `--color-anomaly-cold` | `30, 58, 138` | `#1e3a8a` | Anchor at z ≤ −3 (coldest) |
| `--color-anomaly-normal` | `87, 83, 78` | `#57534e` | Anchor at z = 0 (near-normal) |
| `--color-anomaly-hot` | `154, 52, 18` | `#9a3412` | Anchor at z ≥ +3 (hottest) |
| `--color-atmosphere-night-wash` | `10, 8, 20` @ 40% | `rgba(10,8,20,0.4)` | Overlay wash applied atop the anomaly gradient when the pin's local time is night (D-03) |

**Pure mapping function contract (mirrors `anomaly.ts`'s pure-function-then-unit-test pattern — put this in `src/anomaly/` alongside `computeAnomaly`, not inline in a component):**

```
anomalyColor(zScore: number | null): string  // returns "#rrggbb"

1. Treat null zScore as z = 0 (same fallback precedent as classifyVerdict's
   `zScore ?? 0`, Pitfall 2) — resolves to --color-anomaly-normal exactly.
2. Clamp z to [-3, 3].
3. Two-segment piecewise-linear RGB interpolation (NOT a single cold→hot
   lerp — that would mud through gray/brown at the midpoint and would not
   pass exactly through the chosen "normal" anchor at z=0):
   - z <= 0: t = (z + 3) / 3;  color = lerp(COLD, NORMAL, t)
   - z >  0: t = z / 3;        color = lerp(NORMAL, HOT, t)
   - lerp per channel: round(c1 + (c2 - c1) * t), clamped to [0,255]
4. Output "#rrggbb".
```

Unit-test anchor points: z=-3 → exactly `#1e3a8a`; z=0 (and z=null) → exactly `#57534e`; z=+3 → exactly `#9a3412`; z=-1.5 and z=+1.5 → segment midpoints (spot-check the lerp math).

**Day/night is a separate, independent axis (D-03)** — it modulates the *backdrop gradient's* presentation only, never the hero text color (hero text always shows the pure `anomalyColor()` value for legibility consistency regardless of time of day):
```
isDaytime(localHour: number): boolean  =>  localHour >= 6 && localHour < 20
```
Night: layer `--color-atmosphere-night-wash` over the backdrop gradient (e.g. a second `background-image` layer or `::before` overlay) — darkens/desaturates the ambient mood without touching the anomaly hue's identity. Day: no wash, full anomaly-hue gradient at normal lightness.

**Required data-signal addition (verified, not assumed — CONTEXT.md D-03 flagged this for confirmation):** `useCurrentWeather`'s public `localDate` field is date-only (`client.ts`'s `localDateFrom` splits `current.time` on `"T"` and keeps only the date portion) — **the hour is currently discarded**, even though Open-Meteo's raw `current.time` ("YYYY-MM-DDTHH:mm", already pin-local per `timezone=auto`) contains it. Planning must add a sibling pure function `localHourFrom(currentTime: string): number` in `client.ts` and a new `localHour: number | null` field on `UseCurrentWeatherResult`, populated identically to `localDate` in `useCurrentWeather.ts`. **Zero new network calls** — this parses data already present in the existing forecast response, so it stays within "no new Open-Meteo calls."

---

## Layout & Composition (Glass System)

Resolves D-05's "composition is handed to the UI-SPEC" using the CONTEXT.md-recommended default, made concrete and buildable:

**Where the backdrop lives:** the docked `LocationPanel` (`<aside>`, currently `bg-secondary`, fixed 760px, beside the map — never over it) becomes the **static anomaly-gradient backdrop container**. The live map region (`flex-auto` on the left) is untouched — no gradient, no glass, no blur, exactly as Phase 4 left it. This satisfies PERF-01 **by construction**: nothing with `backdrop-blur` ever sits over `.leaflet-container`.

**No-pin / no-data state:** before a pin exists, or while the combined current+baseline fetch is still loading, there is no anomaly value to color by. The panel background stays the flat `--color-secondary` fallback (no gradient) rather than showing a placeholder-colored gradient that would misleadingly imply "this is the anomaly" before one is computed. Once `hasSelection` is true and the fetches are in flight, use `anomalyColor(null)` (i.e., the normal anchor) as the gradient base — this is a legitimate "neutral, waiting" mood, not a misleading claim of a specific reading.

**Three glass cards inside the panel** — one per existing content region, applied to the **root container of every state branch** (empty / loading / error / resolved), not just the happy path, so the glass treatment never flickers/resets between states:
1. `LocationDisplay` root
2. `AnomalyCard` root (the hero — gets stronger surface opacity, see below)
3. `TrendRow` root (including each `TrendDayChart`'s "not enough data" placeholder tile)

Shared glass card treatment:
- Background: `rgba(255,255,255,0.62)` (new token `--color-glass-surface`)
- Border: `1px solid rgba(255,255,255,0.45)` (new token `--color-glass-border`)
- Corner radius: `16px` for the three panel-width cards (new token `--radius-glass-lg`); the small 88px trend-tile "not enough data" box keeps its existing `8px` radius (new token `--radius-glass-sm`, same value as today — no change, just formalized)
- Blur: real `backdrop-blur` at Tailwind's `lg` (16px) on the three panel-width cards only. **Do not** add a second nested blur on the small trend tiles — they already sit inside the already-blurred `TrendRow` card; stacking blur on blur wastes paint cost for no visible gain
- Shadow: `0 8px 30px rgba(15,8,30,0.14)` (new token `--shadow-glass`)
- Padding: reuse existing spacing scale — `px-md py-md` (16px) for the two smaller cards, hero card gets `p-lg` (24px)

**Hero elevation (DESIGN-04 — the hero must read as the unmistakable focal point):** the `AnomalyCard` glass card gets a **more opaque** surface than its siblings — `rgba(255,255,255,0.72)` — plus the standard shadow. This is the mechanism for "strengthened hierarchy": highest surface opacity (best legibility guarantee, satisfying D-06's hard contrast requirement), the color-coded delta at 700-weight/48px, and (by virtue of being first in reading order under the location name) top visual position. The z-score chip stays visually subordinate: keep its existing neutral `bg-secondary`/`text-muted` pill styling unchanged — it must **not** take anomaly-hue coloring; that would compete with the hero delta for "which number is the answer."

**Bridging the computed color into CSS (required, scoped exception to the "no inline style" convention):** `anomalyColor()` produces a per-render, data-driven hex value — this cannot be expressed as a static Tailwind class or a static `@theme` token, the same way the existing static `var(--color-chart-*)` tokens can. The correct, minimal bridge: set a single CSS custom property via inline `style` on the `LocationPanel` root — `style={{ '--anomaly-color': anomalyColor(zScore) } as React.CSSProperties}` — and have all downstream CSS (gradient stops, hero text color) read `var(--anomaly-color)`. This is explicitly **not** a reopening of "no inline style" broadly: it is one narrowly-scoped bridge point for one data-driven value, directly analogous to how recharts already bridges via inline `var(--token)` SVG props. Do not introduce additional inline styles beyond this single custom-property assignment.

**Recommended gradient (implementation guidance, not a hard mandate on exact syntax):**
```css
@property --anomaly-color {
  syntax: '<color>';
  inherits: true;
  initial-value: #57534e;
}
/* registering the custom property makes it natively interpolable for the
   one-time entrance transition below — see Motion & Performance Contract */

.panel-backdrop {
  background: linear-gradient(155deg,
    color-mix(in srgb, var(--anomaly-color) 80%, white 20%) 0%,
    var(--anomaly-color) 55%,
    color-mix(in srgb, var(--anomaly-color) 75%, black 25%) 100%);
}
.panel-backdrop.is-night::before {
  content: '';
  position: absolute;
  inset: 0;
  background: var(--color-atmosphere-night-wash);
  pointer-events: none;
}
```

---

## Motion & Performance Contract (PERF-01, PERF-02, D-10)

- **PERF-01 compliance:** real `backdrop-blur` exists **only** on the three glass cards inside the docked `LocationPanel`, which sits beside — never over — `.leaflet-container`. No component floats over the live map. This is a structural guarantee from the composition above, not a runtime check to add.
- **PERF-02 / motion:** a **one-time entrance transition** when the backdrop or hero recolors on a **new pin** (or on the loading→resolved anomaly transition). Use Tailwind's `motion-safe:` variant directly — it already wraps `prefers-reduced-motion: no-preference` natively, so no custom media query is needed:
  - `motion-safe:transition-colors motion-safe:duration-300 motion-safe:ease-out` on the panel backdrop element and on the hero delta text element.
  - Registering `--anomaly-color` via `@property` (above) is what makes this transition **actually interpolate the color** smoothly rather than snapping — without it, most browsers do not animate an un-registered custom-property-driven gradient.
- **Hard rule:** no `requestAnimationFrame`, no canvas, no JS-driven interval/timeout animation loop anywhere. The entrance transition is pure CSS, triggered implicitly by React re-rendering with a new `--anomaly-color` value — never orchestrated by JS timing code.
- With `prefers-reduced-motion: reduce`, `motion-safe:` utilities emit no transition at all — the backdrop/hero snap instantly to the new color. Verify this is the actual behavior (no fallback JS-driven fade path exists to accidentally bypass the media query).

---

## Recharts Re-theme (DESIGN-05, D-08, D-09)

**Layout is locked** — `TrendYAxisColumn`, the shared Y-axis domain, and `TrendLegend`'s exact copy strings are untouched. Only token **values** and light finish:

| Token | Current | New | Rationale |
|-------|---------|-----|-----------|
| `--color-chart-historical` | `rgba(37, 99, 235, 0.22)` (translucent accent-blue) | `rgba(87, 83, 78, 0.22)` (translucent, echoes `--color-anomaly-normal`) | Ties the historical dot-cloud into the new palette's "calm neutral" mood instead of leftover accent-blue; still a background/context mark, not a foreground signal |
| `--color-chart-mean` | `var(--color-accent)` | **unchanged** — stays `var(--color-accent)` | Accent is already explicitly reserved for exactly this element (the mean `ReferenceLine`); no redefinition needed, this is an intentional exception to "recolor everything" |
| `--color-chart-actual` | `#ea580c` | **unchanged** | Already a fixed, always-orange "this is the real reading" marker per Phase 3's D-02 intent — it is not itself anomaly-hue-encoded (a cold day's actual marker is still orange), so it doesn't need to shift with the new palette; it remains clearly distinguishable from both the recolored historical dots and the (unchanged) mean line |
| Trend tile placeholder box (`bg-dominant border-border-subtle`) | flat white/gray | glass-card treatment (`--color-glass-surface` / `--color-glass-border`, `--radius-glass-sm`) | Visual consistency with the rest of the redesigned panel — copy ("Not enough data") unchanged |

Because `TrendLegend`'s copy describes these three marks by role ("Temperatures on this day in the last 30 years" / "30-year average" / "Temperature now"), not by exact hue, the recolor above keeps every legend sentence true without any copy change.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | Not applicable this phase — no new button/CTA is introduced. The existing "drop a pin on the map" interaction (Phase 1) is unchanged; this phase is a visual re-skin only |
| Empty state heading | "No location selected" (`LocationDisplay`, **locked**, unchanged — restyled onto the glass card, wording untouched) |
| Empty state body | "Click anywhere on the map to drop a pin and see its place name here." (**locked**, unchanged) + AnomalyCard's paired empty line: "Drop a pin to see today's current conditions." (**locked**, unchanged) |
| Error state | "Couldn't compute an anomaly here." (`AnomalyCard`, **locked**, unchanged wording — verify `text-destructive` still meets contrast on the neutral no-anomaly glass card; see UI Considerations) |
| Destructive confirmation | Not applicable — this app has no destructive actions (no delete/remove flows anywhere) |
| **Zero-delta / near-normal fix (DESIGN-06, D-07)** | Change `VERDICT_LABEL.typical` in `src/anomaly/anomaly.ts` from `"Typical for today"` to **`"Right on the 30-year average"`**. Extends across the whole `typical` tier (not just the exact-zero case), per CONTEXT.md's explicit permission ("planning may reasonably extend the same worded framing across the near-normal band"). Combined with `formatDelta`'s existing whole-number, no-decimal rounding (`"0"`, never `"0.0"` — Pitfall 4's deliberate anti-false-precision policy stays intact, do **not** introduce decimal display to match the founder's illustrative "0.0°C" example verbatim), the rendered pair reads: big **"0°C"** immediately followed by **"Right on the 30-year average"** — never a bare, unexplained zero |

---

## UI Considerations

Applicable state considerations resolved: 6 covered, 3 backstop, 0 unresolved.

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | TrendRow (list-collection) | ✅ covered | `TrendRow` returns `null` when `days` is null/empty (Phase 3, unchanged) — no dangling empty glass card renders; the panel simply has one fewer card that render cycle |
| loading | LocationDisplay, AnomalyCard (static-content) | ✅ covered | Existing spinner+text branches restyled onto the glass card; spinner keeps `--color-accent` ring, unchanged copy |
| loading | Panel backdrop color while anomaly is unresolved | 🧪 backstop | Backdrop uses `anomalyColor(null)` (the normal anchor) while current/baseline are in flight — needs a visual check that this neutral "waiting" gradient doesn't read as a misleading "normal weather" claim before data resolves, and that the entrance transition into the real color reads smoothly once resolved |
| error | AnomalyCard error branch (static-content) | 🧪 backstop | Copy is locked/unchanged, but `text-destructive` (#dc2626) now sits on the *neutral-anchor* glass card (not the old flat `bg-secondary`) — needs a contrast check against the new glass surface, not just against the old flat gray |
| populated | TrendRow at full 7-day happy path (list-collection) | ✅ covered | Existing locked layout, only token values/finish change per the re-theme table above |
| partial | Per-day "not enough data" tile mixed with usable tiles (list-collection) | ✅ covered | Existing `usable:false` branch (D-14) restyled to the glass-card treatment; placeholder tiles stay neutral (no anomaly-hue coloring — they represent absent data, not a temperature reading) |
| overflow | Hero delta digit count (static-content) | ✅ covered | `formatDelta`'s whole-number ± format realistically caps at 3 characters including sign; fits the 760px hero card at any `anomalyColor()` value — no truncation risk introduced by this phase |
| zero-one-many | TrendRow item count (list-collection) | ✅ covered — dismissed | Not applicable: `TrendRow` is a fixed 0-or-7-item collection per the Phase 3 contract; this phase introduces no variable-N collection |
| long-text | LocationDisplay place-name heading (static-content) | 🧪 backstop | Wrapping/overflow behavior for an unusually long place name is inherited unchanged from Phase 1, but the new fixed-padding, rounded glass card is a new visual container around it — needs a visual check that a long wrapped 2–3 line name still sits well inside the card's padding/radius rather than looking cramped |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized this phase |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
