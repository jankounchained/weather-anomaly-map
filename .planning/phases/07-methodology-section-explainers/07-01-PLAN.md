---
phase: 07-methodology-section-explainers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/anomaly/anomaly.ts
  - src/anomaly/anomaly.test.ts
  - src/app/DeltaPanel.tsx
  - src/app/DeltaPanel.test.tsx
autonomous: true
requirements:
  - EXPLAIN-04
user_setup: []

must_haves:
  truths:
    - "ROADMAP success criterion 3: the user sees a plain-language percentile framing rendered inside the Delta panel's populated branch, between the verdict line and the z-score chip (PD-06)."
    - "computePercentileRank ranks today against the SAME window samples computeAnomaly uses (via computeWindowSamples), so today's percentile and today's z-score can never disagree on which samples they rank against (PD-05)."
    - "Edge EXPLAIN-04/empty: computePercentileRank returns a clamped 1-99 integer even for a single-sample, all-below, or all-above baseline; computeAnomalyForToday sets percentile to null (no line rendered) whenever zScore is null (PD-04)."
    - "Edge EXPLAIN-04/boundary: the displayed percentile integer is clamped to [1,99] - a value strictly below all samples reads 1 (never 0), strictly above reads 99 (never 100), and one step inside the clamp is preserved unrounded-away (PD-03)."
    - "Edge EXPLAIN-04/adjacency: an exact tie between today and a sample contributes half its count via the Hazen/midrank formula (below + ties/2)/n, so a value matching the sample median reads ~50%, never 0% or 100% (PD-02)."
    - "Edge EXPLAIN-04/ordering: computePercentileRank is a deterministic pure function of (today, samples) with ties resolved by the Hazen convention - identical inputs always yield the same integer, independent of the sample array's order (PD-01/PD-02)."
    - "Edge EXPLAIN-04/precision: the percentile renders as a whole-number integer + '%' (Math.round, no decimals), matching formatDelta's whole-degree precision; the 45-55 inclusive band renders 'Around the middle for this date.', p>55 renders warmer, p<45 renders colder (PD-07, UI-SPEC median band)."
    - "UI-consideration E2/populated: the percentile line renders exactly one of three bounded copy branches - warmer (p>55), colder (p<45), near-median (45-55) - as a whole-number %."
    - "UI-consideration E2/partial (degenerate variance): when anomaly.zScore === null the percentile line is omitted entirely (no placeholder, no empty row), matching the z-chip's own null-variance handling in the same panel (PD-04)."
    - "UI-consideration E2/empty+loading+error (by reference): the percentile line renders only inside DeltaPanel's populated branch; the Empty/Loading/Error branches (Phase 6, unchanged) render no percentile line."
    - "UI-consideration E2/overflow+long-text: the percentile line is a short authored sentence plus a 1-3 digit integer inside the 760px PanelShell column; only the integer varies, so it wraps if needed and never clips."
  artifacts:
    - "src/anomaly/anomaly.ts exports computePercentileRank and percentileLabel and AnomalyForToday gains a percentile field"
    - "src/anomaly/anomaly.test.ts covers Hazen ties, 1-99 clamp bounds, null suppression, and the three percentileLabel branches"
    - "src/app/DeltaPanel.tsx renders the percentile line between verdict and z-chip in the populated branch"
    - "src/app/DeltaPanel.test.tsx asserts percentile ordering and null suppression with percentile-bearing fixtures"
  key_links:
    - "computeAnomalyForToday folds percentile into the AnomalyForToday object DeltaPanel already receives via its existing anomaly prop - no new prop threading, no App.tsx change"
    - "DeltaPanel imports percentileLabel and the extended AnomalyForToday type from ../anomaly/anomaly - the type extension is the compiler-enforced link that forces the test fixtures to add the percentile field"
  prohibitions:
    - statement: "MUST NOT display a percentile line when the baseline cannot support one - when anomaly.zScore is null the line is suppressed entirely; silence is preferred over a misleading number (PD-04)."
    - statement: "MUST NOT compute the percentile from a normal-curve CDF Phi(z); it MUST be the empirical rank of today among the actual window samples, which stays honest on skewed climates and does not overstate confidence at the extremes (PD-01)."
    - statement: "MUST NOT render a percentile line whose warmer/colder direction contradicts the z-score/verdict it sits beside - the warmer/colder framing flips at the median so a cold day never reads 'warmer than N%' (PD-07)."
---

<objective>
Add the empirical percentile framing of the anomaly (EXPLAIN-04): a pair of hand-rolled pure helpers in `src/anomaly/anomaly.ts` (`computePercentileRank` for the math, `percentileLabel` for the copy), a new `percentile` field on `AnomalyForToday`, and a single plain-text percentile line in `DeltaPanel`'s populated branch between the verdict and the z-score chip.

Purpose: give a lay-friendly, distribution-free reading of how unusual today is ("Warmer than 91% of years for this date.") that leads the technical `z 1.3` chip, without drifting from the z-score on which samples it ranks against.
Output: extended `anomaly.ts` (2 new exports + 1 extended interface + 1 wiring line), extended `anomaly.test.ts`, extended `DeltaPanel.tsx` (1 new `<p>`), extended `DeltaPanel.test.tsx` (fixtures + ordering/suppression assertions).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07-methodology-section-explainers/07-CONTEXT.md
@.planning/phases/07-methodology-section-explainers/07-RESEARCH.md
@.planning/phases/07-methodology-section-explainers/07-PATTERNS.md
@.planning/phases/07-methodology-section-explainers/07-UI-SPEC.md

@src/anomaly/anomaly.ts
@src/anomaly/anomaly.test.ts
@src/app/DeltaPanel.tsx
@src/app/DeltaPanel.test.tsx
</context>

<artifacts_produced>
New symbols this plan creates (source-grounding pass MUST exclude these from drift verification — they do not exist yet):
- `computePercentileRank(today: number, samples: number[]): number` — new export in `src/anomaly/anomaly.ts`
- `percentileLabel(percentile: number | null): string | null` — new export in `src/anomaly/anomaly.ts`
- `AnomalyForToday.percentile: number | null` — new field on the existing `AnomalyForToday` interface in `src/anomaly/anomaly.ts`
- New `describe('computePercentileRank', ...)` and `describe('percentileLabel', ...)` blocks in `src/anomaly/anomaly.test.ts`
- Percentile-line assertions + `percentile`-bearing fixtures in `src/app/DeltaPanel.test.tsx`
- No new files, no new CSS, no new design tokens, no new dependencies.
</artifacts_produced>

<planner_assumptions>
Flagged edge-probe assumption (spec-less fallback — surfaced, not silently dropped):
- EXPLAIN-04 `encoding` ("Whose definition of length/equality applies?") — **N/A (heuristic misfire).** `computePercentileRank` operates on numeric samples using JS numeric comparison (`<`) and strict numeric equality (`===`) for ties; there is no string/length/collation/encoding concept in a pure numeric percentile. No acceptance criterion authored.
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| already-fetched Open-Meteo data → derived percentile integer → DOM | The window samples come from the already-validated `baseline.daily` archive fetch and `today` from the already-validated current-weather fetch; the only value crossing into new render code is a clamped 1-99 integer. No user-supplied input reaches this phase's new code paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering / Information Disclosure | `DeltaPanel` percentile `<p>` + `percentileLabel` | low | mitigate | Render the percentile exclusively as an ordinary JSX text node (`{percentileLabel(anomaly.percentile)}`) — never through a raw-HTML sink — preserving the codebase's T-01-02 / T-02-07 / T-06-05 invariant (PD-08). The only dynamic value is a clamped 1-99 integer produced by `computePercentileRank`'s own `Math.max(1, Math.min(99, Math.round(...)))`, string-interpolated only into an authored sentence, so there is no injection surface even in principle. |

**Package installs this phase:** none (RESEARCH.md Package Legitimacy Audit: N/A) — no supply-chain (`T-{phase}-SC`) threat surface.
**ASVS L1, block-on high:** no high-severity open threats. The single low threat is mitigated by the existing JSX-text-node rendering discipline.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add computePercentileRank + percentileLabel pure helpers, extend AnomalyForToday, wire into computeAnomalyForToday, unit-test</name>
  <files>src/anomaly/anomaly.ts, src/anomaly/anomaly.test.ts</files>
  <read_first>
    - src/anomaly/anomaly.ts — the `classifyVerdict`/`verdictLabel` math+copy split (lines 52-63) to mirror; `computeAnomaly` (lines 26-35); the `AnomalyForToday` interface (lines 242-246); `computeAnomalyForToday` (lines 255-269) and its final `return { delta, zScore, verdictTier }` (line 268).
    - src/anomaly/anomaly.test.ts — the existing `describe('computeAnomaly', ...)` / `describe('classifyVerdict', ...)` style (plain `expect(...).toBe(...)`, one behavior per `it`, no `beforeEach`/mocking, plain arrays as fixtures) and the named-import block (lines 1-17).
    - .planning/phases/07-methodology-section-explainers/07-RESEARCH.md — Pattern 1 and the "Existing Vitest test pattern to extend" Code Example (exact assertions to copy).
    - .planning/phases/07-methodology-section-explainers/07-UI-SPEC.md — Copywriting Contract (percentile copy strings) and the "Median-band threshold" (45-55 inclusive) rule.
  </read_first>
  <behavior>
    computePercentileRank(today, samples):
    - Test: computePercentileRank(0, [10,11,12,13]) === 1 (strictly below all → clamped to 1, PD-03).
    - Test: computePercentileRank(100, [10,11,12,13]) === 99 (strictly above all → clamped to 99, PD-03).
    - Test: computePercentileRank(10, [10,20,30,40]) === 13 (below=0, ties=1 → (0+0.5)/4=12.5% → round 13; exact tie is NOT 0/100, PD-02).
    - Test: computePercentileRank(25, [10,20,30,40]) === 50 (at median, no ties, ~50%).
    percentileLabel(percentile):
    - Test: percentileLabel(null) === null (PD-04 suppression).
    - Test: percentileLabel(56) === 'Warmer than 56% of years for this date.' (p>55, PD-07).
    - Test: percentileLabel(44) === 'Colder than 56% of years for this date.' (p<45, reports 100-p, PD-07).
    - Test: percentileLabel(45) === 'Around the middle for this date.' AND percentileLabel(55) === 'Around the middle for this date.' (both inclusive band boundaries).
    computeAnomalyForToday wiring:
    - Test: for a resolved anomaly with non-null zScore, the returned object has an integer `percentile` in [1,99]; when computeAnomaly returns zScore null (zero-variance baseline), the returned `percentile` is null.
  </behavior>
  <action>
    In src/anomaly/anomaly.ts, colocated immediately after `verdictLabel` (near line 63, matching the `classifyVerdict`/`verdictLabel` math+copy pairing per RESEARCH.md Open Questions recommendation):

    1. Add `export function computePercentileRank(today: number, samples: number[]): number` implementing the PD-01/PD-02/PD-03/PD-05 empirical Hazen/midrank rank: count samples strictly below `today`, count exact ties (`=== today`), compute `((below + ties/2) / samples.length) * 100`, and return it clamped with `Math.max(1, Math.min(99, Math.round(raw)))`. Do NOT add an internal zero-variance/`stdDev === 0` guard — per PD-04 the caller only invokes this when its own zScore is non-null (reusing computeAnomaly's ONE degeneracy signal, mirroring hasUsableSampleCount's "one shared definition" discipline). Document the shared-samples contract in the doc comment.

    2. Add `export function percentileLabel(percentile: number | null): string | null` implementing PD-07 + the UI-SPEC median band: return null when `percentile` is null (PD-04 suppression propagated); for `percentile > 55` return the warmer sentence using `percentile`; for `percentile < 45` return the colder sentence using `100 - percentile`; otherwise (45-55 inclusive) return the around-the-middle sentence. Use the EXACT copy strings from the UI-SPEC Copywriting Contract (see acceptance_criteria) — do not paraphrase.

    3. Extend the `AnomalyForToday` interface (lines 242-246) with `percentile: number | null`.

    4. In `computeAnomalyForToday` (lines 255-269), after `const verdictTier = classifyVerdict(zScore ?? 0)`, add `const percentile = zScore === null ? null : computePercentileRank(todayTemp, samples)` (PD-04: reuse the existing zScore-null signal), and include `percentile` in the returned object.

    5. In src/anomaly/anomaly.test.ts, add `computePercentileRank` and `percentileLabel` to the named import from `./anomaly`, then add `describe('computePercentileRank', ...)` and `describe('percentileLabel', ...)` blocks with the cases in the behavior block, mirroring the existing file's style (comments cite the PD number each case defends).
  </action>
  <verify>
    <automated>npx vitest run src/anomaly/anomaly.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function computePercentileRank' src/anomaly/anomaly.ts` returns 1.
    - `grep -c 'export function percentileLabel' src/anomaly/anomaly.ts` returns 1.
    - src/anomaly/anomaly.ts `AnomalyForToday` interface contains a `percentile: number | null` field (`grep -c 'percentile: number | null' src/anomaly/anomaly.ts` >= 1).
    - `computeAnomalyForToday` computes `percentile` via `zScore === null ? null : computePercentileRank(todayTemp, samples)` and returns it (`grep -c 'computePercentileRank(todayTemp, samples)' src/anomaly/anomaly.ts` returns 1).
    - `computePercentileRank`'s body is the filter-below / filter-ties / Hazen / clamp form only — it does NOT re-derive its own variance/degeneracy guard (the caller gates on zScore null per PD-04); confirm by reading the helper (it references `samples` and `today` but introduces no `sd`/variance/`sampleStdDev` call of its own).
    - Exact copy asserted in tests, verbatim: `Warmer than 56% of years for this date.`, `Colder than 56% of years for this date.`, `Around the middle for this date.`
    - Tests assert: `computePercentileRank(0,[10,11,12,13])===1`, `computePercentileRank(100,[10,11,12,13])===99`, `computePercentileRank(10,[10,20,30,40])===13`, `computePercentileRank(25,[10,20,30,40])===50`, `percentileLabel(null)===null`, `percentileLabel(45)` and `percentileLabel(55)` both === 'Around the middle for this date.'
    - `npx vitest run src/anomaly/anomaly.test.ts` exits 0.
  </acceptance_criteria>
  <done>computePercentileRank + percentileLabel exist as pure exports, AnomalyForToday carries percentile, computeAnomalyForToday populates it (null when zScore null), and the new Vitest blocks pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Insert the percentile line into DeltaPanel's populated branch and update DeltaPanel tests</name>
  <files>src/app/DeltaPanel.tsx, src/app/DeltaPanel.test.tsx</files>
  <read_first>
    - src/app/DeltaPanel.tsx — the named import from `../anomaly/anomaly` (lines 16-21); the populated branch (lines 76-104), specifically the verdict `<p className="m-0 text-heading font-heading">` (line 98), the micro-copy `<p className="m-0 text-body font-body">` (lines 95-97, the exact class the percentile line copies), and the z-score chip `<p>` (lines 99-103).
    - src/app/DeltaPanel.test.tsx — the `afterEach(cleanup)` convention (line 8); every `anomaly={{ ... }}` fixture (lines 63, 92, 105, 118); the populated-state ordering test using `container.innerHTML.indexOf(node.outerHTML)` (lines 57-84); the null-variance test (lines 99-110).
    - .planning/phases/07-methodology-section-explainers/07-UI-SPEC.md — Component Inventory ("Plain `<p className="m-0 text-body font-body">` — no new wrapper, no chip") and PD-06 order.
    - .planning/phases/07-methodology-section-explainers/07-PATTERNS.md — Pattern 2 (exact insertion point + fixture-update note).
  </read_first>
  <behavior>
    - Test: populated fixture with `percentile: 91` renders `Warmer than 91% of years for this date.` as a plain `<p>`, positioned in the DOM after the verdict `<p>` and before the z-score chip `<p>` (PD-06 order: verdict → percentile → z-chip).
    - Test: fixture with `zScore: null, percentile: null` renders NO percentile line (queryByText for any "% of years" copy returns null) while still rendering the z-chip no-variance fallback string (PD-04 suppression).
  </behavior>
  <action>
    In src/app/DeltaPanel.tsx:
    1. Add `percentileLabel` to the existing named import from `'../anomaly/anomaly'` (lines 16-21), alongside `formatDelta, isAnomalyReady, verdictLabel, type AnomalyForToday`.
    2. Between the verdict `<p>` (line 98) and the z-score chip `<p>` (line 99), insert a guarded percentile line: render, only when `anomaly.percentile !== null`, a plain `<p className="m-0 text-body font-body">{percentileLabel(anomaly.percentile)}</p>` — matching the existing micro-copy `<p>` class exactly, NO chip/pill wrapper (UI-SPEC Component Inventory), rendered as an ordinary JSX text node only (PD-08). This preserves the locked populated order Δ → micro-copy → verdict → percentile → z-chip (PD-06).

    In src/app/DeltaPanel.test.tsx:
    3. Add a `percentile` field to every existing `anomaly={{ ... }}` fixture (required — the extended `AnomalyForToday` type makes TypeScript fail the build otherwise): line 63 fixture (`slightly-warmer`, zScore 1.8) → `percentile: 91`; line 92 fixture (`typical`, zScore -0.04) → `percentile: 50`; line 105 fixture (`typical`, zScore null) → `percentile: null`; line 118 fixture (`slightly-warmer`, zScore 1.8) → `percentile: 91`.
    4. Extend the populated-state ordering test (lines 57-84) to also grab the percentile line via `getByText('Warmer than 91% of years for this date.')` and assert `html.indexOf(verdict.outerHTML) < html.indexOf(percentileLine.outerHTML)` and `html.indexOf(percentileLine.outerHTML) < html.indexOf(zScoreChip.outerHTML)`.
    5. Add a new suppression test: render the resolved fixture with `zScore: null, percentile: null` and assert `queryByText((t) => t.includes('% of years for this date'))` returns null while the z-chip no-variance string still renders (PD-04).
  </action>
  <verify>
    <automated>npx vitest run src/app/DeltaPanel.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'percentileLabel' src/app/DeltaPanel.tsx` returns 2 (import + call).
    - src/app/DeltaPanel.tsx renders the percentile line guarded by `anomaly.percentile !== null` (`grep -c 'anomaly.percentile !== null' src/app/DeltaPanel.tsx` returns 1) using `<p className="m-0 text-body font-body">` (no `rounded-full`/`bg-secondary` chip classes on the percentile `<p>`).
    - The exact string `Warmer than 91% of years for this date.` is asserted present in the populated ordering test.
    - The ordering test asserts verdict-index < percentile-index < z-chip-index.
    - A suppression test asserts the percentile line is absent when `zScore`/`percentile` are null while `z — (too little variance to compute)` still renders.
    - `npx vitest run src/app/DeltaPanel.test.tsx` exits 0 and `npx tsc --noEmit -p tsconfig.app.json` reports no errors (all fixtures carry the `percentile` field).
  </acceptance_criteria>
  <done>DeltaPanel renders the plain-text percentile line in PD-06 order, suppresses it on null variance, and the updated DeltaPanel tests (ordering + suppression) plus the TypeScript build pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/anomaly/anomaly.test.ts src/app/DeltaPanel.test.tsx` — all green (percentile math, label branches, ordering, suppression).
- `npx tsc --noEmit -p tsconfig.app.json` — no type errors (AnomalyForToday.percentile threaded through all fixtures and the panel).
- Manual read-through: the percentile line sits between the verdict and the z-chip in the populated branch, is a plain `<p>` (no chip), and disappears when variance is degenerate.
</verification>

<success_criteria>
- EXPLAIN-04 satisfied: a plain-language percentile framing renders alongside (above) the z-score chip in the Delta panel.
- The percentile is the empirical Hazen/midrank rank of today among the same window samples used for the z-score (PD-01/PD-02/PD-05), clamped to 1-99 (PD-03), suppressed on null variance (PD-04), warmer/colder-flipped at the median (PD-07), and rendered as a JSX text node only (PD-08).
- No new dependency, no new file, no App.tsx change.
</success_criteria>

<output>
Create `.planning/phases/07-methodology-section-explainers/07-01-SUMMARY.md` when done.
</output>
