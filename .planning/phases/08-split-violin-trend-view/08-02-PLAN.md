---
phase: 08-split-violin-trend-view
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/trend.ts
  - src/app/trend.test.ts
  - src/index.css
autonomous: true
requirements: [TREND-01, TREND-02]
must_haves:
  truths:
    - "buildViolinPaths computes both halves' KDE from ONE shared pooled Silverman bandwidth (silvermanBandwidth([...priorSamples, ...recentSamples]), factor 1.0) — never per-half (TREND-01, spike statistics reference)"
    - "both halves normalize to ONE shared max density per day (PD-06) and both draw to the same maxHalfWidth=36 regardless of sample count (PD-05, equal-width)"
    - "prior half draws LEFT (side -1), recent half draws RIGHT (side +1) (PD-11)"
    - "a half with n>=20 returns { kind:'curve', path, mean, n }; a half with n<20 returns { kind:'rug', points, mean, n } — via halfDrawsCurve (TREND-02, PD-01/PD-02)"
    - "computeSharedYDomain flattens [...recentSamples, ...priorSamples, actual, recentMean, priorMean] across all 7 usable days into one ~8%-padded domain, guarding the empty case with a sane fallback (TREND-01, D-06)"
    - "UI/overflow: each half's curve grid is clamped to that half's own [sampleMin, sampleMax] (not the full padded y-domain), so a curve tail never touches or crosses the tile frame"
    - "the new chart tokens --color-chart-prior-fill/-prior-stroke/-recent-fill/-recent-stroke exist in src/index.css with the UI-SPEC-locked values (PD-12)"
  artifacts:
    - src/app/trend.ts
    - src/app/trend.test.ts
    - src/index.css
  key_links:
    - "buildViolinPaths imports silvermanBandwidth / kdeCurve / halfDrawsCurve from ../anomaly/kde and mean from ../anomaly/anomaly"
    - "ViolinHalf is exported from trend.ts and consumed by TrendDayChart's violinShape in Plan 03"
  prohibitions:
    - "must NOT bias one half's apparent spread by using a per-half bandwidth instead of the shared pooled bandwidth"
---

<objective>
Add the pure split-violin geometry to `src/app/trend.ts` — `buildViolinPaths()`
(port of the validated spike geometry) and the `ViolinHalf` output type — and
extend `computeSharedYDomain` to flatten both halves. Add the new recent/prior
`--color-chart-*` token pair to `src/index.css` at the UI-SPEC-locked values.

Purpose: turn two per-half KDE curves into closed SVG half-paths (or rug points)
on the shared temperature scale, honoring equal-width halves, one shared per-day
density peak, and prior-left / recent-right — the geometry the tile render (Plan
03) draws. Keeping this in the dependency-free `trend.ts` module mirrors the
existing `jitterX` / `buildHistoricalPoints` / `computeSharedYDomain` convention.
Output: `buildViolinPaths`, `ViolinHalf`, an extended `computeSharedYDomain`,
and four new CSS chart tokens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-split-violin-trend-view/08-CONTEXT.md
@.planning/phases/08-split-violin-trend-view/08-PATTERNS.md
@.planning/phases/08-split-violin-trend-view/08-UI-SPEC.md
@.claude/skills/spike-findings-gsd-test/references/split-violin-geometry.md
@src/app/trend.ts
@src/anomaly/kde.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildViolinPaths geometry + ViolinHalf type + extended computeSharedYDomain</name>
  <files>src/app/trend.ts, src/app/trend.test.ts</files>
  <read_first>
    - `src/app/trend.ts` (`jitterX`, `buildHistoricalPoints`, `computeSharedYDomain` — the pure-geometry style + empty-array guard to mirror)
    - `.claude/skills/spike-findings-gsd-test/sources/003-split-violin-tile/violin.mjs` (verified `buildViolinDay` geometry to port: shared pooled bandwidth, sharedMax density, halfPath, side -1/+1)
    - `.claude/skills/spike-findings-gsd-test/references/split-violin-geometry.md` (non-negotiables: shared max density, equal width, clamp tails, prior-left/recent-right)
    - `.planning/phases/08-split-violin-trend-view/08-UI-SPEC.md` (Violin Geometry & Marks Contract; constants cx=44, maxHalfWidth=36, steps=96; clamp-to-sampleMin/Max rule)
    - `src/anomaly/kde.ts` (silvermanBandwidth, kdeCurve, halfDrawsCurve signatures) · `src/anomaly/types.ts` (two-sample TrendDayResult)
    - `src/app/trend.test.ts` (Vitest convention to extend)
  </read_first>
  <behavior>
    - computeSharedYDomain over 7 usable two-sample days returns one [min,max] covering every recentSamples/priorSamples value plus each day's actual and both means, padded ~10%; returns [0,1] for all-unusable input (empty guard)
    - buildViolinPaths(recent, prior, opts) uses ONE shared pooled bandwidth silvermanBandwidth([...prior,...recent]) for BOTH kdeCurve calls (not two per-half bandwidths)
    - buildViolinPaths returns { prior, recent }; prior.side draws left (path x <= cx), recent draws right (path x >= cx)
    - both halves scale by one shared max density: given identical recent/prior samples, the two half-paths are mirror images about cx
    - a half with n>=20 returns kind:'curve' with a non-empty SVG path string starting with "M" and ending with "Z"; a half with n<20 returns kind:'rug' with points:[{y}] of length n and mean null when n===0
    - both halves draw to the same maxHalfWidth regardless of n (a 55-sample and a 275-sample half both reach maxHalfWidth at their peak)
    - each half's curve grid is clamped to that half's own [min(samples), max(samples)] so the path does not span the full padded domain
  </behavior>
  <action>
    Extend `computeSharedYDomain` (keep the exact empty-array guard + 10% pad):
    change the flatten from `[...day.samples, day.actual, day.mean]` to
    `[...day.recentSamples, ...day.priorSamples, day.actual, day.recentMean,
    day.priorMean]` for usable days. Update the doc-comment (still cites D-06).
    Add `export type ViolinHalf =
    | { kind: 'curve'; path: string; mean: number; n: number }
    | { kind: 'rug'; points: { y: number }[]; mean: number | null; n: number }`.
    Add `export function buildViolinPaths(recentSamples, priorSamples, opts)`
    porting spike `buildViolinDay`, adapted to TS/no-React and to use ONE shared
    pooled bandwidth ALWAYS (drop the spike's `bandwidthMode` param — statistics
    reference: always shared). `opts = { yMin, yMax, plotTop, plotHeight, cx,
    maxHalfWidth, steps = 96 }`. Build both kdeCurves with the shared `h`; compute
    `sharedMax = max(peak(recentCurve), peak(priorCurve)) || 1` (PD-06); half path
    `x = cx + side * (density/sharedMax) * maxHalfWidth`, side=-1 prior / +1
    recent, closing top→edge→bottom→Z at cx. Clamp each half's curve grid to that
    half's own [min,max] sample range (NOT the padded domain) so tails never kiss
    the frame. Choose curve vs rug via `halfDrawsCurve(samples.length)`; rug
    returns pixel-y `points` and `mean` (null when empty). Import
    silvermanBandwidth/kdeCurve/halfDrawsCurve from `../anomaly/kde` and `mean`
    from `../anomaly/anomaly`. Extend `trend.test.ts` covering every behavior
    above (mirror-image symmetry, curve/rug boundary at n=20, equal max width,
    clamp range, extended domain).
  </action>
  <verify>
    <automated>npm test -- src/app/trend.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'export function buildViolinPaths' src/app/trend.ts` and `grep -q 'export type ViolinHalf' src/app/trend.ts`
    - `grep -q 'silvermanBandwidth(' src/app/trend.ts` used once over the POOLED array (no two per-half silvermanBandwidth calls) — a test asserts identical recent/prior samples yield mirror-image paths
    - `grep -q 'recentSamples' src/app/trend.ts` (computeSharedYDomain flattens both halves)
    - `npm test -- src/app/trend.test.ts` exits 0 with tests proving: n=20 → kind:'curve', n=19 → kind:'rug'; both halves reach maxHalfWidth; curve grid clamped to sample min/max
    - a test proves computeSharedYDomain returns [0,1] for all-unusable days (empty guard preserved)
  </acceptance_criteria>
  <done>
    trend.ts exports buildViolinPaths + ViolinHalf and an extended
    computeSharedYDomain; one shared pooled bandwidth, shared density peak, equal
    width, prior-left/recent-right, clamped tails, curve/rug via halfDrawsCurve;
    trend.test.ts passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add recent/prior violin chart tokens to index.css</name>
  <files>src/index.css</files>
  <read_first>
    - `src/index.css` (existing `--color-chart-historical/-mean/-actual` block lines 16-21; `--color-anomaly-normal` #57534e / `--color-anomaly-hot` #9a3412 anchors)
    - `.planning/phases/08-split-violin-trend-view/08-UI-SPEC.md` (Color section — the four locked token values and their rationale)
    - `.planning/phases/08-split-violin-trend-view/08-PATTERNS.md` (index.css section)
  </read_first>
  <action>
    Add four tokens alongside the existing `--color-chart-*` block, using the
    UI-SPEC Color section's LOCKED values (authoritative — these supersede any
    earlier draft hues in the geometry reference):
    `--color-chart-prior-fill: rgba(87, 83, 78, 0.24)`,
    `--color-chart-prior-stroke: #57534e` (= --color-anomaly-normal),
    `--color-chart-recent-fill: rgba(154, 52, 18, 0.26)`,
    `--color-chart-recent-stroke: #9a3412` (= --color-anomaly-hot).
    Add a short comment tying them to PD-12 and the anomaly-scale palette (prior =
    neutral historical grey, recent = warm "one to watch"). Do NOT touch
    `--color-chart-mean` / `--color-chart-actual` / `--color-chart-historical`
    (PD-12: mean ticks, diamond, and rug dots keep their existing tokens).
  </action>
  <verify>
    <automated>grep -Eq -- '--color-chart-prior-fill:\s*rgba\(87, 83, 78, 0\.24\)' src/index.css && grep -Eq -- '--color-chart-recent-stroke:\s*#9a3412' src/index.css && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - all four new tokens present with the exact UI-SPEC values (`grep` confirms prior-fill/prior-stroke/recent-fill/recent-stroke)
    - existing `--color-chart-mean`, `--color-chart-actual`, `--color-chart-historical` unchanged (grep still finds their original values)
    - `npm run build` succeeds (Tailwind v4 @theme compiles the new tokens)
  </acceptance_criteria>
  <done>
    Four new violin chart tokens exist at the locked values; the three retained
    marks' tokens are untouched; the build compiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure math → geometry | kde.ts outputs shaped into SVG path strings / pixel points here |
| geometry → render | ViolinHalf path/points consumed by SVG render in Plan 03 |

## STRIDE Threat Register (ASVS L1 · block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Tampering | buildViolinPaths density normalization (divide by sharedMax) | medium | mitigate | `sharedMax || 1` guard prevents divide-by-zero; kdeCurve densities are finite (Plan 01 guards); unit-tested |
| T-08-04 | Spoofing (visual misrepresentation) | per-half vs pooled bandwidth choice | medium | mitigate | ONE shared pooled bandwidth enforced + test asserts mirror symmetry, so shape differences reflect data not sample-size artifact (spike finding) |
| T-03-07 | Injection (raw-HTML sink) | SVG path strings | low | mitigate | Path strings are numeric-only `d` attributes rendered as JSX in Plan 03; no HTML sink (breadcrumb) |
| T-08-SC | Tampering (supply chain) | npm installs | low | accept | No new packages — geometry hand-rolled in trend.ts |
</threat_model>

<verification>
- `npm test -- src/app/trend.test.ts` passes (geometry: shared bandwidth, shared
  density, equal width, prior-left/recent-right, clamp, curve/rug boundary,
  extended domain)
- `npm run build` compiles the four new CSS tokens and typechecks trend.ts
- grep confirms exactly one pooled silvermanBandwidth call (no per-half bandwidth)
</verification>

<success_criteria>
- buildViolinPaths + ViolinHalf produce closed half-paths (or rug points) on the
  shared scale with one pooled bandwidth, one shared density peak, equal width,
  prior-left/recent-right, clamped tails
- computeSharedYDomain flattens both halves + actual + both means
- Four locked recent/prior chart tokens added; retained-mark tokens untouched
</success_criteria>

<output>
Create `.planning/phases/08-split-violin-trend-view/08-02-SUMMARY.md` when done.
</output>
