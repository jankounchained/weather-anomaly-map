---
phase: 08-split-violin-trend-view
plan: 04
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/app/TrendLegend.tsx
  - src/app/TrendLegend.test.tsx
autonomous: false
requirements: [TREND-03]
must_haves:
  truths:
    - "the trend legend renders 5 items whose native-SVG swatches mirror the surviving tile marks: prior-25yr left half, recent-5yr right half, the per-half mean tick, the actual-value diamond, and the rug 'too few years → dots' (TREND-03, PD-10)"
    - "all legend labels are static JSX text nodes and all swatches are native SVG shapes — never a raw-HTML sink and never a Recharts custom-label HTML injection (T-03-07)"
    - statement: "the 5-item legend renders with swatches matching the surviving marks; final wording is set by the reviewer copy round-trip"
      verification: backstop
  artifacts:
    - src/app/TrendLegend.tsx
    - src/app/TrendLegend.test.tsx
  key_links:
    - "legend swatches reference the SAME --color-chart-* tokens the tiles render (prior/recent fill+stroke, --color-chart-mean, --color-chart-actual, --color-chart-historical) so the key can never visually drift from the marks it explains"
    - "legend copy is finalized through the PD-10 reviewer round-trip (blocking checkpoint), not written unilaterally"
---

<objective>
Rebuild `TrendLegend` for the split-violin marks: replace the old dot / 30-year
line / diamond key with a 5-item key naming the prior-25yr half, the recent-5yr
half, the per-half mean tick, the preserved actual-value diamond, and the rug
"too few years → dots" fallback — swatches drawn as native SVG mirroring the
tile marks. Route the copy through the PD-10 reviewer round-trip (the same
process that finalized the Phase 3 legend) before treating any wording as final.

Purpose: TREND-03 — the legend is the reader's key to the new visualization; if
it still describes marks the violin replaced, the tile is unreadable. PD-10
explicitly forbids writing the final copy unilaterally.
Output: a 5-item `TrendLegend` with reviewer-approved copy and tests.
</objective>

## Artifacts this phase produces

Newly-created symbols (exclude from source-grounding / drift verification):

- **Modified component:** `TrendLegend` (`src/app/TrendLegend.tsx`) — rebuilt from a 3-item to a 5-item key (prior half, recent half, mean tick, actual diamond, rug/dots); reuses the existing `TrendLegendItem` wrapper (not new)
- **Modified test:** `src/app/TrendLegend.test.tsx` — asserts the 5 items + swatch shapes and the reviewer-approved final copy
- **No new exported symbols** — this plan only changes legend items + copy, finalized via the PD-10 reviewer round-trip

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-split-violin-trend-view/08-CONTEXT.md
@.planning/phases/08-split-violin-trend-view/08-PATTERNS.md
@.planning/phases/08-split-violin-trend-view/08-UI-SPEC.md
@src/app/TrendLegend.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild TrendLegend with the 5 split-violin key items (draft copy)</name>
  <files>src/app/TrendLegend.tsx, src/app/TrendLegend.test.tsx</files>
  <read_first>
    - `src/app/TrendLegend.tsx` (whole file — `TrendLegendItem` wrapper to reuse verbatim, the 3 existing swatch patterns circle/rect/polygon, `role="list"` container, native-SVG-only discipline)
    - `.planning/phases/08-split-violin-trend-view/08-UI-SPEC.md` (Copywriting Contract legend table — the 5 DRAFT labels + swatch mapping; note "DRAFT, PD-10 requires a reviewer round-trip")
    - `.planning/phases/08-split-violin-trend-view/08-PATTERNS.md` (TrendLegend.tsx section — swatch/token mapping)
    - `src/app/TrendLegend.test.tsx` (existing 3-item test to rewrite)
    - `src/index.css` (the new prior/recent tokens from Plan 02, plus existing mean/actual/historical tokens)
  </read_first>
  <action>
    Reuse `TrendLegendItem` verbatim and the `flex flex-row flex-wrap gap-md`
    `role="list"` container. Replace the 3 items with 5, using the UI-SPEC
    Copywriting Contract DRAFT labels + swatches:
    (1) "Prior 25 years" — left-half violin-shape swatch in
    --color-chart-prior-fill / --color-chart-prior-stroke;
    (2) "Recent 5 years" — right-half violin-shape swatch in
    --color-chart-recent-fill / --color-chart-recent-stroke;
    (3) "Average for that period" — short horizontal tick swatch in
    --color-chart-mean (ONE swatch for both per-half ticks, since they share the
    token, per PD-07);
    (4) "Temperature now" — diamond swatch in --color-chart-actual with 1px white
    stroke (item 4 kept verbatim from today's legend);
    (5) "Too few years → shown as dots" — small translucent dot-cluster swatch in
    --color-chart-historical (new — explains the rug fallback before a user hits
    it). Every label is a static string literal rendered as an ordinary JSX text
    node; every swatch is native SVG — never a raw-HTML sink. Mark the copy in a
    code comment as DRAFT pending the PD-10 reviewer round-trip (Task 2). Rewrite
    `TrendLegend.test.tsx` to assert 5 legend items, the 5 swatch shapes present
    (two violin-shape swatches, a mean-tick swatch, a diamond polygon, a dot
    circle), and `role="list"`.
  </action>
  <verify>
    <automated>npm test -- src/app/TrendLegend.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'var(--color-chart-prior-fill)' src/app/TrendLegend.tsx` and `grep -q 'var(--color-chart-recent-fill)' src/app/TrendLegend.tsx` (both half swatches use the new tokens)
    - `grep -q 'var(--color-chart-historical)' src/app/TrendLegend.tsx` (rug/dot item present) and `grep -q 'var(--color-chart-actual)' src/app/TrendLegend.tsx` (diamond item kept)
    - `[ "$(grep -c 'dangerouslySetInnerHTML' src/app/TrendLegend.tsx)" = "0" ]` (native SVG only — T-03-07)
    - `npm test -- src/app/TrendLegend.test.tsx` exits 0 with a test asserting exactly 5 legend items and the 5 expected swatch shapes
  </acceptance_criteria>
  <done>
    TrendLegend renders 5 native-SVG key items (draft copy) mapped to the
    surviving marks; the old dot/30-year-line/diamond key is replaced; tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PD-10 reviewer copy round-trip for the split-violin legend (blocking checkpoint)</name>
  <files>(none — reviewer sign-off; any returned wording is applied back into src/app/TrendLegend.tsx + src/app/TrendLegend.test.tsx)</files>
  <read_first>
    - `src/app/TrendLegend.tsx` (the 5 draft items just built in Task 1)
    - `.planning/phases/08-split-violin-trend-view/08-UI-SPEC.md` (Copywriting Contract legend table + the "DRAFT, PD-10 requires a reviewer round-trip" note; UI Considerations legend backstop)
    - `.planning/phases/08-split-violin-trend-view/08-CONTEXT.md` (PD-10 — legend re-derived, finalized via reviewer copy round-trip, same process as the Phase 3 legend)
  </read_first>
  <action>
    Run the PD-10 reviewer copy round-trip on the 5 draft legend labels — the same
    process that finalized the Phase 3 legend. Present the rendered legend, walk
    the reviewer through the how-to-verify steps, and record their verbatim
    verdict. Do NOT auto-approve (this checkpoint ignores workflow.auto_advance).
    If the reviewer returns revised wording, apply the EXACT returned string(s)
    into TrendLegend.tsx and its test, then re-run the legend test before closing.
  </action>
  <what-built>
    A rebuilt 5-item TrendLegend for the split-violin trend view (draft copy from
    the UI-SPEC Copywriting Contract): "Prior 25 years", "Recent 5 years",
    "Average for that period", "Temperature now", "Too few years → shown as dots",
    each with a native-SVG swatch mirroring the tile mark it explains.
  </what-built>
  <how-to-verify>
    This is the PD-10 reviewer copy round-trip (same process that finalized the
    Phase 3 legend) — the UI-SPEC leaves the exact wording UNLOCKED on purpose.
    1. Run `npm run dev`, drop a pin, and view the "Last 7 Days" panel legend
       beneath the tiles (or inspect the 5 rendered swatches + labels).
    2. Confirm each of the 5 labels correctly and legibly names its mark:
       prior-25yr half, recent-5yr half, the per-half mean tick, the
       actual-value "temperature now" diamond, and the thin-side rug/dots.
    3. Confirm no surviving item still describes a mark the violin removed (e.g.
       the old full-width "30-year average" line).
    4. Reply "approved" to lock the draft as-is, OR return the exact revised
       string(s) for any item — the executor then applies your exact wording and
       re-runs `npm test -- src/app/TrendLegend.test.tsx` before finishing.
  </how-to-verify>
  <resume-signal>Reply "approved", or provide the exact final wording for any legend item(s).</resume-signal>
  <verify>
    <human-check>Reviewer confirms all 5 legend items legibly name their surviving marks (prior half, recent half, mean tick, actual diamond, rug/dots) with no leftover item describing a removed mark; their verbatim verdict (or revised wording) is recorded in the SUMMARY.</human-check>
  </verify>
  <acceptance_criteria>
    - Reviewer explicitly signs off on the final legend wording (verbatim verdict recorded in the SUMMARY) — this is the PD-10 round-trip, never a silent pass and never auto-approved.
    - If wording was revised, the exact returned strings are applied in TrendLegend.tsx and `npm test -- src/app/TrendLegend.test.tsx` passes with the final copy.
    - The final legend has no item describing a mark the violin removed (no full-width "30-year average" line item).
  </acceptance_criteria>
  <done>
    The 5-item split-violin legend copy is reviewer-approved via the PD-10
    round-trip (verdict recorded); any revised wording is applied and re-tested —
    closing TREND-03's legend requirement.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static legend copy → DOM | fixed string literals + native SVG swatches; no data-driven or user-authored text |

## STRIDE Threat Register (ASVS L1 · block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Injection (raw-HTML sink) | legend labels + swatches | high | mitigate | Labels are static JSX text nodes; swatches are native SVG; acceptance criterion greps the sink count to 0 — no HTML injection surface |
| T-08-07 | Repudiation (unauthorized copy change) | final legend wording | low | mitigate | PD-10 blocking reviewer checkpoint records explicit sign-off; wording is not silently finalized |
| T-08-SC | Tampering (supply chain) | npm installs | low | accept | No new packages — native SVG swatches only |
</threat_model>

<verification>
- `npm test -- src/app/TrendLegend.test.tsx` passes (5 items + swatch shapes)
- `grep -c 'dangerouslySetInnerHTML' src/app/TrendLegend.tsx` is 0 (T-03-07)
- The PD-10 reviewer checkpoint shows explicit sign-off (backstop — not a silent
  pass); any returned wording is applied and re-tested before the plan closes
</verification>

<success_criteria>
- The legend is a 5-item key correctly explaining every surviving split-violin
  mark, with no leftover item describing a removed mark
- All labels are JSX text / all swatches native SVG (T-03-07)
- Final copy is reviewer-approved via the PD-10 round-trip, not written
  unilaterally
</success_criteria>

<output>
Create `.planning/phases/08-split-violin-trend-view/08-04-SUMMARY.md` when done.
</output>
