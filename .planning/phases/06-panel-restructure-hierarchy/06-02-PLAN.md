---
phase: 06-panel-restructure-hierarchy
plan: 02
type: execute
wave: 2
depends_on: ['06-01']
files_modified:
  - src/app/LocationDisplay.tsx
  - src/app/TrendRow.tsx
  - src/app/TrendRow.test.tsx
autonomous: true
requirements: [LAYOUT-02]

must_haves:
  truths:
    - "The Location panel shows a 'Location' headline eyebrow (via PanelHeadline) above its existing place-name h2 in every branch, matched to the 'Last 7 Days' style (LAYOUT-02)."
    - "The History panel shows the 'Last 7 Days' headline (via PanelHeadline) in every state, including empty/loading/error — the headline is no longer only present on the populated chart (LAYOUT-02)."
    - "LocationDisplay renders inside a PanelShell (tokenized glass card) in all four of its branches (empty / loading / resolved-with-name / coordinate-fallback), preserving each branch's existing behavior (PD-08 full migration)."
    - "TrendRow renders inside a PanelShell(as='section') and its chart-internals block (TrendYAxisColumn + 7× TrendDayChart + TrendLegend) is byte-for-byte unchanged so Phase 8 can rebuild on the stable wrapper (PD-08 chart internals unchanged)."
    - "History (Last 7 Days) empty state shows exactly 'Drop a pin to see the last 7 days of temperatures here.' (UI-SPEC Copywriting Contract; closes the pre-split gap where the trend panel rendered nothing on empty)."
    - "History (Last 7 Days) loading state shows exactly 'Loading the last 7 days…' (UI-SPEC Copywriting Contract)."
    - "History (Last 7 Days) error state shows exactly 'Recent history unavailable for this location.' (UI-SPEC Copywriting Contract)."
    - "History populated state renders the existing fixed 7-day chart (TrendDayChart × 7 on the shared Y-axis) unchanged, with the PanelHeadline eyebrow added above it (UI-SPEC UI Considerations: History populated)."
    - "History partial state (some days missing) follows the already-shipped TrendRow/TrendDayChart behavior — no new partial-data handling introduced (UI-SPEC UI Considerations: History partial)."
    - "History overflow is a non-risk: the fixed 7-day window is bounded content inside the 760px PanelShell column (UI-SPEC UI Considerations: History overflow)."
    - "History zero-one-many is a non-risk: the window is always exactly 7 days, so there is no singular/plural copy or spacing variance (UI-SPEC UI Considerations: History zero-one-many)."
    - "The Location place name (dynamic length from reverse-geocode) wraps naturally inside the 760px PanelShell; the new 'Location' eyebrow sits above it and does not add truncation logic (UI-SPEC UI Considerations: Location place name long-text)."
    - "All dynamic text in LocationDisplay (place name, coordinates) and TrendRow (day values via unchanged chart internals) renders as plain JSX text nodes only — no raw-HTML sink (T-01-02 / T-02-07)."
  artifacts:
    - "src/app/LocationDisplay.tsx — migrated to PanelShell + PanelHeadline('Location')"
    - "src/app/TrendRow.tsx — migrated to PanelShell + PanelHeadline('Last 7 Days') with empty/loading/error/populated state branches"
    - "src/app/TrendRow.test.tsx — new state-branch + headline coverage"
  key_links:
    - "TrendRow gains props `hasSelection`, `currentStatus`, `baselineStatus` and imports isAnomalyReady from ../anomaly/anomaly to decide its loading branch — App.tsx (plan 06-03) passes these; this is why 06-03 depends on this plan's TrendRow interface."
    - "LocationDisplay and TrendRow both import PanelShell + PanelHeadline from plan 06-01; TrendRow keeps its existing imports of computeSharedYDomain, TrendDayChart/TrendYAxisColumn, TrendLegend for the unchanged chart block."
---

<objective>
Migrate the two already-shipped panels (Location and History) onto the shared primitives so all four panels enter Phases 7-8 on one consistent system: LocationDisplay adopts `PanelShell` + a new `PanelHeadline("Location")` eyebrow, and TrendRow swaps its outer glass `<section>` + inline eyebrow for `PanelShell` + `PanelHeadline("Last 7 Days")` AND gains the UI-SPEC-authored empty/loading/error state branches so the History panel is self-explanatory in every state like the others (PD-04 spirit, phase goal).

Purpose: LAYOUT-02 requires a consistent headline on the Location and History panels; PD-08 mandates full migration onto the primitives; the UI-SPEC's UI Considerations author empty/loading/error copy for History that closes the pre-split gap where the trend panel rendered nothing.

Output: LocationDisplay + TrendRow rebuilt on the primitives, with History's four states wired to the authored copy. Chart internals stay untouched for Phase 8.
</objective>

## Artifacts this phase produces (this plan's share)

- `src/app/LocationDisplay.tsx` — modified: PanelShell + PanelHeadline("Location")
- `src/app/TrendRow.tsx` — modified: PanelShell + PanelHeadline("Last 7 Days") + empty/loading/error/populated branches (new props `hasSelection`, `currentStatus`, `baselineStatus`)
- `src/app/TrendRow.test.tsx` — new tests

Scope note vs PATTERNS.md: the pattern map lists TrendRow as a two-line wrapper swap; this plan additionally wires the UI-SPEC-authored empty/loading/error state branches (an explicit, documented extension of PD-08, required because the APPROVED UI-SPEC marks those History states as covered/closed this phase). The chart-internals block (TrendYAxisColumn + TrendDayChart × 7 + TrendLegend) remains byte-for-byte unchanged so Phase 8's dependency on a stable wrapper holds.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-panel-restructure-hierarchy/06-UI-SPEC.md
@.planning/phases/06-panel-restructure-hierarchy/06-PATTERNS.md
@.planning/phases/06-panel-restructure-hierarchy/06-CONTEXT.md
@src/app/PanelShell.tsx
@src/app/PanelHeadline.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate LocationDisplay onto PanelShell + PanelHeadline("Location")</name>
  <files>src/app/LocationDisplay.tsx</files>
  <read_first>
    - src/app/LocationDisplay.tsx (target — 4 branches at lines 25-66, each opening with the duplicated glass div; keep all branch behavior)
    - src/app/PanelShell.tsx and src/app/PanelHeadline.tsx (the primitives from plan 06-01 to import and compose)
    - .planning/phases/06-panel-restructure-hierarchy/06-UI-SPEC.md (Copywriting Contract row "Panel headline — Location" = "Location"; the h2 place-name and coordinate-fallback are unchanged behavior)
    - .planning/phases/06-panel-restructure-hierarchy/06-PATTERNS.md (LocationDisplay section)
  </read_first>
  <behavior>
    - Empty branch (!hasSelection): PanelShell contains PanelHeadline "Location" then the existing "No location selected" h2 + helper copy.
    - Loading branch (status !== 'resolved'): PanelShell + PanelHeadline "Location" + existing spinner + "Looking up place name…".
    - Resolved-with-name branch: PanelShell + PanelHeadline "Location" + the place-name h2.
    - Coordinate-fallback branch: PanelShell + PanelHeadline "Location" + the formatCoords line.
  </behavior>
  <action>
    Replace the duplicated glass `<div className="flex flex-col gap-sm bg-glass-surface ...">` (and the loading branch's `<div ... role="status">`) in each of LocationDisplay's four branches with `<PanelShell>` (default div; the loading branch keeps `role="status"` — pass it through, or wrap the spinner row inside PanelShell while preserving the status semantics). Add `<PanelHeadline>Location</PanelHeadline>` as the first child of every branch, above the existing h2/p content. Preserve every branch's existing copy and structure (the "No location selected" empty state, the spinner + "Looking up place name…" loading state, the place-name h2, and the silent coordinate fallback via formatCoords — D-02). Keep the no-raw-HTML-sink comment/invariant. Import PanelShell + PanelHeadline from './PanelShell' and './PanelHeadline'.
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/app/</automated>
  </verify>
  <acceptance_criteria>
    - Every LocationDisplay branch renders a PanelHeadline whose text is exactly "Location".
    - Every branch renders inside PanelShell (grep confirms the raw duplicated glass class string is no longer inline in LocationDisplay.tsx — the glass classes now live only in PanelShell): the file imports PanelShell/PanelHeadline and references neither `bg-glass-surface` nor `backdrop-blur-lg` directly.
    - The loading branch preserves `role="status"`.
    - The place-name h2 and coordinate-fallback (`formatCoords`) branches are behaviorally unchanged.
    - `npm run build` type-checks and the existing app test suite still passes.
  </acceptance_criteria>
  <done>LocationDisplay renders through PanelShell with a "Location" eyebrow in all four branches; build + tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate TrendRow onto the primitives and wire its four states (LAYOUT-02 + History UI Considerations)</name>
  <files>src/app/TrendRow.tsx, src/app/TrendRow.test.tsx</files>
  <read_first>
    - src/app/TrendRow.tsx (target — line 37 outer section, line 38 inline eyebrow, lines 41-59 chart-internals block that MUST stay unchanged, current `if (days === null || days.length === 0) return null` at line 30)
    - src/app/PanelShell.tsx and src/app/PanelHeadline.tsx (primitives from 06-01)
    - src/anomaly/anomaly.ts (isAnomalyReady from 06-01 — used for the loading branch)
    - src/weather/types.ts (WeatherStatus type for the new props)
    - src/app/LocationDisplay.tsx (spinner markup pattern for the loading state — size-4 rounded-full border-2 border-accent border-t-transparent animate-location-spin, role="status")
    - .planning/phases/06-panel-restructure-hierarchy/06-UI-SPEC.md (Copywriting Contract rows "History (Last 7 Days) — empty/loading/error state" for the EXACT strings; UI Considerations rows for History)
    - src/app/TrendLegend.test.tsx (test style: render + getByText + queryByText, fireEvent, vanilla assertions)
  </read_first>
  <behavior>
    - Empty (!hasSelection): PanelShell(as="section") + PanelHeadline "Last 7 Days" + exactly "Drop a pin to see the last 7 days of temperatures here."
    - Loading (hasSelection && !isAnomalyReady(currentStatus, baselineStatus)): PanelShell + headline + spinner (role="status") + exactly "Loading the last 7 days…".
    - Error (isAnomalyReady but days is null/empty — resolved with no usable recent series): PanelShell + headline + text-destructive "Recent history unavailable for this location.".
    - Populated (days is a non-empty array): PanelShell + headline + the UNCHANGED chart block (Y-axis column + 7× TrendDayChart + TrendLegend).
  </behavior>
  <action>
    Change `TrendRowProps` to add `hasSelection: boolean`, `currentStatus: WeatherStatus`, `baselineStatus: WeatherStatus` alongside the existing `days` and `units`. Import `WeatherStatus` from '../weather/types' and `isAnomalyReady` from '../anomaly/anomaly'. Replace the single `return null` early-return with sequential early-return branches mirroring LocationDisplay's pattern: empty → loading → error → populated, using the EXACT UI-SPEC copy strings above. Every branch renders `<PanelShell as="section">` with `<PanelHeadline>Last 7 Days</PanelHeadline>` as its first child (so the headline shows in all four states). In the populated branch, keep `const yDomain = computeSharedYDomain(days)` and the existing chart-internals JSX (the `<div className="flex flex-row items-start gap-sm">` block with TrendYAxisColumn + the mapped TrendDayChart list + `<TrendLegend />`) byte-for-byte unchanged — only its wrapping `<section>`/eyebrow changes to PanelShell/PanelHeadline. The loading branch reuses the shared spinner markup (size-4 rounded-full border-2 border-accent border-t-transparent animate-location-spin, aria-hidden) inside a row with role="status". Preserve the no-raw-HTML-sink invariant. Create TrendRow.test.tsx asserting: the "Last 7 Days" headline is present in the empty, loading, and error states (getByText), each state's exact copy string renders, and a populated render (a minimal non-empty `days` array of one usable TrendDayResult) still renders the chart (e.g. an svg or the TrendLegend labels) — use fireEvent/vanilla assertions, no jest-dom, no user-event.
  </action>
  <verify>
    <automated>npx vitest run src/app/TrendRow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TrendRowProps includes `hasSelection`, `currentStatus`, `baselineStatus`; the loading branch routes through the shared predicate — grep: `grep -c 'isAnomalyReady' src/app/TrendRow.tsx` returns ≥1 (no re-derived inline resolved-status comparison).
    - Empty state renders exactly "Drop a pin to see the last 7 days of temperatures here.".
    - Loading state renders exactly "Loading the last 7 days…" and a role="status" element.
    - Error state renders exactly "Recent history unavailable for this location." with text-destructive.
    - The "Last 7 Days" PanelHeadline is present in the empty, loading, and error states (not only populated).
    - The populated chart block is unchanged: `grep -c 'TrendYAxisColumn' src/app/TrendRow.tsx` returns ≥1 and `grep -c 'TrendLegend' src/app/TrendRow.tsx` returns ≥1, and a populated render still renders the chart.
    - TrendRow.test.tsx passes.
  </acceptance_criteria>
  <done>TrendRow renders through PanelShell/PanelHeadline in all four states with the exact authored copy, keeps the chart internals untouched, and its tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reverse-geocode response → LocationDisplay DOM | The place name comes from an external BigDataCloud reverse-geocode call and is rendered into the panel; a tampered response must not be able to inject markup. |
| Open-Meteo archive/forecast data → TrendRow chart DOM | Historical/recent daily values flow through the (unchanged) chart internals; rendered as chart marks/text, never as raw HTML. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering | LocationDisplay place-name / coordinate rendering | low | mitigate | Place name and coordinates render as plain JSX text nodes only (existing T-01-02 invariant preserved through the PanelShell migration — no dangerouslySetInnerHTML introduced). |
| T-06-04 | Tampering | TrendRow state-copy + unchanged chart internals | low | mitigate | New state-branch copy is static authored text; chart values render through the unchanged TrendDayChart marks (JSX/SVG text nodes only). No raw-HTML sink. |

No new dependencies are installed this phase, so no supply-chain (T-*-SC) checkpoint applies. No secrets, auth, network, or persistence surface is introduced (existing Open-Meteo/geocode data flow is consumed upstream in App.tsx, unchanged).
</threat_model>

<verification>
- `npx vitest run src/app/TrendRow.test.tsx` passes; `npm run build` type-checks the new TrendRow props.
- `npx vitest run src/app/` (full app suite) passes — existing LocationDisplay/Trend tests still green after the migration.
- `grep -c 'isAnomalyReady' src/app/TrendRow.tsx` returns ≥1 (loading gate goes through the shared predicate, not a re-derived inline comparison).
</verification>

<success_criteria>
- Location and History panels both carry a consistent PanelHeadline eyebrow matched to the "Last 7 Days" style (LAYOUT-02).
- History panel is self-explanatory in empty/loading/error/populated states using the exact UI-SPEC copy.
- TrendRow's chart-internals block is unchanged (Phase 8 wrapper stability preserved).
- No new design tokens; all glass styling flows through PanelShell.
</success_criteria>

<output>
Create `.planning/phases/06-panel-restructure-hierarchy/06-02-SUMMARY.md` when done.
</output>
