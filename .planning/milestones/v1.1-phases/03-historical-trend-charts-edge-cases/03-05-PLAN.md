---
phase: 03-historical-trend-charts-edge-cases
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/TrendDayChart.tsx
  - src/app/TrendRow.tsx
  - src/app/TrendLegend.tsx
  - src/app/TrendLegend.test.tsx
  - src/app/App.css
  - .planning/phases/03-historical-trend-charts-edge-cases/03-UI-SPEC.md
autonomous: true
gap_closure: true
requirements: [VIZ-01, VIZ-02]
must_haves:
  truths:
    - "Each usable day's distribution is legible 'at a glance' at its slot width — the average is emphasized without hiding the spread — and the leftmost tile's plot area now matches its 6 siblings, not visually squished (VIZ-02, D-01/D-02/D-03)"
    - "The trend row is legible at a glance without prior explanation: a persistent legend explains the pale dots = 30-year historical values (D-01), the bright line = 30-year average (D-03), and the orange diamond = that day's actual reading (D-02) (VIZ-02)"
  artifacts:
    - "src/app/TrendDayChart.tsx"
    - "src/app/TrendRow.tsx"
    - "src/app/TrendLegend.tsx"
    - "src/app/App.css"
    - ".planning/phases/03-historical-trend-charts-edge-cases/03-UI-SPEC.md"
  key_links:
    - "The leftmost day tile's plot-area width equals the other 6 tiles — shared y-axis labeling no longer steals plotting width from within a fixed slot; the YAxis gets an explicit narrow width so Recharts does not reserve its ~60px default"
    - "The legend swatches consume the same --color-chart-historical / --color-chart-mean / --color-chart-actual tokens the charts render, so the key always matches the marks"
    - "No horizontal scroll is introduced in the location panel — the trend-row width stays within the panel content width (panel widened if the axis labeling needs it, and 03-UI-SPEC.md's width math updated to match)"
---

<objective>
Close the two blocking VIZ-02 "legible at a glance" gaps found by the 03-04 human-verify checkpoint (see 03-VERIFICATION.md `gaps:` block):

1. **Gap 1 — leftmost tile squished.** The leftmost trend tile (index 0 of 7) renders visually cramped vs. its 6 siblings because `showYAxis={index === 0}` gives only that slot visible Y-axis tick labels, and those labels eat into the same fixed `CHART_WIDTH` (88px) plotting area the other 6 slots use entirely for the dot cloud / mean line / diamond. Equalize every tile's plot area.
2. **Gap 2 — no legend.** No persistent key exists explaining what the pale dots, bright line, and orange diamond represent; the per-diamond hover `<title>` only surfaces this on hover, per day. Add a glanceable legend.

Both fixes also update 03-UI-SPEC.md so the design contract matches the corrected implementation (the current UI-SPEC text describes the exact Y-axis approach that caused Gap 1, and its Component Inventory has no legend entry).

Purpose: VIZ-02's bar is "legible at a glance." The mechanics shipped in Plans 01-03 are wired and unit-tested, but the phase's stated value — a first-time viewer reading the week at a glance — is unmet until the row reads as 7 equal tiles with a self-explaining key.

Output: An equal-plot-area small-multiples row, a new `TrendLegend` component, matching CSS + color-token reuse, and an updated 03-UI-SPEC.md. No new network calls, no new dependencies. Authoritative pass/fail is deferred to the fresh human-verify checkpoint in 03-06.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-historical-trend-charts-edge-cases/03-CONTEXT.md
@.planning/phases/03-historical-trend-charts-edge-cases/03-UI-SPEC.md
@.planning/phases/03-historical-trend-charts-edge-cases/03-VERIFICATION.md
@.planning/phases/03-historical-trend-charts-edge-cases/03-04-SUMMARY.md
@src/app/TrendDayChart.tsx
@src/app/TrendRow.tsx
@src/app/App.css
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Equalize the leftmost tile's plot area (Gap 1) + trivial render-path hardening</name>
  <files>src/app/TrendDayChart.tsx, src/app/TrendRow.tsx, src/app/App.css, .planning/phases/03-historical-trend-charts-edge-cases/03-UI-SPEC.md</files>
  <read_first>
    - src/app/TrendDayChart.tsx — the `ComposedChart width={CHART_WIDTH}` (88px, fixed) and the `YAxis ... hide={!showYAxis}` at the root of Gap 1. Note the YAxis currently has NO explicit `width` prop, so Recharts reserves its ~60px default inside the 88px chart on the one slot where it is visible — that is the squish.
    - src/app/TrendRow.tsx — `showYAxis={index === 0}` and the `.trend-row__charts` composition of the 7 tiles.
    - src/app/App.css — `.location-panel` (`flex: 0 0 720px; width: 720px;`), `.trend-row__charts` (`gap: var(--space-sm)` = 8px), `.trend-day` (`width: 88px`). Current row width math: 7×88 + 6×8 = 664px; panel content = 720 − 2×16 (var(--space-md) padding) = 688px; slack = 24px.
    - src/index.css — the `--color-chart-*` / `--color-muted` tokens (already defined; reuse, do not add new ones for Gap 1).
    - 03-UI-SPEC.md — "Chart Visual Encoding" → the "Y-axis (temperature)" row and the "Per-slot footprint" table (both currently describe the defective approach); "Layout & Interaction Notes" → the panel-width math.
    - 03-VERIFICATION.md `gaps[0]` and 03-04-SUMMARY.md "Issue 1" — the confirmed root cause and the two sanctioned fix directions.
  </read_first>
  <action>
    Make all 7 day tiles present an identical plot area so the leftmost is no longer squished. Approach is your discretion (03-VERIFICATION.md and 03-04-SUMMARY.md explicitly leave it open) — pick ONE of these, or a better one you can justify:

    - **Recommended — approach (b), single shared axis column:** render the y-axis labeling exactly once, in its own narrow column to the LEFT of the 7-tile row, and make every `TrendDayChart` render an identical (axis-hidden) chart. This makes all 7 tiles pixel-identical, which is the most faithful answer to the reviewer's exact words ("This tile doesn't look like the rest"). Keep the standalone axis in the same family as the charts (fixed explicit width/height matching the 120px chart body and the shared `yDomain` — no auto-sizing responsive wrapper, since jsdom has no ResizeObserver). Remove the per-index `showYAxis` asymmetry from TrendRow.tsx.
    - **Acceptable — approach (a), per-tile compensation:** keep the leftmost tile's YAxis visible but give it an explicit narrow `width` (so Recharts stops reserving ~60px) AND widen only the leftmost tile's total chart width by that same axis width, so its plot area equals the other 6. Requires a leftmost-slot CSS modifier so the wider chart is not clipped.

    Whichever you choose, ALL of these invariants are mandatory (they are what the 03-06 checkpoint will re-verify):
    1. Every one of the 7 day tiles has an equal plot-area width — the dot cloud / mean line / diamond occupy the same horizontal region in tile 0 as in tiles 1-6.
    2. Any visible YAxis gets an explicit narrow `width` prop — never rely on the Recharts default reservation.
    3. NO horizontal scroll is introduced in the location panel. Recompute the row width for your approach and compare against the 688px panel content width. If the axis labeling needs more than the current 24px slack, widen `.location-panel` (BOTH `flex: 0 0 <w>px` and `width: <w>px` in App.css — they must stay equal) just enough to fit; `.map-region` (`flex: 1 1 auto`) absorbs the difference on the app's desktop-first ≥1280px target. Do not add a horizontal scrollbar and do not shrink the plot below a legible size.
    4. Keep the existing prohibitions intact: no auto-sizing responsive chart wrapper (explicit width/height only), the mean `ReferenceLine` keeps `ifOverflow="visible"`, jitter stays deterministic (no non-deterministic random source).
    5. Update 03-UI-SPEC.md to describe the CORRECTED approach: rewrite the "Y-axis (temperature)" row of the "Chart Visual Encoding" table and the "Per-slot footprint" table so they document how the axis is now labeled (once, in its own column — or the leftmost-tile compensation you chose), and update the "Layout & Interaction Notes" panel-width math to the new numbers if you widened the panel.

    Also apply two trivial render-path hardenings from 03-REVIEW.md, scoped to `src/app/TrendDayChart.tsx` ONLY (this file is already open for the width fix; do NOT touch trend.ts, client.ts, or anomaly.ts):
    - IN-01: in `formatActualTooltip`, guard the constructed `Date` — if `Number.isNaN(date.getTime())`, fall back to the raw `dateStr` rather than letting `toLocaleDateString` throw a RangeError during render.
    - IN-03: replace the `units ?? ''` fallback so a null `units` does not leave a dangling degree symbol in the tooltip (use a sensible default unit).

    Follow the codebase doc-comment convention: every touched export keeps its rationale + requirement-ID JSDoc; if you add a standalone axis component, give it a doc comment citing D-06/VIZ-02 and the Gap-1 fix.
  </action>
  <verify>
    <automated>npm test -- src/app/TrendDayChart.test.tsx src/app/trend.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (`tsc -b && vite build`) exits 0.
    - Existing `src/app/TrendDayChart.test.tsx` and `src/app/trend.test.ts` still pass unchanged (the placeholder/real-chart branches and the shared-domain helpers are untouched in behavior).
    - `grep -c "width={" src/app/TrendDayChart.tsx` shows the YAxis now receives an explicit width (or the axis is factored into its own explicit-width component) — no code path leaves a visible Recharts axis at its default reserved width.
    - No auto-sizing responsive chart wrapper in the production trend files: `grep -c 'ResponsiveContainer' src/app/TrendDayChart.tsx src/app/TrendRow.tsx` (and the new axis component if any) each return 0. (Scope this grep to production `.tsx` files only — `TrendDayChart.test.tsx` legitimately names it in a comment.)
    - `grep -c 'ifOverflow="visible"' src/app/TrendDayChart.tsx` is unchanged (mean line still never silently discards).
    - In `src/app/App.css`, `.location-panel`'s `flex-basis` and `width` values are still equal to each other (if you widened the panel, both changed together).
    - 03-UI-SPEC.md "Chart Visual Encoding" Y-axis row and "Per-slot footprint" table now describe the corrected axis approach (no longer "Only the leftmost ... chart renders visible tick labels" inside a shared fixed 88px width).
  </acceptance_criteria>
  <done>The leftmost tile's plot area equals the other 6 (no squish), any visible axis has an explicit width, no horizontal scroll appears in the panel, the build and existing unit tests pass, and 03-UI-SPEC.md matches the corrected approach.</done>
</task>

<task type="auto">
  <name>Task 2: Add a persistent legend explaining the dot / line / diamond marks (Gap 2)</name>
  <files>src/app/TrendLegend.tsx, src/app/TrendLegend.test.tsx, src/app/TrendRow.tsx, src/app/App.css, .planning/phases/03-historical-trend-charts-edge-cases/03-UI-SPEC.md</files>
  <read_first>
    - src/app/TrendRow.tsx — where the legend mounts (inside the `.trend-row` section, adjacent to `.trend-row__charts`).
    - src/app/LocationPanel.tsx and src/app/AnomalyCard.tsx — the codebase's one-component-per-file convention and the "all dynamic text renders as ordinary JSX text nodes, never a raw-HTML sink" discipline to mirror.
    - src/index.css — the `--color-chart-historical` (D-01 dot), `--color-chart-mean` (D-03 line), `--color-chart-actual` (D-02 diamond), and `--color-muted` tokens the legend swatches + labels must reuse verbatim (the key must match the marks exactly).
    - src/app/TrendDayChart.tsx — the exact shapes drawn (a translucent `<circle r={3}>` for historical dots, a 2px `ReferenceLine` for the mean, a `<polygon>` diamond with 1px white stroke for the actual value) so the legend swatches visually match them.
    - 03-VERIFICATION.md `gaps[1]` and 03-04-SUMMARY.md "Issue 2" — the exact missing affordance.
    - 03-UI-SPEC.md — "Component Inventory", "Copywriting Contract", and "Chart Visual Encoding" sections to extend with the new legend.
  </read_first>
  <action>
    Create a new `TrendLegend` component in `src/app/TrendLegend.tsx` (one-component-per-file, matching LocationPanel/AnomalyCard) that renders a persistent, glanceable key with three entries, each a small native-SVG swatch beside a short label:
    - Pale/translucent dot swatch (a `<circle>` filled `var(--color-chart-historical)`) — label "Each of the last 30 years" (the 30-year historical distribution, D-01).
    - Bright line swatch (a short horizontal `<line>`/`<rect>` stroked/filled `var(--color-chart-mean)`) — label "30-year average" (the historical mean, D-03).
    - Orange diamond swatch (a `<polygon>` filled `var(--color-chart-actual)` with a 1px white stroke) — label "That day's temperature" (the day's actual reading, D-02).

    Render all swatches and labels as plain JSX text nodes and native SVG only — never a raw-HTML sink and never a Recharts custom-label HTML injection (the copy is static string literals anyway, so there is no free-text/injection surface, but keep the codebase discipline). Reuse the exact `--color-chart-*` tokens so the key can never drift from the marks. Match the swatch shapes to what TrendDayChart actually draws (circle, line, diamond).

    Mount `<TrendLegend />` inside `TrendRow`'s `.trend-row` section, directly below the `.trend-row__charts` row (recommended) or between the heading and the charts — your discretion, but it must be persistently visible without hovering. The legend renders alongside the 7 charts whenever the row renders; it does not need its own gating (TrendRow already returns null on no-data).

    Add `.trend-legend*` styles to `src/app/App.css`: a horizontal flex row that wraps if needed, `var(--space-sm)`/`var(--space-md)` gaps, label text at `--font-size-label` (14px) in `--color-muted`, small fixed-size swatches (~12-16px). No new card/border chrome (consistent with the borderless trend row). Do NOT introduce new spacing tokens, font sizes, or color tokens — reuse the existing scale.

    Add a light render test `src/app/TrendLegend.test.tsx` (mirroring TrendDayChart.test.tsx's testing-library style): assert all three legend labels render, and assert the component renders SVG swatches (querySelector on `svg`/`circle`/`polygon`).

    Update 03-UI-SPEC.md:
    - Add a `TrendLegend` row to the "Component Inventory" table (responsibility: persistent key for the three chart marks).
    - Add the three legend labels to the "Copywriting Contract" table (exact copy strings).
    - Note the legend in the "Chart Visual Encoding" / "Color" sections as the persistent, at-a-glance key that reuses the three `--color-chart-*` tokens.
  </action>
  <verify>
    <automated>npm test -- src/app/TrendLegend.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/TrendLegend.tsx` exists and exports a `TrendLegend` component; `TrendRow.tsx` imports and renders it.
    - `src/app/TrendLegend.test.tsx` passes and asserts the three labels ("Each of the last 30 years", "30-year average", "That day's temperature" — or your final approved copy, kept in sync with the UI-SPEC Copywriting Contract) plus the presence of SVG swatches.
    - `grep -rin legend src/` now returns ≥1 hit (closes the 03-VERIFICATION.md Gap-2 grep gate: `grep -rin legend src/` was 0).
    - No raw-HTML sink in the new/modified files: `grep -c 'dangerouslySetInnerHTML' src/app/TrendLegend.tsx src/app/TrendRow.tsx` each return 0.
    - `grep -c 'var(--color-chart-historical)\|var(--color-chart-mean)\|var(--color-chart-actual)' src/app/TrendLegend.tsx` shows all three tokens reused (the key matches the marks).
    - `npm run build` exits 0.
    - 03-UI-SPEC.md Component Inventory has a `TrendLegend` entry and the Copywriting Contract lists the three legend labels.
  </acceptance_criteria>
  <done>A persistent legend renders with the 7-chart row explaining the pale dots (30-year values), the bright line (30-year average), and the orange diamond (that day's reading), using the same color tokens as the marks; its test passes; the build passes; and 03-UI-SPEC.md documents the new component and copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Open-Meteo API → chart layer | Numeric daily/historical values already validated upstream in `getCurrentWeather`/`getHistoricalBaseline` (T-03-01, Plan 02) before reaching the trend components |
| Static legend/axis copy → DOM | New legend labels are static string literals; axis tick text is Open-Meteo-derived numeric temperatures — no free-text/user input crosses into this phase's new surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering / XSS | src/app/TrendLegend.tsx, src/app/TrendRow.tsx, src/app/TrendDayChart.tsx (new legend copy + explicit axis labels) | low | mitigate | All new legend/axis content renders as plain JSX text nodes and native SVG (circle/line/polygon swatches, numeric ticks) — no raw-HTML sink, no Recharts custom-label HTML injection. Legend copy is static literals (zero injection surface); axis ticks are already-validated finite numbers. Grep gate in Task 2 acceptance criteria (`grep -c 'dangerouslySetInnerHTML' … == 0`). |
| T-03-05 | Denial of Service (client-side crash) | src/app/TrendDayChart.tsx render path | low | mitigate | Carried from Plan 03. The IN-01 defensive `Date` guard added in Task 1 prevents a malformed `dateStr` from throwing a RangeError in the tooltip render path; upstream numeric data remains validated (T-03-01). |
| T-03-02 | Tampering / XSS | src/app/TrendRow.tsx / TrendDayChart.tsx (existing marks/labels) | low | accept | Unchanged from Plan 03 — existing weekday labels, "Not enough data" copy, and the actual-marker `<title>` remain JSX/native-SVG only; this plan adds no raw-HTML sink to them. |

No package-manager install occurs in this plan (recharts was installed in Plan 01 under T-03-SC) — no supply-chain checkpoint or `[ASSUMED]`/`[SUS]` gate is required here.
</threat_model>

<verification>
- The leftmost trend tile's plot area equals its 6 siblings (no squish), verified structurally now (equal-width invariant, explicit axis width, build + existing tests green) and authoritatively by the 03-06 human-verify checkpoint against live data.
- A persistent legend explains the three marks; `grep -rin legend src/` ≥1 and the TrendLegend render test passes.
- No horizontal scroll is introduced in the location panel.
- All existing prohibitions hold: no auto-sizing responsive chart wrapper in production trend files, `ifOverflow="visible"` retained, deterministic jitter retained, no raw-HTML sink.
</verification>

<success_criteria>
- VIZ-02 (Gap 1): the 7-tile small-multiples row reads as 7 equal tiles — the leftmost is no longer squished.
- VIZ-02 (Gap 2): a persistent, glanceable legend makes the dot/line/diamond language readable without hovering.
- 03-UI-SPEC.md is updated so the design contract matches the corrected axis approach and documents the new legend.
- Build passes; existing unit tests pass; the new TrendLegend test passes; no new dependencies, no new network calls.
</success_criteria>

## Artifacts this plan produces

- **New:** `src/app/TrendLegend.tsx` — `TrendLegend` component (persistent key for the three chart marks).
- **New:** `src/app/TrendLegend.test.tsx` — render test for the legend.
- **New CSS classes:** `.trend-legend` and its item/swatch/label sub-classes in `src/app/App.css`.
- **Possibly new:** a standalone axis component/render (if approach (b) is chosen) — an explicit-width, marks-less axis rendered once to the left of the row.
- **Modified:** `src/app/TrendDayChart.tsx` (explicit YAxis width / equalized plot area + IN-01/IN-03 hardening), `src/app/TrendRow.tsx` (mounts the legend; drops the per-index axis asymmetry), `src/app/App.css` (leftmost-tile / panel-width adjustments + legend styles), `.planning/phases/03-historical-trend-charts-edge-cases/03-UI-SPEC.md` (corrected Y-axis + footprint + new legend contract).
- **No new color/spacing/typography tokens** — all reuse the existing `src/index.css` scale.

<output>
Create `.planning/phases/03-historical-trend-charts-edge-cases/03-05-SUMMARY.md` when done, recording the axis approach chosen, any panel-width change, the final legend copy, and the UI-SPEC sections updated.
</output>
