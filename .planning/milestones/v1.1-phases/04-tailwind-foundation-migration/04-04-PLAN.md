---
phase: 04-tailwind-foundation-migration
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - src/app/App.css
autonomous: false
requirements: [STYLE-02, STYLE-03, STYLE-04]
user_setup: []

must_haves:
  truths:
    - "The legacy hand-written component stylesheet is deleted and no file imports it; no BEM class remains in any component .tsx"
    - "The hand-written index.css form is gone — index.css is now the Tailwind entry — so both original hand-written stylesheets are removed (STYLE-02)"
    - "Every var(--color-*) referenced from TSX resolves to a name defined in the @theme block (the four chart/muted tokens align)"
    - "tsc -b (forced clean) and vite build exit 0, and the full vitest run suite passes (STYLE-04)"
    - "The Leaflet map (tiles, zoom controls, attribution, draggable pin) and all AnomalyCard/LocationDisplay/TrendRow/TrendLegend states render visually equivalent to v1.0 under Preflight (STYLE-03, STYLE-01)"
  artifacts:
    - "src/app/App.css deleted from the working tree"
    - "Verification evidence recorded in the plan SUMMARY (build/test output + manual visual-equivalence result)"
  key_links:
    - "Preflight (global, layer base) + Leaflet's own unlayered !important img/svg rules -> map renders correctly without any scoped override (verified visually, not assumed)"
  prohibitions:
    - text: "MUST NOT make any visual/aesthetic change; the bar is visual equivalence to v1.0 (the sharper look is Phase 5)"
      status: flagged
    - text: "MUST NOT rename or alter the token names --color-chart-historical, --color-chart-mean, --color-chart-actual, --color-muted"
      status: flagged
    - text: "MUST NOT re-theme the recharts trend visuals; chart re-theming is Phase 5 / DESIGN-05"
      status: flagged
    - text: "MUST NOT introduce a tailwind.config.js; Tailwind v4 CSS-first only"
      status: flagged
    - text: "MUST NOT leave any component importing the legacy hand-written stylesheet, and MUST delete it by end of phase"
      status: flagged
    - text: "MUST NOT change app feature/behavior of any kind"
      status: flagged
---

<objective>
Tear down the now-dead legacy stylesheet and prove the migration is complete and visually equivalent to v1.0. Delete the legacy component stylesheet, run the repo-wide static gates (no BEM classes remain, nothing imports the legacy stylesheet, every TSX `var(--color-*)` resolves), run the full production build + Vitest suite, and perform the mandatory manual visual-equivalence walkthrough of the map and every component state.

Purpose: close STYLE-02 (legacy hand-written CSS removed, no component depends on it), STYLE-04 (production build + test suite green with Tailwind), and confirm STYLE-03 (Leaflet under Preflight) plus STYLE-01 (visual equivalence) hold. A green build + test run is NOT proof of visual parity — the Vitest suite only asserts SVG element structure — so the manual check is required, not optional.

Output: `App.css` deleted; static + build + test gates green; manual visual-equivalence confirmed.

Edge-probe note: STYLE-01..04 are mechanical/structural requirements — the deterministic edge-probe found no input-driven edge cases. The genuine risk surface is migration completeness (static gates) and visual equivalence (the manual check), both encoded in this plan's `must_haves` and tasks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-tailwind-foundation-migration/04-CONTEXT.md
@.planning/phases/04-tailwind-foundation-migration/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the legacy stylesheet and run the static completeness gates</name>
  <files>src/app/App.css</files>
  <read_first>
    - src/app/App.css (the legacy component stylesheet being removed — confirm every rule it holds is now migrated to utilities/@theme before deleting)
    - src/app/App.tsx (confirm plan 02 already removed the import of this stylesheet)
    - .planning/phases/04-tailwind-foundation-migration/04-CONTEXT.md (D-02: both hand-written stylesheets deleted by end of phase)
  </read_first>
  <action>
    Confirm plans 02 and 03 migrated all seven components (App, LocationPanel, LocationDisplay, AnomalyCard, TrendRow, TrendDayChart, TrendLegend) to utilities and that App.tsx no longer imports the legacy component stylesheet. Then delete the legacy component stylesheet listed in this task's `<files>` via `git rm` (it is now fully dead — no element references its classes and nothing imports it). Do not delete or alter `src/index.css` (that is the Tailwind entry). Then run the repo-wide static gates in `<verify>`: (a) no BEM class strings remain in any component `.tsx`, (b) nothing imports the deleted stylesheet, and (c) the four load-bearing `--color-*` token names are both defined in the entry CSS and still referenced from the trend components — proving no rename slipped through.
  </action>
  <!-- planner-discipline-allow: App.css -->
  <verify>
    <automated>! test -f src/app/App.css && ! grep -rq "App.css" src/ && ! grep -rEq "anomaly-card|location-display|location-panel|trend-row|trend-day|trend-legend|app-shell|map-region" src/app --include=*.tsx && for t in "var(--color-chart-historical)" "var(--color-chart-mean)" "var(--color-chart-actual)" "var(--color-muted)"; do grep -Frq "$t" src/app --include=*.tsx || { echo "TSX no longer references $t"; exit 1; }; done && for d in --color-chart-historical --color-chart-mean --color-chart-actual --color-muted; do grep -q -- "$d" src/index.css || { echo "entry CSS missing $d"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/app/App.css` is false (the legacy stylesheet no longer exists).
    - `grep -rq "App.css" src/` returns nothing (no file imports or references it).
    - The repo-wide BEM negative grep over `src/app/*.tsx` returns nothing (no BEM class survives).
    - All four `var(--color-chart-*)`/`var(--color-muted)` references still exist in the trend components AND their `--color-*` definitions still exist in `src/index.css` (no rename slipped through).
  </acceptance_criteria>
  <done>The legacy component stylesheet is deleted, nothing imports it, no BEM class remains, and every load-bearing token name still aligns between entry CSS and TSX.</done>
</task>

<task type="auto">
  <name>Task 2: Full production build + complete Vitest suite (STYLE-04)</name>
  <files>src/</files>
  <read_first>
    - .planning/phases/04-tailwind-foundation-migration/04-RESEARCH.md (Pitfall 5: tsc -b incremental cache can mask a stale error — force a clean build; Pitfall 4: green tests are not proof of visual parity)
    - package.json (the build script is `tsc -b && vite build`; tests are `vitest run`)
  </read_first>
  <action>
    Run a forced-clean production build and the complete test suite. Remove any stale TypeScript build cache first (`rm -f tsconfig.tsbuildinfo` / `tsconfig.*.tsbuildinfo`) to avoid a cached false pass, then run `npx tsc -b --force` followed by `npx vite build`, then the full `npx vitest run`. All must exit 0. If the build fails on an unknown utility or a malformed arbitrary value, fix the offending className in the component it came from (do not weaken the gate). Do not modify test files to make them pass — the suite must pass as written.
  </action>
  <verify>
    <automated>rm -f tsconfig.tsbuildinfo tsconfig.*.tsbuildinfo 2>/dev/null; npx tsc -b --force && npx vite build && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b --force` exits 0 (clean type-check, no cached pass).
    - `npx vite build` exits 0 and emits a static `dist/` (Tailwind compiled; all utilities/arbitrary values valid).
    - `npx vitest run` exits 0 with the full pre-existing suite passing and no test files modified.
  </acceptance_criteria>
  <done>The production build and the complete Vitest suite pass with Tailwind in place (STYLE-04).</done>
</task>

<task type="auto">
  <name>Task 3: Manual visual-equivalence walkthrough (Preflight/Leaflet + all states)</name>
  <files>src/</files>
  <read_first>
    - .planning/phases/04-tailwind-foundation-migration/04-RESEARCH.md (Verification Protocol / Pitfall 1 + Pitfall 4: the map must be visually checked, not assumed; green build/tests do not prove visual parity)
    - .planning/phases/04-tailwind-foundation-migration/04-CONTEXT.md (success criterion #2: the map — zoom/attribution/tile/draggable pin — must render and behave correctly)
  </read_first>
  <action>
    Serve the built app for a browser visual comparison against v1.0. The automated gate confirms the app builds into a loadable bundle; the human-check performs the actual visual-equivalence comparison (the Vitest suite asserts only SVG structure, so it cannot prove visual parity). If the map region collapses to zero height or tiles/markers/controls look wrong, the `[&_.leaflet-container]` sizing or a Preflight leak is the suspect — RESEARCH.md expects NO scoped override to be needed, so only add a narrowly-scoped fix if the check actually reveals a regression, and route that back as a gap.
  </action>
  <verify>
    <automated>npx vite build</automated>
    <human-check>
      Run `npm run dev`, open the app, and compare against v1.0 (use a `git stash` of the pre-migration checkout or v1.0 screenshots). Confirm each is visually equivalent:
      1. Map / Preflight-Leaflet (STYLE-03): tiles render at correct size (not oversized/native), the map fills the full map region height, zoom +/- controls show their button chrome, attribution sits bottom-right, and the pin is draggable + click-to-place works.
      2. LocationPanel: fixed 760px width, secondary background, vertical scroll when content overflows.
      3. LocationDisplay — all states: empty, loading (row layout + spinning ring), resolved (heading), fallback (label).
      4. AnomalyCard — all four states: empty, loading (spinner), error (destructive-red body), resolved (hero delta noticeably largest, info button circle, verdict, muted z-score pill on secondary background).
      5. TrendRow + TrendDayChart: heading eyebrow, shared Y-axis column aligned with the 7 tiles, today label bold+accent, not-enough-data placeholder card with its border.
      6. TrendLegend: swatch colors (historical dot, mean line, actual marker) match v1.0 and legend copy is unchanged.
      Reply "approved" if visually equivalent, or list the specific differences.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npx vite build` exits 0 (the app the reviewer loads is buildable).
    - Human reviewer confirms the map (tiles, zoom controls, attribution, draggable pin) and every component state render visually equivalent to v1.0.
    - Any regression found is captured for gap closure rather than silently accepted.
  </acceptance_criteria>
  <done>The map and all component states are confirmed visually equivalent to v1.0 by a human reviewer; STYLE-01/STYLE-03 hold.</done>
</task>

</tasks>

<threat_model>
Security enforcement: ASVS L1, block_on: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Deletes a CSS file and runs read-only verification — no new inputs, network calls, auth, or data handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | stylesheet deletion + build/test/visual verification | low | accept | Removing dead CSS and verifying introduces no attack surface. Supply-chain threat T-04-SC is handled by plan 01's install checkpoint. |

No threats at or above `high` in this plan's scope.
</threat_model>

<verification>
- Static gates: legacy stylesheet deleted, nothing imports it, no BEM class remains, token names aligned (Task 1).
- Build/test gates: forced-clean `tsc -b --force`, `vite build`, and full `vitest run` all exit 0 (Task 2).
- Visual gate: mandatory human walkthrough of the map + all component states vs v1.0 (Task 3 `<human-check>`).
</verification>

<success_criteria>
- Both original hand-written stylesheets removed; no component depends on them (STYLE-02).
- Production build and full test suite pass with Tailwind in place (STYLE-04).
- The Leaflet map renders/behaves correctly under Preflight and the UI is visually equivalent to v1.0 (STYLE-03, STYLE-01) — confirmed by the manual walkthrough.
</success_criteria>

<artifacts_this_phase_produces>
Phase-4 artifacts finalized here (see plan 01 for token/plugin artifacts):
- `src/app/App.css` deleted (the last hand-written component stylesheet removed).
- No new symbols/exports — this plan removes a file and verifies.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-tailwind-foundation-migration/04-04-SUMMARY.md` when done.
</output>
