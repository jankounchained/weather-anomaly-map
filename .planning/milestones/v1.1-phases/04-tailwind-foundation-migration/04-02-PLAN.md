---
phase: 04-tailwind-foundation-migration
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/app/App.tsx
  - src/app/LocationPanel.tsx
  - src/app/LocationDisplay.tsx
  - src/app/AnomalyCard.tsx
autonomous: true
requirements: [STYLE-01, STYLE-02, STYLE-03]
user_setup: []

must_haves:
  truths:
    - "The app shell, map region, LocationPanel, LocationDisplay (empty/loading/resolved/fallback), and AnomalyCard (empty/loading/error/resolved) render visually equivalent to v1.0 using only Tailwind utilities"
    - "The Leaflet map still fills its region: the .leaflet-container width/height:100% sizing is preserved via an arbitrary-variant utility on the map div (MapContainer sets no explicit height)"
    - "App.tsx no longer imports the legacy hand-written component stylesheet"
    - "Both loading spinners (AnomalyCard + LocationDisplay) use the shared animate-location-spin utility from plan 01"
  artifacts:
    - "src/app/App.tsx, src/app/LocationPanel.tsx, src/app/LocationDisplay.tsx, src/app/AnomalyCard.tsx with utility-class className strings"
  key_links:
    - "map div [&_.leaflet-container]:h-full/w-full -> Leaflet's runtime-generated .leaflet-container gets sized so the map renders (STYLE-03)"
    - "animate-location-spin (plan 01 @theme) -> the spinner spans in both loading branches"
  prohibitions:
    - text: "MUST NOT make any visual/aesthetic change; every branch must render visually equivalent to v1.0"
      status: flagged
    - text: "MUST NOT change any component's markup, props, conditional branches, or behavior — className strings only (plus removing the one legacy CSS import in App.tsx)"
      status: flagged
    - text: "MUST NOT rename or alter any design token; consume the plan 01 @theme utilities/tokens as-is"
      status: flagged
    - text: "MUST NOT leave App.tsx importing the legacy hand-written stylesheet"
      status: flagged
---

<objective>
Migrate the layout and hero surfaces to Tailwind utilities: the app shell + map region (App.tsx), the side panel (LocationPanel.tsx), the location display (LocationDisplay.tsx, all four render branches), and the anomaly card (AnomalyCard.tsx, all four render branches). Rewrite each `className` from BEM to the utilities generated by plan 01's `@theme`, remove App.tsx's legacy stylesheet import, and preserve the load-bearing map-container sizing.

Purpose: STYLE-01 (styling via Tailwind utilities in JSX) and STYLE-02 (App.tsx stops depending on the hand-written stylesheet) for these four components; the map-region sizing utility keeps the Leaflet map rendering under Preflight (STYLE-03). The physical `App.css` file stays until plan 04 (it is now fully dead once no element references its classes).

Output: four components rewritten to utility classes; App.tsx import of the legacy stylesheet removed.

Edge-probe note: STYLE-01..04 are mechanical requirements with no input-driven edge cases; the risk surface here is faithful visual translation + preserving the map sizing, captured in `must_haves`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-tailwind-foundation-migration/04-CONTEXT.md
@.planning/phases/04-tailwind-foundation-migration/04-PATTERNS.md
@.planning/phases/04-tailwind-foundation-migration/04-RESEARCH.md
@src/map/MapView.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: App shell + map region + panel to utilities; drop legacy CSS import</name>
  <files>src/app/App.tsx, src/app/LocationPanel.tsx</files>
  <read_first>
    - src/app/App.tsx (current: line 2 imports the legacy stylesheet; lines 74-75 hold the two className sites)
    - src/app/LocationPanel.tsx (lines 13-14 hold the two className sites)
    - src/map/MapView.tsx (confirms MapContainer sets NO explicit height/width — the map depends entirely on the .leaflet-container sizing rule)
    - .planning/phases/04-tailwind-foundation-migration/04-PATTERNS.md (§ App.tsx and § LocationPanel.tsx — the exact source classes, CSS rules, and the map-region/leaflet-container micro-decision)
  </read_first>
  <action>
    In App.tsx, translate the two className sites (source classes + CSS in PATTERNS.md § App.tsx):
    - The outer shell div → `flex flex-row w-screen h-screen overflow-hidden`.
    - The map container div → `flex-auto min-w-0 h-full bg-dominant [&_.leaflet-container]:h-full [&_.leaflet-container]:w-full`. The trailing arbitrary-variant pair REPLACES the old descendant rule that sized Leaflet's generated `.leaflet-container` to width/height 100%. This is load-bearing: MapView's MapContainer sets no height, so without it the map collapses to zero height. This is the chosen mechanism for the irreducible descendant selector (Claude's discretion per D-05 / PATTERNS.md) — keeping it a utility per D-01 rather than adding scoped CSS to the entry file (D-02/D-03).
    Then remove the App.tsx import line (currently line 2) that pulls in the legacy hand-written component stylesheet — App.tsx is now fully utility-based and is the only file importing it. Do NOT touch `main.tsx`'s `./index.css` import (that is the Tailwind entry and stays). Leave all markup, hooks, props, and JSX structure otherwise identical.

    In LocationPanel.tsx, translate the two className sites (PATTERNS.md § LocationPanel.tsx):
    - The panel `<aside>` → `flex-[0_0_760px] w-[760px] h-full bg-secondary flex flex-col py-lg px-md box-border overflow-y-auto` (preserves the 760px fixed width, secondary background, 24px/16px padding, and vertical scroll).
    - The inner content `<div>` → `flex flex-col gap-md`.
  </action>
  <verify>
    <automated>! grep -Eq "app-shell|map-region" src/app/App.tsx && grep -q "leaflet-container" src/app/App.tsx && ! grep -q "\.css'" src/app/App.tsx && ! grep -q "location-panel" src/app/LocationPanel.tsx && grep -q "className=" src/app/LocationPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eq "app-shell|map-region" src/app/App.tsx` returns nothing (old layout classes gone).
    - `grep -q "leaflet-container" src/app/App.tsx` matches (the map-sizing arbitrary-variant utility is present).
    - App.tsx contains no `.css'` import (the legacy stylesheet import is removed; the Tailwind entry is imported in main.tsx, not here).
    - `grep -q "location-panel" src/app/LocationPanel.tsx` returns nothing; the file still has `className=` utility strings.
    - App.tsx markup/props/branches are otherwise unchanged (only className strings + the removed import).
  </acceptance_criteria>
  <done>App shell, map region, and side panel render from utilities; the map-container sizing is preserved as a utility; App.tsx no longer imports the legacy stylesheet.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: LocationDisplay to utilities (all four branches, shared spinner)</name>
  <files>src/app/LocationDisplay.tsx</files>
  <read_first>
    - src/app/LocationDisplay.tsx (four render branches; className sites on lines 27/28/29/40/41/43/44/51/52/60/61)
    - .planning/phases/04-tailwind-foundation-migration/04-PATTERNS.md (§ LocationDisplay.tsx — exact source classes + CSS; the spinner is identical to AnomalyCard's)
  </read_first>
  <action>
    Translate every className site (source classes + CSS in PATTERNS.md § LocationDisplay.tsx), preserving each branch exactly:
    - Root container (empty / resolved / fallback branches) → `flex flex-col gap-sm`.
    - Root container (loading branch) → `flex flex-row items-center gap-sm` (the loading modifier switches to a row with centered items).
    - Heading element → `m-0 text-heading font-heading`.
    - Body element → `m-0 text-body font-body`.
    - Label element → `m-0 text-label font-label`.
    - Spinner `<span>` → `size-4 shrink-0 rounded-full border-2 border-accent border-t-transparent animate-location-spin` (16px circle, accent ring with a transparent top edge, using plan 01's shared `animate-location-spin` utility). Keep `aria-hidden="true"` and the `role="status"` on the loading container.
    Do not alter the branch logic, text content, or ARIA attributes — className strings only.
  </action>
  <verify>
    <automated>! grep -q "location-display" src/app/LocationDisplay.tsx && grep -q "animate-location-spin" src/app/LocationDisplay.tsx && grep -q "role=\"status\"" src/app/LocationDisplay.tsx && grep -q "className=" src/app/LocationDisplay.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "location-display" src/app/LocationDisplay.tsx` returns nothing (no BEM classes remain).
    - `grep -q "animate-location-spin" src/app/LocationDisplay.tsx` matches (spinner uses the shared utility).
    - `role="status"` and `aria-hidden` attributes are preserved.
    - All four branches still render their heading/body/label/spinner as before (visual equivalence).
  </acceptance_criteria>
  <done>LocationDisplay renders all four branches from utilities, with the spinner on the shared `animate-location-spin` utility.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: AnomalyCard to utilities (all four branches; delta + info edge cases)</name>
  <files>src/app/AnomalyCard.tsx</files>
  <read_first>
    - src/app/AnomalyCard.tsx (four render branches; 14 className sites on lines 33/34/46/47/48/57/58/66/67/68/74/81/82/83)
    - .planning/phases/04-tailwind-foundation-migration/04-PATTERNS.md (§ AnomalyCard.tsx — exact source classes + CSS, plus the delta calc() edge case)
    - .planning/phases/04-tailwind-foundation-migration/04-RESEARCH.md (Pattern 3 worked example — the canonical info-button translation)
  </read_first>
  <action>
    Translate every className site (source classes + CSS in PATTERNS.md § AnomalyCard.tsx), preserving all four branches:
    - Root (empty branch) → `flex flex-col gap-sm`.
    - Root (loading branch) → `flex flex-row items-center gap-sm`; keep `role="status"`.
    - Root (error branch) → `flex flex-col gap-sm`.
    - Root (resolved branch) → `flex flex-col gap-xs` (tighter gap).
    - Body paragraph → `m-0 text-body font-body`; in the ERROR branch the body also takes `text-destructive`.
    - Spinner `<span>` → `size-4 shrink-0 rounded-full border-2 border-accent border-t-transparent animate-location-spin` (same shared utility as LocationDisplay); keep `aria-hidden="true"`.
    - Temperature paragraph → `m-0 text-display font-display`.
    - Header row → `flex flex-row items-center justify-between gap-sm`.
    - Info button → `h-5 w-5 shrink-0 rounded-full border border-accent bg-transparent p-0 text-accent text-label font-heading leading-none cursor-help` (the RESEARCH.md worked example); keep its `type`, `aria-label`, and `title`.
    - Delta paragraph → `m-0 text-[calc(var(--text-display)*1.7)] font-bold leading-[1.1]`. The arbitrary value reproduces the old `calc(var(--font-size-display) * 1.7)` (now `--text-display`, i.e. 28px * 1.7); this is the chosen mechanism for the irreducible calc (PATTERNS.md edge case).
    - Verdict paragraph → `m-0 text-heading font-heading`.
    - Z-score chip → `mt-xs inline-block w-fit text-label font-label text-muted bg-secondary rounded-full py-[2px] px-sm` (muted pill on the secondary background, 2px/8px padding).
    Do not change branch logic, props, text, or ARIA — className strings only. After the rewrite, run the production build to confirm every arbitrary value (delta calc, the `[0_0_760px]`, etc.) compiles.
  </action>
  <verify>
    <automated>! grep -q "anomaly-card" src/app/AnomalyCard.tsx && grep -q "animate-location-spin" src/app/AnomalyCard.tsx && grep -Fq "text-[calc(var(--text-display)*1.7)]" src/app/AnomalyCard.tsx && grep -q "text-destructive" src/app/AnomalyCard.tsx && npx tsc -b && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "anomaly-card" src/app/AnomalyCard.tsx` returns nothing (no BEM classes remain).
    - The delta paragraph uses `text-[calc(var(--text-display)*1.7)]` and `text-destructive` appears in the error branch's body.
    - The spinner uses `animate-location-spin`; the info button preserves its `aria-label`/`title`.
    - `npx tsc -b && npx vite build` exits 0 (all arbitrary values across the plan compile under Tailwind).
    - All four branches render visually equivalent to v1.0.
  </acceptance_criteria>
  <done>AnomalyCard renders all four branches from utilities including the strengthened delta size and the info/z-score chrome; the full plan compiles.</done>
</task>

</tasks>

<threat_model>
Security enforcement: ASVS L1, block_on: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side className rewrites only — no new inputs, network calls, auth, or data handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering | client JSX className rewrite (App/LocationPanel/LocationDisplay/AnomalyCard) | low | accept | A styling migration introduces no new attack surface; markup/props/behavior are unchanged. Supply-chain threat T-04-SC is handled by plan 01's install checkpoint. |

No threats at or above `high` in this plan's scope.
</threat_model>

<verification>
- Per-task grep gates confirm no BEM classes remain and the required utilities (map sizing, spinner, delta calc, error color) are present.
- `npx tsc -b && npx vite build` (Task 3) confirms the four rewritten components compile with Tailwind scanning the new class strings.
- Definitive build+test+visual verification is plan 04.
</verification>

<success_criteria>
- App shell, map region, LocationPanel, LocationDisplay (4 branches), and AnomalyCard (4 branches) styled entirely via Tailwind utilities (STYLE-01).
- App.tsx no longer imports the legacy hand-written stylesheet (STYLE-02, for this file).
- The Leaflet map keeps its full-region sizing via a utility, so it renders under Preflight (STYLE-03).
- Both spinners share the `animate-location-spin` utility; the production build compiles.
</success_criteria>

<artifacts_this_phase_produces>
Phase-4 artifacts consumed/produced here (see plan 01 for the token/plugin artifacts):
- `src/app/App.tsx`, `src/app/LocationPanel.tsx`, `src/app/LocationDisplay.tsx`, `src/app/AnomalyCard.tsx` rewritten to utility classes.
- The map-region `[&_.leaflet-container]:h-full [&_.leaflet-container]:w-full` arbitrary-variant utility (replaces the old scoped descendant rule).
- No new symbols/exports — className strings only.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-tailwind-foundation-migration/04-02-SUMMARY.md` when done.
</output>
