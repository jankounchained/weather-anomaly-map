---
phase: 05-glass-atmospheric-redesign
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/app/App.tsx
  - src/app/LocationPanel.tsx
  - src/app/LocationDisplay.tsx
autonomous: true
requirements: [DESIGN-01, DESIGN-02, DESIGN-03, PERF-01, PERF-02]
must_haves:
  truths:
    - "App.tsx computes anomalyColorValue = anomalyColor(anomaly?.zScore ?? null) and isNight = !isDaytime(current.localHour ?? 12) and threads both down to LocationPanel (D-01, D-03)"
    - "localHour defaults to noon (12 -> daytime) before data resolves, preventing a night-wash flash on every pin drop (Pitfall 5)"
    - "LocationPanel root sets --anomaly-color via the single sanctioned inline-style bridge and applies .panel-backdrop only when hasSelection; before a pin it stays flat bg-secondary (D-05)"
    - "LocationPanel root is position:relative so the .is-night::before wash is scoped to the panel, never escaping to the map region or viewport (Pitfall 2)"
    - "the LocationDisplay content region renders as a translucent glass card (bg-glass-surface, border-glass-border, rounded-glass-lg, shadow-glass, backdrop-blur-lg) on the root of every branch (DESIGN-03, D-04)"
    - "real backdrop-blur exists ONLY inside the docked LocationPanel's glass cards; the map region div and MapView carry no glass, gradient, or blur — PERF-01 satisfied by construction (PERF-01, D-05)"
    - "motion is a one-time entrance transition via motion-safe: utilities on the panel backdrop; under prefers-reduced-motion no transition is emitted, and no rAF / setInterval / canvas animation loop exists anywhere (PERF-02, D-10)"
    - "LocationDisplay empty state ('No location selected' + 'Click anywhere on the map...') renders on the glass card with copy unchanged (E1 empty)"
    - "LocationDisplay loading state (reverse-geocode spinner, --color-accent ring) renders on the glass card, copy unchanged (E1 loading)"
    - "LocationDisplay resolved place-name heading (20px/600) renders on the glass card (E1 populated)"
    - "LocationDisplay coordinate fallback (no usable name) renders on the glass card — no new error surface introduced (E1 error)"
    - "a long place name wraps within the glass card rather than overflowing (E1 overflow)"
    - "Panel backdrop no-pin/loading stays the flat --color-secondary fallback rather than a misleading placeholder gradient (E4 empty)"
    - "Panel backdrop resolved shows the data-driven anomaly-hue gradient via the --anomaly-color bridge, interpolated through @property registration (E4 populated)"
    - "Panel backdrop night applies the --color-atmosphere-night-wash overlay when isDaytime(localHour) is false; the hero hue is unaffected (E4 night)"
    - statement: "an unusually long place name wraps to 2-3 lines and stays inside the glass card padding/radius rather than looking cramped"
      verification: backstop
    - statement: "with hasSelection true and data in flight, the anomalyColor(null) neutral gradient does not read as a false 'normal weather' claim, and the one-time entrance transition into the resolved color reads smoothly"
      verification: backstop
  artifacts:
    - src/app/App.tsx
    - src/app/LocationPanel.tsx
    - src/app/LocationDisplay.tsx
  key_links:
    - "App -> LocationPanel prop threading (anomalyColorValue, isNight) is the connection carrying the shared color/time signal into CSS"
    - "the inline --anomaly-color custom property on LocationPanel is what every downstream var(--anomaly-color) read (gradient stops here, inherited hero text in Plan 03) resolves against"
    - "the docked-panel-beside-map composition is the structural guarantee for PERF-01 — nothing with backdrop-blur ever layers over .leaflet-container"
  prohibitions:
    - "must NOT introduce any always-on animation / drift or a JS / canvas / rAF / setInterval animation loop (PERF-02)"
    - "must NOT add glass, gradient, or blur to the live map region or MapView (PERF-01)"
    - "must NOT introduce inline styles beyond the single --anomaly-color custom-property bridge on the LocationPanel root"
---

<!-- planner-discipline-allow: backdrop-blur -->

<objective>
Wire the atmospheric backdrop and the panel-level glass composition: compute the shared color/day-night signal in App.tsx, bridge it into CSS on the docked LocationPanel as the static anomaly-gradient container, and restyle LocationDisplay onto a translucent glass card. This is the DESIGN-02 backdrop + DESIGN-03 surface work, delivered inside the disciplined-glass performance policy (PERF-01/PERF-02).

Purpose: Establishes the "condition/anomaly-driven mood" (D-01) on the one surface where real backdrop-blur is safe by construction — the docked panel that sits beside, never over, the live map (D-05, PERF-01). The map region is left exactly as Phase 4 delivered it.
Output: App.tsx prop threading; LocationPanel as .panel-backdrop with the --anomaly-color inline bridge, conditional is-night wash, and motion-safe entrance transition; LocationDisplay glass cards across all branches. No feature/behavior change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-glass-atmospheric-redesign/05-UI-SPEC.md
@.planning/phases/05-glass-atmospheric-redesign/05-PATTERNS.md
@.planning/phases/05-glass-atmospheric-redesign/05-01-PLAN.md
</context>

<artifacts_produced>
New symbols/props this plan creates (source-grounding: exclude from drift verification — net-new here):
- `LocationPanelProps` extended with `anomalyColorValue: string` and `isNight: boolean` (src/app/LocationPanel.tsx)
- App-scope computed constants `anomalyColorValue`, `isNight` (src/app/App.tsx)
- Consumes (does not create): anomalyColor, isDaytime (Plan 01, src/anomaly/anomaly.ts); localHour field (Plan 01, src/weather); .panel-backdrop / --anomaly-color / glass tokens (Plan 01, src/index.css)
- No new files.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Compute the shared color/day-night signal in App.tsx and thread it to LocationPanel</name>
  <files>src/app/App.tsx</files>
  <read_first>
    - src/app/App.tsx (analog: the anomaly/trendDays computed-value pattern lines 40-65 and the LocationPanel/AnomalyCard prop-threading lines 83-99)
    - src/anomaly/anomaly.ts (the anomalyColor / isDaytime signatures added in Plan 01)
    - src/weather/useCurrentWeather.ts + src/weather/types.ts (the localHour field added in Plan 01)
    - 05-RESEARCH.md Pattern 3 (compute in App.tsx, pass primitives down) and Pitfall 5 (default localHour to noon while loading)
    - 05-PATTERNS.md "src/app/App.tsx" section
  </read_first>
  <action>
    Import anomalyColor and isDaytime from '../anomaly/anomaly' (extend the existing import or add alongside the computeAnomalyForToday import). In the component body, after the existing `anomaly` const, add two plain consts (no useMemo — React Compiler handles memoization, consistent with existing code): anomalyColorValue = anomalyColor(anomaly?.zScore ?? null) and isNight = !isDaytime(current.localHour ?? 12). The `?? 12` default resolves to daytime while localHour is null (idle/loading), avoiding a night-wash flash before data resolves (Pitfall 5). Thread both onto the existing <LocationPanel> JSX as new props: anomalyColorValue={anomalyColorValue} isNight={isNight}, appended to the current flat prop list (hasSelection/status/name/lat/lng) with no restructuring. Do NOT pass any color prop to <AnomalyCard> — the hero reads the inherited var(--anomaly-color) set on LocationPanel (Plan 03). Leave the map-region div and <MapView> exactly as they are — add no glass, gradient, or blur class to them (PERF-01: the map surface receives no atmospheric treatment).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (types line up with the extended LocationPanelProps from Task 2)
    - `npx vite build` exits 0
    - Source assertion: `grep -q "anomalyColor(anomaly" src/app/App.tsx` and `grep -q "isDaytime(current.localHour ?? 12)" src/app/App.tsx`
    - Source assertion: `grep -q "anomalyColorValue={anomalyColorValue}" src/app/App.tsx` and `grep -q "isNight={isNight}" src/app/App.tsx`
    - PERF-01 confinement: the map-region div carries no atmospheric class — `grep -c "backdrop-blur" src/app/App.tsx` returns 0 and `grep -c "panel-backdrop" src/app/App.tsx` returns 0
  </acceptance_criteria>
  <done>App.tsx computes the shared anomaly color + day/night booleans (noon default while loading) and threads them to LocationPanel only; the map region is untouched; types and build pass.</done>
</task>

<task type="auto">
  <name>Task 2: Make LocationPanel the static anomaly-gradient backdrop with the --anomaly-color bridge and motion-safe transition</name>
  <files>src/app/LocationPanel.tsx</files>
  <read_first>
    - src/app/LocationPanel.tsx (analog: the <aside> shell lines 11-20 and the LocationPanelProps interface lines 7-9)
    - 05-UI-SPEC.md "Layout & Composition (Glass System)" (where the backdrop lives, the no-pin/no-data flat fallback, the --anomaly-color bridge as the ONE sanctioned inline-style exception) and "Motion & Performance Contract"
    - 05-RESEARCH.md Pitfall 2 (root needs position:relative for the ::before wash) and Pitfall 5 (neutral waiting state)
    - 05-PATTERNS.md "src/app/LocationPanel.tsx" section
    - src/index.css .panel-backdrop / .is-night::before classes + @property --anomaly-color (created in Plan 01)
  </read_first>
  <action>
    Extend LocationPanelProps (which extends LocationDisplayProps, so hasSelection is already available) with anomalyColorValue: string and isNight: boolean, matching the existing interface-extension style. Destructure them in the component signature and pass the remaining LocationDisplayProps through to LocationDisplay via `...props` as today (anomalyColorValue/isNight are consumed here, not forwarded to LocationDisplay). On the <aside> root: when hasSelection is true, apply `relative panel-backdrop` plus the motion-safe entrance transition utilities `motion-safe:transition-colors motion-safe:duration-300 motion-safe:ease-out`, and conditionally append `is-night` when isNight is true; set the inline bridge style={{ '--anomaly-color': anomalyColorValue } as React.CSSProperties}. When hasSelection is false, keep the current flat `bg-secondary` background with no panel-backdrop, no is-night, and no inline var (the no-pin neutral fallback — do not render a misleading placeholder gradient). Preserve every existing layout class (flex-[0_0_760px], w-[760px], h-full, flex, flex-col, py-lg, px-md, box-border, overflow-y-auto) in both cases; `relative` is only additive. This inline custom-property assignment is the single sanctioned inline-style bridge for the phase — introduce no other inline styles. Add no requestAnimationFrame, timer, interval, or canvas — the transition is pure CSS triggered by React re-rendering with a new --anomaly-color value.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npx vitest run` (full suite) exits 0
    - Source assertion: `grep -q "anomalyColorValue: string" src/app/LocationPanel.tsx` and `grep -q "isNight: boolean" src/app/LocationPanel.tsx`
    - Source assertion: `grep -q "panel-backdrop" src/app/LocationPanel.tsx` and `grep -q "relative" src/app/LocationPanel.tsx` and `grep -q "is-night" src/app/LocationPanel.tsx`
    - Source assertion: `grep -q "'--anomaly-color'" src/app/LocationPanel.tsx` (the single inline bridge)
    - Motion: `grep -q "motion-safe:transition-colors" src/app/LocationPanel.tsx`; no JS loop — `grep -c "requestAnimationFrame\|setInterval" src/app/LocationPanel.tsx` returns 0
    - No-pin fallback preserved: `grep -q "bg-secondary" src/app/LocationPanel.tsx`
  </acceptance_criteria>
  <done>LocationPanel is a relative-positioned .panel-backdrop that bridges --anomaly-color inline, applies the night wash and motion-safe transition only when a pin exists, and falls back to flat bg-secondary before a pin — full suite and types pass.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle LocationDisplay onto the translucent glass card across all branches</name>
  <files>src/app/LocationDisplay.tsx</files>
  <read_first>
    - src/app/LocationDisplay.tsx (analog: the four branch roots — empty lines 25-35, loading 37-50, resolved-name 52-58, coordinate fallback 62-66)
    - 05-UI-SPEC.md "Layout & Composition (Glass System)" (three glass cards, applied to the root of every state branch; smaller cards get px-md py-md) and the "UI Considerations" E1 rows
    - 05-PATTERNS.md "Combined loading-gate / single-render-branch structure" shared pattern (preserve branch conditions/count)
    - src/index.css glass tokens (created in Plan 01)
  </read_first>
  <action>
    Apply the shared glass-card treatment to the ROOT element returned by each of LocationDisplay's four branches (empty, loading, resolved-name, coordinate fallback) so the glass never flickers/resets between states. Add to each root's className: `bg-glass-surface border border-glass-border rounded-glass-lg shadow-glass backdrop-blur-lg px-md py-md` (the two smaller cards use px-md py-md per the UI-SPEC; this is one of them). Preserve each branch's existing inner structure and classes exactly — the flex/gap layout, the role="status" and aria-hidden on the loading spinner, the --color-accent spinner ring, the text-heading/font-heading headings, the formatCoords fallback, and all copy strings (locked, unchanged). Do not add, remove, reorder, or merge branches; only the root container className changes. Long place names must continue to wrap within the card (no nowrap/truncate added).
  </action>
  <verify>
    <automated>npx vitest run && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` (full suite) exits 0 and `npx vite build` exits 0
    - Source assertion: glass card classes present on the branch roots — `grep -c "bg-glass-surface" src/app/LocationDisplay.tsx` returns >= 4 (one per branch root)
    - Source assertion: `grep -q "backdrop-blur-lg" src/app/LocationDisplay.tsx` and `grep -q "rounded-glass-lg" src/app/LocationDisplay.tsx`
    - Copy unchanged: `grep -q "No location selected" src/app/LocationDisplay.tsx` and `grep -q "Click anywhere on the map" src/app/LocationDisplay.tsx`
    - Branch count preserved (4 returns): the existing empty/loading/resolved/fallback structure is intact
    - <human-check>Backstop (E1 long-text): drop a pin on a location with a very long place name; confirm it wraps to 2-3 lines and stays inside the glass card's padding and rounded corners rather than looking cramped or clipped.</human-check>
  </acceptance_criteria>
  <done>Each LocationDisplay branch renders on the translucent glass card with locked copy and preserved branch structure; suite and build pass; long-name wrapping verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App render scope -> LocationPanel props | anomalyColorValue (computed #rrggbb) and isNight (boolean) crossed as typed props |
| computed color -> CSS custom property | anomalyColorValue assigned to the inline --anomaly-color custom property, read by .panel-backdrop CSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | --anomaly-color inline bridge on LocationPanel | low | mitigate | The bridged value is only ever anomalyColor()'s computed #rrggbb output (numeric-derived, clamped) — never a free-form or user-controlled string; no other inline style is introduced |
| T-05-04 | Denial of Service (perf) | backdrop-blur over the live map | low | mitigate | Structural guarantee: all backdrop-blur lives inside the docked LocationPanel beside the map; the map region receives no atmospheric class (PERF-01), so pan/zoom paint cost is unchanged |

No new inputs, network calls, persistence, auth, or dangerouslySetInnerHTML — no ASVS L1 "high" threat to block on.
</threat_model>

<flagged_assumptions>
Spec-less edge probe items that are NOT genuine data-shape boundaries for this plan (surfaced explicitly, not silently dropped):
- DESIGN-01 "unclassified" probe: the holistic "cohesive glassy uplift" is an aggregate visual property, not a discrete testable data boundary — satisfied by the combined backdrop + glass-card treatment, verified via the human walkthrough, not a unit boundary.
- DESIGN-03 "unclassified" probe: translucent-surface treatment is a styling property with no data-shape edge; covered by the glass-card truths above.
- PERF-02 "ordering" probe: the reduced-motion kill-switch has no ordering / stable-sort axis — not applicable.
</flagged_assumptions>

<verification>
- `npx vitest run` (full suite) and `npx vite build` pass.
- No inline style beyond the single --anomaly-color bridge; no rAF/interval/canvas anywhere in the touched files.
- Map region unchanged (no atmospheric class); PERF-01 confinement holds.
</verification>

<success_criteria>
The docked panel presents the anomaly-driven gradient backdrop with a night wash, LocationDisplay reads as translucent glass across every state, and the map stays untouched — all motion behind motion-safe:, no animation loop, real blur confined to the static backdrop (PERF-01/PERF-02), with the full suite and production build green.
</success_criteria>

<output>
Create `.planning/phases/05-glass-atmospheric-redesign/05-02-SUMMARY.md` when done.
</output>
