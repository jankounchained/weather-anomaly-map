---
phase: 05-glass-atmospheric-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/anomaly/anomaly.ts
  - src/anomaly/anomaly.test.ts
  - src/weather/client.ts
  - src/weather/client.test.ts
  - src/weather/types.ts
  - src/weather/useCurrentWeather.ts
autonomous: true
requirements: [DESIGN-02, DESIGN-03, DESIGN-05, DESIGN-06, PERF-02]
must_haves:
  truths:
    - "anomalyColor(zScore: number | null) is a pure exported function in src/anomaly/anomaly.ts returning a #rrggbb string via a two-segment RGB lerp through the normal anchor (D-02)"
    - "anomalyColor(-3)==='#1e3a8a', anomalyColor(0)==='#57534e', anomalyColor(null)==='#57534e', anomalyColor(3)==='#9a3412' (exact anchor hex, unit-tested) — the zero/near-normal boundary and null/empty input both resolve to the exact normal anchor (D-02)"
    - "isDaytime(localHour: number) returns true for the half-open range [6,20): 5->false, 6->true, 19->true, 20->false (D-03)"
    - "localHourFrom(currentTime) parses the location-local hour (0-23) from Open-Meteo current.time with zero new network calls (D-03)"
    - "UseCurrentWeatherResult carries localHour: number | null, populated identically to localDate across the resolved/idle/loading/error branches of useCurrentWeather"
    - "VERDICT_LABEL.typical reads 'Right on the 30-year average' so a zero / near-normal delta renders framed verdict copy, never a bare ambiguous '0' (D-06, D-07)"
    - "@property --anomaly-color is registered with syntax '<color>', inherits: true, initial-value #57534e so the entrance transition interpolates the color rather than snapping (D-10, PERF-02)"
    - ".panel-backdrop and .panel-backdrop.is-night::before CSS classes exist producing the anomaly-hue linear-gradient plus the night wash overlay (D-01, D-02, D-03)"
    - "--color-chart-historical token value is swapped to rgba(87, 83, 78, 0.22) (echoing the normal anchor); --color-chart-mean and --color-chart-actual token values are unchanged (D-08, D-09)"
    - "glass token family exists in @theme: --color-glass-surface, --color-glass-border, --radius-glass-lg, --radius-glass-sm, --shadow-glass, --color-atmosphere-night-wash (D-03, D-04)"
  artifacts:
    - src/anomaly/anomaly.ts
    - src/anomaly/anomaly.test.ts
    - src/weather/client.ts
    - src/weather/types.ts
    - src/weather/useCurrentWeather.ts
    - src/index.css
  key_links:
    - "anomalyColor and isDaytime are the single shared color/time source consumed by BOTH the backdrop gradient and the hero delta — never recomputed independently in two places"
    - "@property --anomaly-color registration is the load-bearing link that makes motion-safe:transition-colors actually interpolate the gradient/hero color instead of snapping"
    - "localHourFrom -> localHour field -> useCurrentWeather population is the chain that delivers the pin-local day/night signal with no new fetch"
  prohibitions:
    - "must NOT alter formatDelta's whole-number rounding to match the illustrative '0.0°C' example — only VERDICT_LABEL.typical changes (Pitfall 3)"
    - "must NOT substitute OKLCH / color-mix for the anchor interpolation — the RGB two-segment lerp is locked by exact-hex unit tests"
    - "must NOT change the --color-chart-mean or --color-chart-actual token values (D-08 / D-09 locked)"
    - "must NOT introduce any always-on animation or a JS/canvas/rAF/interval animation loop (PERF-02)"
---

<objective>
Lay the dependency-free foundation for the glass/atmospheric redesign: the pure color + day/night functions, the pin-local hour data signal, the zero-delta copy fix, and every new CSS token / registered custom property / backdrop class that the Wave-2 composition plans consume.

Purpose: These are the leaf primitives — one shared anomaly->color source (D-02), one day/night axis (D-03), the framed zero-delta copy (D-06/D-07), and the @theme tokens + @property + .panel-backdrop CSS (D-01..D-04, D-08). Building them first lets the two Wave-2 presentation plans run in parallel against a stable contract.
Output: New exports anomalyColor / isDaytime (src/anomaly/anomaly.ts), localHourFrom + localHour field (src/weather), changed VERDICT_LABEL.typical, and new CSS tokens / @property / gradient classes (src/index.css). No behavior change, no new fetch, no new dependency.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-glass-atmospheric-redesign/05-UI-SPEC.md
@.planning/phases/05-glass-atmospheric-redesign/05-RESEARCH.md
@.planning/phases/05-glass-atmospheric-redesign/05-PATTERNS.md
</context>

<artifacts_produced>
New symbols/tokens this plan creates (source-grounding: exclude these from drift verification — they are net-new here):

- Functions: `anomalyColor(zScore: number | null): string`, `isDaytime(localHour: number): boolean` (src/anomaly/anomaly.ts); `localHourFrom(currentTime: string): number` (src/weather/client.ts)
- Module-private helpers/constants (src/anomaly/anomaly.ts): `ANOMALY_COLD` `{r:30,g:58,b:138}`, `ANOMALY_NORMAL` `{r:87,g:83,b:78}`, `ANOMALY_HOT` `{r:154,g:52,b:18}`, `lerpChannel`, `toHex`
- New type field: `localHour: number | null` on `UseCurrentWeatherResult` (src/weather/types.ts) and on the internal `ResolvedWeather` (src/weather/useCurrentWeather.ts)
- New @theme tokens (src/index.css): `--color-anomaly-cold` `#1e3a8a`, `--color-anomaly-normal` `#57534e`, `--color-anomaly-hot` `#9a3412`, `--color-atmosphere-night-wash` `rgba(10,8,20,0.4)`, `--color-glass-surface` `rgba(255,255,255,0.62)`, `--color-glass-border` `rgba(255,255,255,0.45)`, `--radius-glass-lg` `16px`, `--radius-glass-sm` `8px`, `--shadow-glass` `0 8px 30px rgba(15,8,30,0.14)`
- Changed token VALUE: `--color-chart-historical` -> `rgba(87, 83, 78, 0.22)`
- New registered custom property: `@property --anomaly-color`
- New CSS classes: `.panel-backdrop`, `.panel-backdrop.is-night::before`
- Changed copy: `VERDICT_LABEL.typical` -> `"Right on the 30-year average"`
- No new files.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add anomalyColor + isDaytime pure functions and the zero-delta verdict copy fix</name>
  <files>src/anomaly/anomaly.ts, src/anomaly/anomaly.test.ts</files>
  <read_first>
    - src/anomaly/anomaly.ts (analog: classifyVerdict lines 46-55, formatDelta 62-69, VERDICT_LABEL 36-44, null-fallback `zScore ?? 0` line 172)
    - src/anomaly/anomaly.test.ts (analog: existing describe blocks + import block lines 1-14)
    - 05-UI-SPEC.md "New tokens — Anomaly Color System" (the exact anomalyColor contract + anchor hex + isDaytime `[6,20)`) and "Copywriting Contract" (the VERDICT_LABEL.typical change)
    - 05-RESEARCH.md Pattern 1 (verbatim RGB-lerp algorithm) and Pitfall 3 (do NOT touch formatDelta rounding)
    - 05-PATTERNS.md "src/anomaly/anomaly.ts" section (private-helper placement, doc-comment style)
  </read_first>
  <behavior>
    - anomalyColor(-3) === '#1e3a8a' (cold anchor)
    - anomalyColor(0) === '#57534e' (normal anchor)
    - anomalyColor(null) === '#57534e' (null treated as z=0, same `?? 0` precedent)
    - anomalyColor(3) === '#9a3412' (hot anchor)
    - anomalyColor beyond range clamps: anomalyColor(-5) === anomalyColor(-3), anomalyColor(5) === anomalyColor(3)
    - anomalyColor(-1.5) and anomalyColor(1.5) fall strictly between the anchors and equal the per-channel two-segment lerp value (spot-check the math)
    - isDaytime(5) === false, isDaytime(6) === true, isDaytime(19) === true, isDaytime(20) === false
    - verdictLabel('typical') === 'Right on the 30-year average'
  </behavior>
  <action>
    Add two exported pure functions to src/anomaly/anomaly.ts, colocated with classifyVerdict/formatDelta, each with a doc comment citing D-02 / D-03 in the existing style. Place three module-private (non-exported) RGB anchor constants ANOMALY_COLD={r:30,g:58,b:138}, ANOMALY_NORMAL={r:87,g:83,b:78}, ANOMALY_HOT={r:154,g:52,b:18} and private helpers lerpChannel(c1,c2,t)=Math.round(c1+(c2-c1)*t) and toHex({r,g,b}) (per-channel clamp to [0,255], toString(16).padStart(2,'0')) just above them — mirroring how windowBounds feeds computeAnomalyForToday. anomalyColor(zScore): treat null as 0 via the existing `zScore ?? 0` idiom, clamp z to [-3,3], then two-segment piecewise-linear RGB interpolation — for z<=0 use t=(z+3)/3 and lerp COLD->NORMAL, for z>0 use t=z/3 and lerp NORMAL->HOT — returning "#rrggbb". Do NOT collapse to a single COLD->HOT lerp (muddies the midpoint and misses the exact z=0 anchor) and do NOT switch color spaces to OKLCH (would break the exact-hex tests). isDaytime(localHour): return localHour >= 6 && localHour < 20. Separately, change only the VERDICT_LABEL.typical value from 'Typical for today' to 'Right on the 30-year average' (D-06, D-07); leave every other key and leave formatDelta's whole-number rounding untouched (Pitfall 3 — the rendered pair is big "0" plus this framed copy). In anomaly.test.ts add a describe('anomalyColor', ...) block asserting the four exact anchor equalities plus clamp and midpoint spot-checks, a describe('isDaytime', ...) block for the [6,20) boundary, and extend the existing verdictLabel expectation for the new typical copy; add anomalyColor and isDaytime to the existing top-of-file import from './anomaly' (do not add a second import statement).
  </action>
  <verify>
    <automated>npx vitest run src/anomaly/anomaly.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/anomaly/anomaly.test.ts` exits 0 with the new anomalyColor / isDaytime describe blocks present
    - Source assertion: `grep -q "export function anomalyColor" src/anomaly/anomaly.ts` and `grep -q "export function isDaytime" src/anomaly/anomaly.ts`
    - Source assertion: `grep -q "Right on the 30-year average" src/anomaly/anomaly.ts`
    - Behavior: anomalyColor(0) and anomalyColor(null) both equal '#57534e'; anomalyColor(-3) equals '#1e3a8a'; anomalyColor(3) equals '#9a3412'
    - Regression guard: formatDelta still returns '0' for a zero delta (`grep -q "return '0'" src/anomaly/anomaly.ts`)
  </acceptance_criteria>
  <done>anomalyColor + isDaytime exist as pure, unit-tested functions with exact anchor hex; VERDICT_LABEL.typical is the framed copy; formatDelta rounding is untouched; the anomaly test file passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the pin-local hour data signal (localHourFrom + localHour field + hook population)</name>
  <files>src/weather/client.ts, src/weather/client.test.ts, src/weather/types.ts, src/weather/useCurrentWeather.ts</files>
  <read_first>
    - src/weather/client.ts (analog: localDateFrom lines 76-81)
    - src/weather/client.test.ts (analog: describe('localDateFrom') line 157, import line 9)
    - src/weather/types.ts (analog: UseCurrentWeatherResult localDate field line 45)
    - src/weather/useCurrentWeather.ts (analog: localDate population in resolved line 52, and the idle/loading/error `localDate: null` sites; ResolvedWeather interface lines 14-21)
    - 05-UI-SPEC.md "Required data-signal addition" and 05-RESEARCH.md Pattern 2 (verbatim localHourFrom implementation) and Pitfall 5 (null-hour default handling is done downstream in App, not here)
  </read_first>
  <behavior>
    - localHourFrom('2026-07-16T14:30') === 14
    - localHourFrom('2026-07-16T00:00') === 0
    - localHourFrom('2026-07-16T09:05') === 9
    - localHourFrom('2026-07-16') === 0 (missing time part falls back to '00:00')
  </behavior>
  <action>
    In src/weather/client.ts add an exported sibling to localDateFrom named localHourFrom(currentTime: string): number that splits Open-Meteo's "YYYY-MM-DDTHH:mm" current.time on 'T', takes the time portion (default '00:00' when absent), slices the first two chars, and returns Number(...) — same doc-comment convention citing D-03. In src/weather/types.ts add localHour: number | null to UseCurrentWeatherResult as a sibling immediately adjacent to localDate (same null-until-resolved convention). In src/weather/useCurrentWeather.ts: add localHour to the internal ResolvedWeather interface; import localHourFrom by extending the existing `import { getCurrentWeather, localDateFrom } from './client'` line (no second import); populate localHour: localHourFrom(response.current.time) in the resolved setResolved branch next to the localDate line; and set localHour: null at every place localDate: null currently appears (the catch branch, the idle early-return, and the loading early-return). In src/weather/client.test.ts add a describe('localHourFrom', ...) block covering the behaviors above, extending the existing import from './client'.
  </action>
  <verify>
    <automated>npx vitest run src/weather/client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/weather/client.test.ts` exits 0 with the new localHourFrom describe block
    - Source assertion: `grep -q "export function localHourFrom" src/weather/client.ts`
    - Source assertion: `grep -q "localHour" src/weather/types.ts` and localHour set in all four branches (`grep -c "localHour" src/weather/useCurrentWeather.ts` returns >= 4)
    - Behavior: localHourFrom('2026-07-16T14:30') === 14 and localHourFrom('2026-07-16') === 0
    - No new network call introduced (localHourFrom parses the string already present in the existing forecast response)
  </acceptance_criteria>
  <done>localHourFrom parses the pin-local hour, UseCurrentWeatherResult exposes localHour populated across every lifecycle branch, and the weather client test file passes — with zero new fetches.</done>
</task>

<task type="auto">
  <name>Task 3: Add anomaly/glass/backdrop CSS tokens, the @property registration, the panel-backdrop classes, and the chart re-theme token value</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (analog: existing @theme block lines 3-54, the keyframe precedent lines 56-60, the trailing :root/body section lines 62-80, and the current --color-chart-historical value line 14)
    - 05-UI-SPEC.md "New tokens — Anomaly Color System" table, "Layout & Composition (Glass System)" token list + the @property/.panel-backdrop CSS example, and the "Recharts Re-theme" token table (historical -> rgba(87,83,78,0.22); mean/actual unchanged)
    - 05-RESEARCH.md Pattern 4 (@property rule verbatim), 05-PATTERNS.md "src/index.css" section (token placement + cited-decision comment convention)
  </read_first>
  <action>
    In src/index.css, inside the existing @theme block, add the new tokens each with a comment citing its UI-SPEC section / D-number in the established style: --color-anomaly-cold: #1e3a8a (D-02, z<=-3), --color-anomaly-normal: #57534e (D-02, z=0), --color-anomaly-hot: #9a3412 (D-02, z>=+3), --color-atmosphere-night-wash: rgba(10, 8, 20, 0.4) (D-03), --color-glass-surface: rgba(255, 255, 255, 0.62) (D-03/D-04), --color-glass-border: rgba(255, 255, 255, 0.45) (D-03/D-04), --radius-glass-lg: 16px (D-03), --radius-glass-sm: 8px (D-03), --shadow-glass: 0 8px 30px rgba(15, 8, 30, 0.14) (D-04). Change the existing --color-chart-historical value from its current translucent-accent blue to rgba(87, 83, 78, 0.22) (D-08 — echoes the normal anchor); leave --color-chart-mean (var(--color-accent)) and --color-chart-actual (#ea580c) exactly as they are (D-08/D-09 locked). After the @theme block, as a sibling top-level at-rule next to the existing @keyframes, register the custom property: @property --anomaly-color with syntax '<color>', inherits: true, initial-value: #57534e (PERF-02/D-10 — makes the color interpolable). In the trailing plain-CSS section (below the keyframe, alongside the :root/body rules), add the .panel-backdrop class — position: relative plus the linear-gradient(155deg, ...) using color-mix(in srgb, var(--anomaly-color) 80%, white 20%) 0%, var(--anomaly-color) 55%, color-mix(in srgb, var(--anomaly-color) 75%, black 25%) 100% — and the .panel-backdrop.is-night::before rule (content:''; position:absolute; inset:0; background: var(--color-atmosphere-night-wash); pointer-events:none). Replicate the UI-SPEC CSS exactly; do not re-derive the gradient stops.
  </action>
  <verify>
    <automated>npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vite build` exits 0 (Tailwind v4 compiles the new @theme tokens and CSS)
    - Source assertion: `grep -q "@property --anomaly-color" src/index.css` and `grep -q "initial-value: #57534e" src/index.css`
    - Source assertion: `grep -q ".panel-backdrop" src/index.css` and `grep -q "is-night::before" src/index.css`
    - Source assertion: new glass tokens present — `grep -q "\-\-color-glass-surface" src/index.css`, `grep -q "\-\-shadow-glass" src/index.css`, `grep -q "\-\-radius-glass-lg" src/index.css`
    - Re-theme applied: `grep -q "rgba(87, 83, 78, 0.22)" src/index.css` (new historical value present)
    - Locked-token guard: `grep -q "\-\-color-chart-actual: #ea580c" src/index.css` (actual marker unchanged, D-09)
  </acceptance_criteria>
  <done>All new anomaly/glass/backdrop tokens exist with cited comments, @property --anomaly-color is registered, .panel-backdrop / .is-night::before classes are present, the chart historical token is re-valued, mean/actual are untouched, and vite build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Open-Meteo API -> client parse | current.time string parsed by localHourFrom; already defensively parsed upstream in client.ts (V5 guards unchanged) |
| numeric anomaly -> CSS color string | anomalyColor turns a clamped numeric zScore into a well-formed #rrggbb string later composed into CSS custom properties / gradients |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | anomalyColor output composed into CSS | low | mitigate | Output is fully computed from a numeric zScore clamped to [-3,3] and emitted via toHex/clamp as a fixed #rrggbb string — no free-form or user-controlled string ever reaches a style/CSS context |
| T-05-02 | Tampering | localHourFrom(current.time) | low | accept | Parses an already-validated API string to a Number; a malformed value yields NaN handled as a default downstream (Plan 02), never interpolated into markup |

This phase introduces no new inputs, network calls, persistence, auth, or dangerouslySetInnerHTML — no ASVS L1 "high" threat exists to block on.
</threat_model>

<flagged_assumptions>
Spec-less edge probe items that are NOT genuine boundaries for this plan (surfaced explicitly, not silently dropped):
- DESIGN-02 "ordering" probe: a scalar z-score -> color mapping has no ordering / stable-sort axis. Not applicable; anomalyColor is a deterministic pure function of a single number.
</flagged_assumptions>

<verification>
- `npx vitest run` (full suite) passes — new anomaly + client tests included.
- `npx vite build` passes with the new tokens / @property / classes.
- No new dependency added to package.json; no new network call.
</verification>

<success_criteria>
anomalyColor / isDaytime / localHourFrom exist as pure, unit-tested functions; localHour flows through the weather hook; VERDICT_LABEL.typical is the framed zero-delta copy; and src/index.css carries every new token, the @property registration, and the .panel-backdrop classes that Wave 2 depends on — full suite and production build green, no behavior change.
</success_criteria>

<output>
Create `.planning/phases/05-glass-atmospheric-redesign/05-01-SUMMARY.md` when done.
</output>
