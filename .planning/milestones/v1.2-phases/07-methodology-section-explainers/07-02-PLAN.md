---
phase: 07-methodology-section-explainers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/MethodologyPanel.tsx
  - src/app/MethodologyPanel.test.tsx
  - src/app/App.tsx
autonomous: true
requirements:
  - EXPLAIN-03
user_setup: []

must_haves:
  truths:
    - "ROADMAP success criterion 1: the user sees a methodology section collapsed by default (native `<details>` without `open`) and can expand it to read what the tool is for ('What This Shows') and how the anomaly is computed ('How It's Computed': 30-year baseline, day-of-year window, mean, delta, z-score, percentile) (PD-10/PD-12)."
    - "ROADMAP success criterion 2: the user can expand and collapse the section with both mouse and keyboard (native `<summary>` handles Enter/Space and click with an accent `:focus-visible` ring), and the disclosure is single-level flat - no nested `<details>`, no third heading level (PD-10)."
    - "The Methodology panel is always visible in every state (idle/empty/loading/error/resolved) - it mounts unconditionally as a LocationPanel child and is NOT gated on hasSelection or anomaly resolution, so a first-time visitor can expand it before dropping a pin (PD-11)."
    - "The panel composes the Phase 6 primitives: a single `PanelShell` glass card wrapping one `<details className=\"group\">`, with the `<summary>` carrying PanelHeadline's EXACT class string (not the PanelHeadline component nested inside, which would emit an invalid `<p>` in `<summary>`) plus the interaction classes, per PD-09/PD-10 and UI-SPEC Component Inventory."
    - "The disclosure chevron (`▸` glyph, aria-hidden) rotates via `group-open:rotate-90`, which requires `className=\"group\"` on the `<details>` element itself; the rotation is gated `motion-safe:transition-transform motion-safe:duration-200` so reduced-motion users get an instant, non-animated flip (PD-10, PERF-02). There is no expand/collapse height animation to gate (UI-SPEC)."
    - "The trust/limitation notes are all present in the body: Open-Meteo data-source credit, the ERA5-reanalysis/modeled-data caveat tying the whole-degree delta to formatDelta's rounding, and the sparse-data note that anomaly/percentile may be unavailable in remote spots (PD-13)."
    - "Edge EXPLAIN-03/empty: the methodology panel renders the same compiled-in static copy in every state; it has no data source and no zero-items condition (PD-11)."
    - "UI-consideration E1/overflow: the panel sits inside LocationPanel's `overflow-y-auto` scrollable aside; expanding adds page-flow height, never clips and never needs its own internal scroll region."
    - "UI-consideration E1/long-text: all methodology copy is static/authored (no dynamic per-location text, REQUIREMENTS.md Out of Scope) and wraps normally inside the 760px column - no truncation risk."
  artifacts:
    - "src/app/MethodologyPanel.tsx exists as a new stateless component composing PanelShell + native `<details>`/`<summary>` with the locked copy"
    - "src/app/MethodologyPanel.test.tsx exists asserting collapsed-by-default, click-to-reveal, and prop-less always-visible rendering"
    - "src/app/App.tsx imports and mounts `<MethodologyPanel />` as the final LocationPanel child, after `<TrendRow />`"
  key_links:
    - "`<MethodologyPanel />` mounts inside LocationPanel's `{children}` slot (LocationPanel.tsx line 39), which renders in both the hasSelection and empty-state branches - so PD-11 always-visible needs no new gating logic in LocationPanel"
    - "`className=\"group\"` on `<details>` is the activation link for `group-open:rotate-90` on the chevron span - without it the chevron silently never rotates (RESEARCH Pitfall 3)"
  prohibitions:
    - statement: "MUST NOT describe the 30-year baseline as measured single-station data; the methodology copy MUST state it is Open-Meteo ERA5 modeled/reanalysis climate data, which is why the delta is shown in whole degrees - overstating provenance/precision would mislead about the anomaly's confidence (PD-13 honesty caveat)."
---

<objective>
Add the always-visible Methodology disclosure (EXPLAIN-03): a new stateless `MethodologyPanel` composed from the Phase 6 `PanelShell` primitive wrapping a native `<details>`/`<summary>`, collapsed by default, single-level flat, mounted as the last child of `LocationPanel` after `TrendRow`. It carries locked static copy — "What This Shows", "How It's Computed", and the three trust/limitation notes — so a first-time visitor can understand and trust the anomaly before (or after) dropping a pin.

Purpose: give users who want to understand or trust the anomaly a plain-language "what/how/why", below the fold, without cluttering the at-a-glance view.
Output: new `src/app/MethodologyPanel.tsx`, new `src/app/MethodologyPanel.test.tsx`, one import + one mount line in `src/app/App.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07-methodology-section-explainers/07-CONTEXT.md
@.planning/phases/07-methodology-section-explainers/07-RESEARCH.md
@.planning/phases/07-methodology-section-explainers/07-PATTERNS.md
@.planning/phases/07-methodology-section-explainers/07-UI-SPEC.md
@.planning/phases/06-panel-restructure-hierarchy/06-UI-SPEC.md

@src/app/PanelShell.tsx
@src/app/PanelHeadline.tsx
@src/app/DeltaPanel.tsx
@src/app/DeltaPanel.test.tsx
@src/app/LocationPanel.tsx
@src/app/App.tsx
</context>

<artifacts_produced>
New symbols this plan creates (source-grounding pass MUST exclude these from drift verification — they do not exist yet):
- `MethodologyPanel` — new React function component, `src/app/MethodologyPanel.tsx` (new file)
- `SUMMARY_CLASSES` — module-private const in `src/app/MethodologyPanel.tsx`
- `src/app/MethodologyPanel.test.tsx` — new test file
- `import { MethodologyPanel } from './MethodologyPanel'` + `<MethodologyPanel />` mount in `src/app/App.tsx`
- No new CSS tokens and no new `src/index.css` rules — the chevron rotation uses Tailwind's built-in `group` / `group-open:` utilities only. No new dependencies.
</artifacts_produced>

<planner_assumptions>
Flagged edge-probe assumptions (spec-less fallback — surfaced, not silently dropped):
- EXPLAIN-03 `encoding` ("Whose definition of length/equality applies?") — **N/A (heuristic misfire).** MethodologyPanel renders static authored copy; there is no length/equality/collation logic anywhere in it.
- EXPLAIN-03 `idempotency` ("What happens if this runs twice on the same input?") — **N/A (heuristic misfire).** A pure static render with no side effects or mutation; re-rendering yields identical output.
- EXPLAIN-03 `concurrency` ("If interrupted or run in parallel, what is guaranteed?") — **N/A (heuristic misfire).** The native `<details>` widget holds its own DOM open/close state; there is no shared mutable state, no async work, and nothing to interrupt or parallelize.
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | The methodology copy is 100% static/authored, compiled into the bundle. There is no data source, no user-supplied or per-location content, and no network fetch — no trust boundary is crossed by this panel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Tampering | `MethodologyPanel` static copy | low | mitigate | All methodology copy is authored string literals rendered as ordinary JSX text nodes — never through a raw-HTML sink — preserving the codebase's T-01-02 / T-02-07 / T-06-05 invariant (PD-08). No dynamic, per-location, or user-supplied content exists in this surface (REQUIREMENTS.md Out of Scope: dynamic methodology copy), so there is no injection surface. |

**Package installs this phase:** none — no supply-chain (`T-{phase}-SC`) threat surface.
**ASVS L1, block-on high:** no high-severity open threats; the single low threat is mitigated by static-literal JSX-text-node rendering.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MethodologyPanel component (PanelShell + native details/summary, locked copy) and its tests</name>
  <files>src/app/MethodologyPanel.tsx, src/app/MethodologyPanel.test.tsx</files>
  <read_first>
    - src/app/PanelShell.tsx — the shell API (`children`, base glass classes on line 26); MethodologyPanel wraps its `<details>` in `<PanelShell>` verbatim.
    - src/app/PanelHeadline.tsx — the EXACT eyebrow class string on line 13 (`m-0 text-label leading-[1.5] font-semibold text-muted uppercase tracking-[0.05em]`); copy this onto `<summary>` directly (NOT the component — RESEARCH Pitfall 2).
    - src/app/DeltaPanel.tsx — the `motion-safe:` gating convention (line 82) and the PanelShell/PanelHeadline composition pattern (lines 76-78).
    - src/app/DeltaPanel.test.tsx — the RTL + Vitest file setup and `afterEach(cleanup)` convention (lines 1-8), and the `fireEvent.click` disclosure pattern (lines 112-128) to mirror.
    - .planning/phases/07-methodology-section-explainers/07-UI-SPEC.md — Component Inventory, "Disclosure visual/interaction spec", Typography (subsection headers are Label `font-semibold`, NOT uppercase), and the Copywriting Contract (the VERBATIM copy strings).
    - .planning/phases/07-methodology-section-explainers/07-RESEARCH.md — Pattern 3 (full file shape + `group` gotcha), Pitfall 2 (class string vs component), Pitfall 3 (`className="group"` on `<details>`), Pitfall 5 (jsdom toggles natively).
    - .planning/phases/07-methodology-section-explainers/07-PATTERNS.md — the MethodologyPanel and MethodologyPanel.test.tsx analog sections.
  </read_first>
  <behavior>
    - Test: `render(<MethodologyPanel />)` shows the `<summary>` headline "How This Works" (collapsed by default — the `<details>` has no `open` attribute).
    - Test: `fireEvent.click(getByText('How This Works'))` then `getByText('What This Shows')` and `getByText("How It's Computed")` both resolve (jsdom 29.1.1 toggles `<details>` natively; no polyfill).
    - Test: the component renders with NO props (PD-11 always-visible; it takes no hasSelection/status gate, unlike every other panel).
  </behavior>
  <action>
    Create src/app/MethodologyPanel.tsx as a stateless function component that holds NO React component state and needs none of React's state/ref hooks — the native `<details>` element owns all open/close and keyboard state, unlike InfoTooltip. Do not add any explanatory "no state needed" head comment (keep the file free of the state-hook identifier so the stateless gate stays clean):
    1. `import { PanelShell } from './PanelShell'`.
    2. Define a module-private `SUMMARY_CLASSES` const = PanelHeadline's exact eyebrow string (`text-label leading-[1.5] font-semibold text-muted uppercase tracking-[0.05em]`) concatenated with the interaction classes `flex items-center justify-between gap-sm cursor-pointer list-none [&::-webkit-details-marker]:hidden focus-visible:outline focus-visible:outline-2 focus-visible:outline-accent` (UI-SPEC Disclosure spec — suppresses the native marker so the chevron is not doubled, adds the accent focus ring for keyboard parity with InfoTooltip).
    3. Render `<PanelShell>` wrapping `<details className="group">` (the `group` class is MANDATORY on `<details>` — it is the activation ancestor for `group-open:rotate-90`; without it the chevron silently never rotates, RESEARCH Pitfall 3).
    4. `<summary className={SUMMARY_CLASSES}>` containing the text "How This Works" then a chevron `<span aria-hidden="true" className="text-muted motion-safe:transition-transform motion-safe:duration-200 group-open:rotate-90">▸</span>`.
    5. A body wrapper `<div className="flex flex-col gap-sm pt-sm">` holding, in order: subsection 1 (`<p className="m-0 text-label font-label">What This Shows</p>` + a Body `<p className="m-0 text-body font-body">` with the locked subsection-1 body copy), subsection 2 (`<p className="m-0 text-label font-label">How It's Computed</p>` + a Body `<p>` with the locked subsection-2 pipeline copy), and a trailing Body `<p className="m-0 text-body font-body">` with the locked trust/limitation notes (PD-13). Subsection headers are Label `font-label`, NOT uppercase/tracked (UI-SPEC Typography). All copy VERBATIM from the UI-SPEC Copywriting Contract, rendered as ordinary JSX text nodes only (PD-08) — no raw-HTML sink, no bolding via markup injection. Single flat level: no nested `<details>`, no third heading level (REQUIREMENTS Out of Scope).

    Create src/app/MethodologyPanel.test.tsx mirroring DeltaPanel.test.tsx's setup: import `describe, it, expect, afterEach` from `vitest` and `render, fireEvent, cleanup` from `@testing-library/react`, `import { MethodologyPanel } from './MethodologyPanel'`, `afterEach(cleanup)`. Add the three tests from the behavior block, using exact copy strings.
  </action>
  <verify>
    <automated>npx vitest run src/app/MethodologyPanel.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
    <human-check>ROADMAP research flag (PD-10): toggle the OS/browser reduced-motion setting (Chrome DevTools Rendering tab → "Emulate CSS media feature prefers-reduced-motion: reduce", or macOS System Settings → Accessibility → Display → Reduce Motion), run `npm run dev`, expand/collapse the "How This Works" panel, and confirm the `▸` chevron snaps instantly (no smooth rotation) under reduce-motion and rotates smoothly with motion enabled. jsdom cannot emulate this media feature (RESEARCH Pitfall 4) — this is a manual end-of-phase check, not automatable. Also confirm the chevron actually rotates at all (guards RESEARCH Pitfall 3: `className="group"` present on `<details>`).</human-check>
  </verify>
  <acceptance_criteria>
    - src/app/MethodologyPanel.tsx exists and exports `MethodologyPanel`.
    - `<details>` carries the `group` class: `grep -c 'details className="group"' src/app/MethodologyPanel.tsx` returns 1.
    - The chevron span uses `group-open:rotate-90` and `motion-safe:transition-transform`: `grep -c 'group-open:rotate-90' src/app/MethodologyPanel.tsx` returns 1 and `grep -c 'motion-safe:transition-transform' src/app/MethodologyPanel.tsx` returns 1.
    - The native marker is suppressed: `grep -c '\[&::-webkit-details-marker\]:hidden' src/app/MethodologyPanel.tsx` returns 1.
    - Component is prop-less and stateless: `grep -c 'useState' src/app/MethodologyPanel.tsx` returns 0.
    - No raw-HTML sink: `grep -c 'dangerouslySetInnerHTML' src/app/MethodologyPanel.tsx` returns 0 (all copy is JSX text nodes, PD-08).
    - Exact copy present, VERBATIM (asserted in the test and/or greppable in the source): `How This Works`, `What This Shows`, `How It's Computed`, the subsection-1 body "This tool shows how unusual today's temperature is at any location, compared to what's typical for that exact calendar day over the past 30 years.", the subsection-2 pipeline body beginning "We pull a 30-year historical baseline for this spot from Open-Meteo's archive", and the trust note beginning "Current and historical data comes from Open-Meteo, using ERA5 reanalysis".
    - The click-to-reveal test uses `fireEvent.click(getByText('How This Works'))` and asserts `getByText('What This Shows')` and `getByText("How It's Computed")`.
    - `npx vitest run src/app/MethodologyPanel.test.tsx` exits 0 and `npx tsc --noEmit -p tsconfig.app.json` reports no errors.
    - Human-check recorded (reduced-motion chevron behaves; chevron rotates on expand).
  </acceptance_criteria>
  <done>MethodologyPanel renders the collapsed native disclosure with the locked copy, the group-open chevron rotates (motion-safe-gated), the three RTL tests pass, and the reduced-motion human-check is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Mount MethodologyPanel as the final LocationPanel child in App.tsx</name>
  <files>src/app/App.tsx</files>
  <read_first>
    - src/app/App.tsx — the panel imports (lines 14-17, `./LocationPanel`, `./CurrentConditionsPanel`, `./DeltaPanel`, `./TrendRow`); the `<TrendRow ... />` mount (lines 127-133) that closes at line 133; the `</LocationPanel>` closing tag (line 134).
    - src/app/LocationPanel.tsx — the `{children}` slot (line 39) inside the `flex flex-col gap-md` container that renders in every state, confirming no new gating is needed for PD-11.
    - .planning/phases/07-methodology-section-explainers/07-PATTERNS.md — the App.tsx mount section (exact insertion point).
  </read_first>
  <action>
    In src/app/App.tsx:
    1. Add `import { MethodologyPanel } from './MethodologyPanel'` alongside the other panel imports (after the `./TrendRow` import, line 17).
    2. Insert `<MethodologyPanel />` immediately after the `<TrendRow ... />` element (which closes at line 133) and before `</LocationPanel>` (line 134), so it becomes the last child of LocationPanel. Pass NO props — MethodologyPanel is always visible with static copy (PD-11); it does not take hasSelection/status. Do NOT wrap it in any hasSelection/isAnomalyReady conditional — LocationPanel's `{children}` slot already renders unconditionally in every state.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { MethodologyPanel } from './MethodologyPanel'" src/app/App.tsx` returns 1.
    - `grep -c '<MethodologyPanel />' src/app/App.tsx` returns 1.
    - `<MethodologyPanel />` appears after the `<TrendRow` element and before `</LocationPanel>` (source order: TrendRow, then MethodologyPanel, then the LocationPanel close).
    - `<MethodologyPanel />` is not wrapped in any `hasSelection`/`isAnomalyReady` conditional (it is a bare sibling of `<TrendRow />` in the children block).
    - `npx tsc --noEmit -p tsconfig.app.json` reports no errors and `npx vitest run` exits 0 (full suite still green).
  </acceptance_criteria>
  <done>App.tsx imports and mounts `<MethodologyPanel />` as the final, unconditional LocationPanel child after TrendRow; type-check and full test suite pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — full suite green, including the new MethodologyPanel tests.
- `npx tsc --noEmit -p tsconfig.app.json` — no type errors.
- Manual (`npm run dev`): the "How This Works" panel appears at the bottom of the docked column in every state (before dropping a pin, while loading, on error, resolved); it is collapsed by default; clicking or Enter/Space toggles it; the chevron rotates (and snaps instantly under reduced-motion).
</verification>

<success_criteria>
- EXPLAIN-03 satisfied: a collapsed-by-default, keyboard-and-mouse-operable, single-level methodology section explains what the tool is for and how the anomaly is computed, plus the three trust/limitation notes.
- The panel is always visible (PD-11), composed from Phase 6 primitives (PD-09), native `<details>`/`<summary>` (PD-10), with static copy only (PD-12/PD-13) rendered as JSX text nodes (PD-08).
- No new dependency; no new CSS token or index.css rule.
</success_criteria>

<output>
Create `.planning/phases/07-methodology-section-explainers/07-02-SUMMARY.md` when done.
</output>
