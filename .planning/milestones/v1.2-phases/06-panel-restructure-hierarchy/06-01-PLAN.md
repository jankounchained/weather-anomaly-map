---
phase: 06-panel-restructure-hierarchy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/anomaly/anomaly.ts
  - src/anomaly/anomaly.test.ts
  - src/app/PanelShell.tsx
  - src/app/PanelShell.test.tsx
  - src/app/PanelHeadline.tsx
  - src/app/PanelHeadline.test.tsx
  - src/app/InfoTooltip.tsx
  - src/app/InfoTooltip.test.tsx
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, EXPLAIN-02]

must_haves:
  truths:
    - "isAnomalyReady(currentStatus, baselineStatus) returns true only when BOTH statuses equal 'resolved' — this is the single source of truth for the D-09 combined gate that keeps the current temperature from ever appearing before the anomaly resolves (PD-10, no partial reveal)."
    - "PanelHeadline renders the exact eyebrow class string from TrendRow.tsx line 38 (text-label leading-[1.5] font-semibold text-muted uppercase tracking-[0.05em]) with its children as the headline text — one shared primitive used by all four panels (LAYOUT-02)."
    - "PanelShell wraps children in the tokenized glass card class string (bg-glass-surface border border-glass-border rounded-glass-lg shadow-glass backdrop-blur-lg px-md py-md) and accepts an `as` prop selecting the semantic element (aside/section/div), defaulting to div (PD-06 identical card for every panel)."
    - "InfoTooltip trigger is a real <button type=\"button\"> carrying aria-expanded (toggles false→true on open) and aria-controls pointing at the popover element's id (EXPLAIN-02, WCAG 1.4.13)."
    - "InfoTooltip popover uses role=\"dialog\" with an aria-label naming the panel it explains (not role=\"tooltip\", because the body is multi-sentence prose), and wraps at max-w-[240px] so it never overflows the 760px docked column."
    - "InfoTooltip closes on Escape (returning focus to the trigger), on click-outside, and on blur when not hover-pinned; hover/focus-opened content stays open while the pointer moves onto the popover itself (EXPLAIN-02 boundary — dismissible/hoverable/persistent)."
    - "InfoTooltip popover body renders only static authored prose passed as a prop, as plain JSX text nodes — no dangerouslySetInnerHTML anywhere (T-01-02 / T-02-07 no-raw-HTML-sink invariant preserved)."
    - "InfoTooltip popover body is bounded static authored prose at max-w-[240px] with normal wrapping — no dynamic/location-specific text this phase, so no single unbroken long token and no horizontal overflow (UI-SPEC UI Considerations: InfoTooltip popover long-text)."
    - "InfoTooltip trigger keeps the existing 20px visual (h-5 w-5 rounded-full border border-accent text-accent, literal 'i' glyph) and carries a visible :focus-visible ring so it stays keyboard-discoverable at the sub-44px desktop size (UI-SPEC Spacing exception)."
  artifacts:
    - "src/anomaly/anomaly.ts — exported isAnomalyReady predicate"
    - "src/app/PanelShell.tsx — PanelShell component"
    - "src/app/PanelHeadline.tsx — PanelHeadline component"
    - "src/app/InfoTooltip.tsx — InfoTooltip component"
    - "src/anomaly/anomaly.test.ts, src/app/PanelShell.test.tsx, src/app/PanelHeadline.test.tsx, src/app/InfoTooltip.test.tsx — test coverage"
  key_links:
    - "isAnomalyReady is exported from src/anomaly/anomaly.ts alongside classifyVerdict/hasUsableSampleCount and imports WeatherStatus from ../weather/types — Wave-2/3 panels + App.tsx all import this ONE function (never re-derive the === 'resolved' comparison inline)."
    - "InfoTooltip imports PanelShell for its popover glass surface at reduced max-w-[240px] width — reuses the shared shell, does not restyle the glass tokens."
---

<objective>
Establish the four shared foundations every downstream panel (this phase and Phases 7-8) depends on: the `isAnomalyReady` combined-gate predicate (PD-10 mitigation) and the three reusable UI primitives `PanelShell`, `PanelHeadline`, and `InfoTooltip`. No existing panel is touched yet — this plan only creates net-new, independently-testable building blocks.

Purpose: Extract the duplicated glass-card class string, the eyebrow-headline style, the ad-hoc "i" button, and the duplicated combined-gate literal into single sources of truth, so the Wave-2/3 panel work composes them instead of re-deriving them (and the two split panels cannot drift apart on the gate).

Output: `isAnomalyReady` predicate + `PanelShell` + `PanelHeadline` + `InfoTooltip`, each with test coverage.
</objective>

## Artifacts this phase produces (this plan's share)

- `isAnomalyReady(currentStatus, baselineStatus): boolean` — new export in `src/anomaly/anomaly.ts`
- `PanelShell` — `src/app/PanelShell.tsx` (new)
- `PanelHeadline` — `src/app/PanelHeadline.tsx` (new)
- `InfoTooltip` — `src/app/InfoTooltip.tsx` (new)

Later plans in this phase additionally produce: `CurrentConditionsPanel`, `DeltaPanel` (new panels), the LocationDisplay + TrendRow migrations, the App.tsx two-up rewire, and the RETIREMENT (deletion) of `src/app/AnomalyCard.tsx`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-panel-restructure-hierarchy/06-UI-SPEC.md
@.planning/phases/06-panel-restructure-hierarchy/06-PATTERNS.md
@.planning/phases/06-panel-restructure-hierarchy/06-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the isAnomalyReady combined-gate predicate (PD-10)</name>
  <files>src/anomaly/anomaly.ts, src/anomaly/anomaly.test.ts</files>
  <read_first>
    - src/anomaly/anomaly.ts (target file — add the export alongside classifyVerdict line 51 / hasUsableSampleCount line 180, same pure-helper style)
    - src/anomaly/anomaly.test.ts (target test file — follow its `import { ... } from './anomaly'` + describe/it/expect vitest style)
    - src/weather/types.ts (source of the `WeatherStatus` type = 'idle' | 'loading' | 'resolved')
    - src/app/App.tsx lines 45-70 (the inline `current.status === 'resolved' && baseline.status === 'resolved'` literal this predicate replaces)
    - .planning/phases/06-panel-restructure-hierarchy/06-PATTERNS.md (section "src/anomaly/anomaly.ts (add shared combined-gate predicate — PD-10 mitigation)")
  </read_first>
  <behavior>
    - isAnomalyReady('resolved','resolved') === true
    - isAnomalyReady('loading','resolved') === false
    - isAnomalyReady('resolved','idle') === false
    - isAnomalyReady('idle','idle') === false
    - isAnomalyReady('resolved','loading') === false
  </behavior>
  <action>
    Add `import type { WeatherStatus } from '../weather/types'` to `src/anomaly/anomaly.ts`. Export a pure function `isAnomalyReady(currentStatus: WeatherStatus, baselineStatus: WeatherStatus): boolean` returning `currentStatus === 'resolved' && baselineStatus === 'resolved'` — this is the ONE canonical form of the D-09 combined gate per PD-10; both Wave-2/3 panels and App.tsx will call it instead of re-writing the comparison. Place it with the other pure gate helpers (near `hasUsableSampleCount`). Do NOT change any existing export. Add a `describe('isAnomalyReady')` block to anomaly.test.ts covering the five behavior cases above (import isAnomalyReady in the existing top-of-file import list).
  </action>
  <verify>
    <automated>npx vitest run src/anomaly/anomaly.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/anomaly/anomaly.ts` exports `isAnomalyReady` with signature `(currentStatus: WeatherStatus, baselineStatus: WeatherStatus): boolean`.
    - The function body is exactly the `=== 'resolved' && === 'resolved'` conjunction (no additional conditions).
    - anomaly.test.ts asserts all five behavior cases and passes.
    - No existing export in anomaly.ts is renamed or removed.
  </acceptance_criteria>
  <done>isAnomalyReady is exported, imported by its test, and all five cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build PanelShell and PanelHeadline primitives</name>
  <files>src/app/PanelShell.tsx, src/app/PanelShell.test.tsx, src/app/PanelHeadline.tsx, src/app/PanelHeadline.test.tsx</files>
  <read_first>
    - src/app/TrendRow.tsx (line 37 = canonical glass `<section>` class string; line 38 = the eyebrow `<p>` PanelHeadline reuses verbatim)
    - src/app/LocationDisplay.tsx (lines 27/40/54/63 = the tokenized `bg-glass-surface ... px-md py-md` glass string — the canonical form to normalize on)
    - src/app/TrendLegend.test.tsx (test style: `render(...)`, `getByText`, `container.querySelector`, `.toBeTruthy()` — NO jest-dom matchers available, NO user-event)
    - .planning/phases/06-panel-restructure-hierarchy/06-UI-SPEC.md (Component Inventory — PanelShell/PanelHeadline responsibilities; "no new tokens")
    - .planning/phases/06-panel-restructure-hierarchy/06-PATTERNS.md (PanelShell + PanelHeadline sections)
  </read_first>
  <behavior>
    - PanelShell renders children inside an element carrying the glass class string; default element is a div.
    - PanelShell with as="section" renders a <section>; with as="aside" renders an <aside>.
    - PanelHeadline renders its children text inside a <p> with the eyebrow class string.
  </behavior>
  <action>
    Create `PanelShell` in `src/app/PanelShell.tsx`: a presentational wrapper accepting `children: ReactNode`, an optional `as?: 'div' | 'section' | 'aside'` (default 'div'), and an optional `className?: string` appended after the base classes (for the InfoTooltip popover's `max-w-[240px]`). Base classes are exactly `flex flex-col gap-sm bg-glass-surface border border-glass-border rounded-glass-lg shadow-glass backdrop-blur-lg px-md py-md` — the tokenized LocationDisplay/TrendRow form, NOT AnomalyCard's non-tokenized white-alpha literal and NOT its `p-lg` padding (PD-06: identical shell for all panels; UI-SPEC: no new tokens). Render the element named by `as`. Create `PanelHeadline` in `src/app/PanelHeadline.tsx`: renders `children` inside `<p className="m-0 text-label leading-[1.5] font-semibold text-muted uppercase tracking-[0.05em]">` — the verbatim TrendRow line-38 eyebrow style, do not restyle. Add PanelShell.test.tsx (default div, `as` override to section, children present, glass classes present) and PanelHeadline.test.tsx (renders passed headline text, e.g. "Location", inside a <p> with the eyebrow classes). Use vanilla vitest + @testing-library/react assertions only (no jest-dom).
  </action>
  <verify>
    <automated>npx vitest run src/app/PanelShell.test.tsx src/app/PanelHeadline.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PanelShell base className contains `bg-glass-surface`, `border-glass-border`, `rounded-glass-lg`, `shadow-glass`, `backdrop-blur-lg`, and `px-md py-md` (assert via `container.firstChild` className).
    - PanelShell default renders a DIV; `as="section"` renders a SECTION (assert `element.tagName`).
    - PanelHeadline renders a <p> whose className contains `text-label`, `font-semibold`, `text-muted`, `uppercase`, and `tracking-[0.05em]`, with the passed text as its content.
    - Both test files pass.
  </acceptance_criteria>
  <done>PanelShell and PanelHeadline exist with the exact class strings and pass their tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the accessible InfoTooltip disclosure (EXPLAIN-02)</name>
  <files>src/app/InfoTooltip.tsx, src/app/InfoTooltip.test.tsx</files>
  <read_first>
    - src/app/AnomalyCard.tsx lines 78-85 (the trigger visual to preserve: h-5 w-5 rounded-full border border-accent text-accent, 'i' glyph — its `title` attribute is REPLACED by the popover, not kept)
    - src/app/PanelShell.tsx (created in Task 2 — the popover surface reuses it via className="max-w-[240px]")
    - .planning/phases/06-panel-restructure-hierarchy/06-UI-SPEC.md (Component Inventory → "InfoTooltip interaction contract" — the full WCAG 1.4.13 hoverable/dismissible/persistent spec is the source of truth; there is NO codebase analog for the interaction logic)
    - .planning/phases/06-panel-restructure-hierarchy/06-PATTERNS.md (InfoTooltip section + "No Analog Found" table)
    - src/app/TrendLegend.test.tsx (test style reference — use fireEvent for click/keyDown; NO user-event package available)
  </read_first>
  <behavior>
    - Trigger is <button type="button"> with aria-expanded="false" and aria-controls set to the popover id before opening.
    - fireEvent.click(trigger) sets aria-expanded="true" and renders a role="dialog" element whose id matches aria-controls.
    - The dialog carries an aria-label (naming what it explains) and its className includes max-w-[240px].
    - fireEvent.keyDown Escape closes the dialog (aria-expanded back to "false") and focus returns to the trigger.
    - The static body prose passed in is present in the open dialog as text.
  </behavior>
  <action>
    Create `InfoTooltip` in `src/app/InfoTooltip.tsx` using React `useState` + `useRef` + `useId` (no state manager, per CLAUDE.md stack rules). Props: `label: string` (the aria-label naming the panel it explains, e.g. "About the delta and z-score") and `children: ReactNode` (the static authored body prose). Trigger: a real `<button type="button">` reusing the AnomalyCard trigger visual classes (`h-5 w-5 shrink-0 rounded-full border border-accent bg-transparent p-0 text-accent text-label font-heading leading-none [font-family:initial]`) plus a visible focus ring utility (e.g. `focus-visible:outline focus-visible:outline-2 focus-visible:outline-accent`), rendering the literal `i`, with `aria-expanded={open}` and `aria-controls={popoverId}` (from useId). Popover: rendered when open, `role="dialog"`, `id={popoverId}`, `aria-label={label}`, using `<PanelShell className="max-w-[240px]">` for the glass surface, with `{children}` as plain JSX text nodes only (no raw-HTML sink). Implement the UI-SPEC interaction contract: opens on click/Enter/Space AND on mouseenter/focus (progressive enhancement); hover-opened popover stays open while the pointer is over the trigger OR the popover; closes on Escape (call trigger `.focus()` after closing), on outside click (document mousedown listener when open), and on blur when not hover-pinned. Add InfoTooltip.test.tsx covering the five behavior cases using fireEvent (click, keyDown {key:'Escape'}), asserting attributes via `getAttribute` and focus via `document.activeElement`.
  </action>
  <verify>
    <automated>npx vitest run src/app/InfoTooltip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Trigger is `<button type="button">` with `aria-expanded` and `aria-controls`; aria-controls equals the popover's id.
    - Opening yields an element with `role="dialog"`, a non-empty `aria-label`, and className containing `max-w-[240px]`.
    - Escape closes the dialog and returns focus to the trigger (`document.activeElement === trigger`).
    - Body prose is rendered as text nodes; grep confirms no `dangerouslySetInnerHTML` in InfoTooltip.tsx: `grep -c 'dangerouslySetInnerHTML' src/app/InfoTooltip.tsx` returns 0.
    - The trigger classlist contains `border-accent`, `text-accent`, and a `focus-visible:` ring utility.
    - InfoTooltip.test.tsx passes.
  </acceptance_criteria>
  <done>InfoTooltip satisfies the WCAG 1.4.13 contract (real button, aria-expanded/controls, role=dialog + aria-label, Escape-returns-focus, hover-pinnable, max-w-[240px]) and its test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored copy → InfoTooltip popover DOM | Static explainer prose is passed as `children` and rendered into a role="dialog" element. No dynamic/location-specific text this phase (REQUIREMENTS.md Out of Scope), so the input is developer-authored, not user/network-supplied — but the render path must still forbid raw-HTML injection so it stays safe when Phase 7 methodology copy reuses this primitive. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | InfoTooltip popover body / PanelShell / PanelHeadline children rendering | low | mitigate | Render all body/children as plain JSX text nodes only; never `dangerouslySetInnerHTML`. Preserves the standing T-01-02 / T-02-07 no-raw-HTML-sink invariant so a future dynamic-copy source cannot inject markup. Verified by grep gate in Task 3. |
| T-06-02 | Denial of Service | InfoTooltip document-level outside-click / Escape listeners | low | accept | Listeners are attached only while the popover is open and removed on close/unmount; a single lightweight disclosure poses no realistic DoS surface in a client-only SPA. Accepted (below the ASVS L1 high-block threshold). |

No new dependencies are installed this phase (only existing React + Tailwind), so no supply-chain (T-*-SC) checkpoint applies. No secrets, auth, network, or persistence surface is introduced.
</threat_model>

<verification>
- `npx vitest run src/anomaly/anomaly.test.ts src/app/PanelShell.test.tsx src/app/PanelHeadline.test.tsx src/app/InfoTooltip.test.tsx` all pass.
- `npm run build` succeeds (tsc -b type-checks the new exports and components).
- `grep -c 'dangerouslySetInnerHTML' src/app/InfoTooltip.tsx` returns 0.
</verification>

<success_criteria>
- isAnomalyReady exported and unit-tested (the single D-09 gate source, PD-10).
- PanelShell, PanelHeadline, InfoTooltip exist, styled from existing tokens only (no new tokens), each with passing tests.
- InfoTooltip meets the full UI-SPEC WCAG 1.4.13 interaction contract.
- No downstream file is modified yet (foundation-only plan).
</success_criteria>

<output>
Create `.planning/phases/06-panel-restructure-hierarchy/06-01-SUMMARY.md` when done.
</output>
