---
phase: 06-panel-restructure-hierarchy
plan: 03
type: execute
wave: 3
depends_on: ['06-01', '06-02']
files_modified:
  - src/app/CurrentConditionsPanel.tsx
  - src/app/CurrentConditionsPanel.test.tsx
  - src/app/DeltaPanel.tsx
  - src/app/DeltaPanel.test.tsx
  - src/app/App.tsx
  - src/app/AnomalyCard.tsx
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03, EXPLAIN-01, EXPLAIN-02]

must_haves:
  truths:
    - "The combined AnomalyCard hero is split into two distinct panels — CurrentConditionsPanel (today's temperature) and DeltaPanel (Δ + verdict + z-score) — each its own PanelShell card; AnomalyCard.tsx no longer exists (LAYOUT-01, PD-01, PD-09)."
    - "The Current Conditions and Delta panels sit in a 50/50 equal-width, equal-height (align-stretch) two-up flex row that always stays side-by-side and never reflows between empty/loading/error/resolved states; a fixed gap separates them so they never merge or collide (LAYOUT-02 adjacency, PD-02, PD-03, PD-04)."
    - "Panel order is fixed and stable: Location (top, full-width) → Current Conditions + Delta (two-up row) → Last 7 Days (below, full-width) (LAYOUT-02 ordering, PD-01)."
    - "The Current Conditions panel shows the 'Current Conditions' headline (PanelHeadline), the temperature at Display size (28px, Math.round(tempC) + units), and always-visible micro-copy exactly 'Today's measured temperature.' directly under the number (LAYOUT-02, EXPLAIN-01 adjacency/ordering)."
    - "The Delta panel shows the 'Delta' headline (PanelHeadline), the Δ-glyph number at calc(var(--text-display)*1.7) = 47.6px in var(--anomaly-color), and its internal order is exactly: headline → Δ number → micro-copy 'How today compares to the 30-year average for this date.' → verdict label → z-score chip (PD-07, LAYOUT-03, EXPLAIN-01 adjacency/ordering)."
    - "The user's eye lands on the Delta first: the Δ number (47.6px) is ~1.7× the Current Conditions temperature (28px Display), inside the required 1.5–2× dominance band, and is the only color-driven (var(--anomaly-color)) Δ-glyph-led element — no elevated shadow/tint/glow differentiates the Delta card's shell (LAYOUT-03, PD-06)."
    - "Both panels reveal an accessible InfoTooltip (mouse + keyboard): Current Conditions' tooltip body is exactly 'Based on modeled climate data for this area (~9–25km resolution), not a physical station exactly at this point.'; Delta's tooltip (aria-label naming delta + z-score) body is exactly 'Δ is today's temperature minus the 30-year historical average for this exact calendar day. The z-score below expresses the same gap in standard deviations, so it stays comparable across locations with different day-to-day variability.' (EXPLAIN-02)."
    - "Both panels self-gate through the shared isAnomalyReady(currentStatus, baselineStatus) predicate for their loading branch — neither re-derives the === 'resolved' comparison inline — so the temperature can never appear before the anomaly resolves (no partial reveal; PD-10, LAYOUT-01 boundary)."
    - "Current Conditions empty state shows exactly 'Drop a pin to see today's current temperature here.'; loading 'Calculating today's conditions…'; error 'Temperature unavailable for this location.' (UI-SPEC Copywriting Contract, UI Considerations empty/loading/error)."
    - "Delta empty state shows exactly 'Drop a pin to see how today compares to the 30-year average.'; loading 'Calculating today's anomaly…'; error 'Couldn't compute an anomaly here.'; z-score no-variance fallback 'z — (too little variance to compute)' (UI-SPEC Copywriting Contract, UI Considerations empty/loading/error)."
    - "Current Conditions content is bounded (28px temperature + short static micro-copy) with no dynamic long strings, so no overflow inside the 760px column (UI-SPEC UI Considerations: Current Conditions overflow/long-text)."
    - "Delta content is bounded (47.6px Δ glyph + z-score chip + static micro-copy, with the no-variance fallback defined) with no overflow at extreme z-scores in the 760px column (UI-SPEC UI Considerations: Delta overflow/long-text)."
    - "Content in both cards is top-aligned (headline → number → micro-copy), so the two numbers align under equal-height headlines and the quieter Current Conditions card reinforces Delta's dominance (PD-05)."
    - "All dynamic text (temperature, delta, verdict, z-score) renders as plain JSX text nodes only — no raw-HTML sink (T-01-02 / T-02-07 preserved through the split)."
  artifacts:
    - "src/app/CurrentConditionsPanel.tsx — new panel (temperature + micro-copy + InfoTooltip)"
    - "src/app/DeltaPanel.tsx — new panel (Δ glyph + micro-copy + verdict + z-score chip + InfoTooltip)"
    - "src/app/CurrentConditionsPanel.test.tsx, src/app/DeltaPanel.test.tsx — test coverage"
    - "src/app/App.tsx — modified: two-up row composition + shared gate predicate + TrendRow status props"
    - "src/app/AnomalyCard.tsx — RETIRED (deleted)"
  key_links:
    - "App.tsx imports CurrentConditionsPanel + DeltaPanel (replacing the AnomalyCard import), wraps them in the 50/50 equal-height row, and passes hasSelection + current.status + baseline.status to both panels AND to TrendRow (per plan 06-02's new TrendRow interface)."
    - "App.tsx's `anomaly`/`trendDays` computation switches its inline current/baseline gate literal to call isAnomalyReady, so the gate condition lives in exactly one place (PD-10)."
    - "var(--anomaly-color) still reaches the Delta glyph: it is bridged onto LocationPanel's backdrop (unchanged) and the DeltaPanel Δ number consumes it via style={{ color: 'var(--anomaly-color)' }} regardless of DOM nesting (Phase 5 carry-over)."
    - "Both panels import formatDelta + verdictLabel from ../anomaly/anomaly and isAnomalyReady for the gate; DeltaPanel imports VerdictTier type from ../anomaly/types."
---

<objective>
Split the combined `AnomalyCard` hero into two distinct, self-explanatory panels — `CurrentConditionsPanel` (today's temperature) and `DeltaPanel` (Δ + verdict + z-score) — compose them as a 50/50 equal-width, equal-height two-up row in `App.tsx`, thread the shared `isAnomalyReady` gate through both panels (and App.tsx's own computation and TrendRow), and RETIRE `AnomalyCard.tsx`. The Delta stays the unmistakable focal point (47.6px, anomaly color, Δ-glyph-led).

Purpose: This is the core of LAYOUT-01 (two separate panels), LAYOUT-03 (Delta dominance preserved), EXPLAIN-01 (per-panel micro-copy), and EXPLAIN-02 (per-panel InfoTooltip), plus the LAYOUT-02 headlines on the two new panels.

Output: Two new panels, the rewired App composition, and the deletion of AnomalyCard.
</objective>

## Artifacts this phase produces (this plan's share)

- `CurrentConditionsPanel` — `src/app/CurrentConditionsPanel.tsx` (new)
- `DeltaPanel` — `src/app/DeltaPanel.tsx` (new)
- `src/app/App.tsx` — modified: two-up row + shared gate + TrendRow status props
- `src/app/AnomalyCard.tsx` — RETIRED (deleted)

Depends on plan 06-01 (PanelShell/PanelHeadline/InfoTooltip/isAnomalyReady) and plan 06-02 (TrendRow's new `hasSelection`/`currentStatus`/`baselineStatus` interface that App.tsx now feeds).

## Flagged Assumptions (edge probes not applicable this phase — recorded, not dropped)

The spec-less edge probe raised three algorithmic-boundary edges that do not apply to this static-copy UI refactor. Recorded here explicitly per the no-silent-drop rule:

- **EXPLAIN-01 · boundary** — "min/max/threshold and one step either side." NOT APPLICABLE: the EXPLAIN-01 micro-copy ("Today's measured temperature." / "How today compares…") is static authored text with no numeric threshold or formatting introduced this phase (REQUIREMENTS.md Out of Scope: no dynamic/location-specific copy). The temperature `Math.round` and `formatDelta` are carried over unchanged from AnomalyCard, not new behavior.
- **EXPLAIN-01 · precision** — "precision loss / overflow / rounding / tie-breaking." NOT APPLICABLE: no new numeric formatting is introduced by the micro-copy; the existing `Math.round(tempC)` and `formatDelta` rounding rules are unchanged and already unit-tested in anomaly.test.ts.
- **EXPLAIN-02 · precision** — "precision loss / rounding" on the InfoTooltip. NOT APPLICABLE: the InfoTooltip body is static authored prose with no numeric content this phase; there is no dynamic/location-specific text to round or truncate (REQUIREMENTS.md Out of Scope).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-panel-restructure-hierarchy/06-UI-SPEC.md
@.planning/phases/06-panel-restructure-hierarchy/06-PATTERNS.md
@.planning/phases/06-panel-restructure-hierarchy/06-CONTEXT.md
@src/app/AnomalyCard.tsx
@src/app/App.tsx
@src/app/PanelShell.tsx
@src/app/PanelHeadline.tsx
@src/app/InfoTooltip.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build CurrentConditionsPanel (temperature + micro-copy + InfoTooltip)</name>
  <files>src/app/CurrentConditionsPanel.tsx, src/app/CurrentConditionsPanel.test.tsx</files>
  <read_first>
    - src/app/AnomalyCard.tsx (source of the split — lines 31-59 branching/gate, lines 72-86 the temperature number + "i" button; the temp side migrates HERE, the Δ/verdict/z-score do NOT)
    - src/app/PanelShell.tsx, src/app/PanelHeadline.tsx, src/app/InfoTooltip.tsx (primitives from 06-01)
    - src/anomaly/anomaly.ts (isAnomalyReady for the loading gate)
    - src/weather/types.ts (WeatherStatus type)
    - .planning/phases/06-panel-restructure-hierarchy/06-UI-SPEC.md (Copywriting Contract rows: "Current Conditions" headline, micro-copy, InfoTooltip body, empty/loading/error states — EXACT strings)
    - .planning/phases/06-panel-restructure-hierarchy/06-PATTERNS.md (CurrentConditionsPanel section)
  </read_first>
  <behavior>
    - Empty (!hasSelection): PanelShell + PanelHeadline "Current Conditions" + "Drop a pin to see today's current temperature here.".
    - Loading (!isAnomalyReady): PanelShell + headline + spinner (role="status") + "Calculating today's conditions…".
    - Error (resolved but tempC === null): PanelShell + headline + text-destructive "Temperature unavailable for this location.".
    - Populated: PanelShell + headline + row of [Display-size Math.round(tempC)+units , InfoTooltip] + micro-copy "Today's measured temperature." directly under the number.
    - InfoTooltip open shows exactly "Based on modeled climate data for this area (~9–25km resolution), not a physical station exactly at this point.".
  </behavior>
  <action>
    Create `CurrentConditionsPanel` in `src/app/CurrentConditionsPanel.tsx`. Props: `hasSelection: boolean`, `currentStatus: WeatherStatus`, `baselineStatus: WeatherStatus`, `tempC: number | null`, `units: string | null`. Use sequential early-return branches (empty → loading → error → populated) mirroring AnomalyCard's structure, each wrapped in `<PanelShell>` with `<PanelHeadline>Current Conditions</PanelHeadline>` first, content top-aligned (PD-05). The loading branch routes through the shared `isAnomalyReady(currentStatus, baselineStatus)` predicate (do not re-derive the resolved-status comparison inline) and reuses the shared spinner markup (size-4 rounded-full border-2 border-accent border-t-transparent animate-location-spin, aria-hidden, role="status" on the row). Populated branch: a `flex flex-row items-center justify-between gap-sm` row containing the temperature `<p className="m-0 text-display font-display">{Math.round(tempC)}{units}</p>` and the `<InfoTooltip label="About the current temperature reading">` carrying the exact modeled-data body string; below the row, a static `<p>` with exactly "Today's measured temperature." (EXPLAIN-01, adjacent to the number it explains). Use the EXACT UI-SPEC copy for empty/loading/error. All dynamic text as JSX text nodes only. Create CurrentConditionsPanel.test.tsx asserting each state's exact copy, the Display-size temperature render, the micro-copy presence, and that opening the InfoTooltip reveals the modeled-data body (fireEvent.click; vanilla assertions, no jest-dom/user-event).
  </action>
  <verify>
    <automated>npx vitest run src/app/CurrentConditionsPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PanelHeadline text is exactly "Current Conditions"; micro-copy is exactly "Today's measured temperature." and sits directly under the temperature.
    - Temperature renders at Display size (className contains `text-display`) as `Math.round(tempC)` + units.
    - Empty/loading/error render the exact UI-SPEC strings; loading routes through the shared predicate — `grep -c 'isAnomalyReady' src/app/CurrentConditionsPanel.tsx` returns ≥1 — and has role="status".
    - Opening the InfoTooltip reveals exactly "Based on modeled climate data for this area (~9–25km resolution), not a physical station exactly at this point.".
    - No `dangerouslySetInnerHTML` (grep returns 0).
    - CurrentConditionsPanel.test.tsx passes.
  </acceptance_criteria>
  <done>CurrentConditionsPanel renders all four states with exact copy, a Display-size temperature, adjacent micro-copy, and an accessible InfoTooltip; test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build DeltaPanel (Δ glyph dominance + verdict + z-score chip + InfoTooltip)</name>
  <files>src/app/DeltaPanel.tsx, src/app/DeltaPanel.test.tsx</files>
  <read_first>
    - src/app/AnomalyCard.tsx (source of the split — lines 87-100 the Δ glyph markup + verdict + z-score chip migrate HERE; reorder per PD-07)
    - src/app/PanelShell.tsx, src/app/PanelHeadline.tsx, src/app/InfoTooltip.tsx (primitives from 06-01)
    - src/anomaly/anomaly.ts (formatDelta, verdictLabel, isAnomalyReady)
    - src/anomaly/types.ts (VerdictTier type)
    - .planning/phases/06-panel-restructure-hierarchy/06-UI-SPEC.md (Copywriting Contract: "Delta" headline, delta micro-copy, delta InfoTooltip body covering delta AND z-score, empty/loading/error states, z-score no-variance fallback; Typography "Delta dominance rule" = calc(var(--text-display)*1.7))
    - .planning/phases/06-panel-restructure-hierarchy/06-PATTERNS.md (DeltaPanel section + "New internal order per PD-07")
  </read_first>
  <behavior>
    - Empty (!hasSelection): PanelShell + PanelHeadline "Delta" + "Drop a pin to see how today compares to the 30-year average.".
    - Loading (!isAnomalyReady): PanelShell + headline + spinner (role="status") + "Calculating today's anomaly…".
    - Error (resolved but anomaly === null): PanelShell + headline + text-destructive "Couldn't compute an anomaly here.".
    - Populated internal order: headline → Δ number (47.6px, var(--anomaly-color)) → micro-copy "How today compares to the 30-year average for this date." → verdict label → z-score chip.
    - z-score chip shows `z {zScore.toFixed(1)}` or, when zScore is null, exactly "z — (too little variance to compute)".
    - InfoTooltip (aria-label about delta + z-score) open shows the exact two-sentence delta/z-score body.
  </behavior>
  <action>
    Create `DeltaPanel` in `src/app/DeltaPanel.tsx`. Props: `hasSelection: boolean`, `currentStatus: WeatherStatus`, `baselineStatus: WeatherStatus`, `anomaly: { delta: number; zScore: number | null; verdictTier: VerdictTier } | null`. Sequential early-return branches (empty → loading → error → populated), each `<PanelShell>` + `<PanelHeadline>Delta</PanelHeadline>` first, top-aligned (PD-05). Loading branch routes through the shared `isAnomalyReady(currentStatus, baselineStatus)` predicate (do not re-derive the resolved-status comparison inline) with the shared spinner + role="status". Populated branch, in PD-07 order: (1) the Δ number `<p style={{ color: 'var(--anomaly-color)' }} className="m-0 text-[calc(var(--text-display)*1.7)] font-bold leading-[1.1] motion-safe:transition-colors motion-safe:duration-300 motion-safe:ease-out" aria-label={...}>` with `<span className="opacity-70">Δ</span>{formatDelta(anomaly.delta)}°C` (migrated verbatim, dominance rule locked); (2) the static micro-copy `<p>` exactly "How today compares to the 30-year average for this date." directly under the number; (3) the verdict `<p className="m-0 text-heading font-heading">{verdictLabel(anomaly.verdictTier)}</p>` (demoted to a secondary line); (4) the z-score chip `<p className="mt-xs inline-block w-fit text-label font-label text-muted bg-secondary rounded-full py-[2px] px-sm">` with the null-fallback string. Place the InfoTooltip (label e.g. "About the delta and z-score", body = the exact two-sentence delta/z-score string) as the panel's info affordance (e.g. in a header row alongside the Δ number or headline — implementation detail within the UI-SPEC contract). Use EXACT UI-SPEC copy for empty/loading/error. All dynamic text as JSX text nodes only. Create DeltaPanel.test.tsx asserting: exact headline/empty/loading/error copy, the Δ number className contains `text-[calc(var(--text-display)*1.7)]` and its inline style color is `var(--anomaly-color)`, the internal order (micro-copy appears before the verdict in the DOM), the z-score null-fallback string, and that opening the InfoTooltip reveals the delta/z-score body (fireEvent.click; vanilla assertions).
  </action>
  <verify>
    <automated>npx vitest run src/app/DeltaPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PanelHeadline text is exactly "Delta".
    - The Δ number className contains `text-[calc(var(--text-display)*1.7)]` and its inline style sets color to `var(--anomaly-color)` (LAYOUT-03 dominance + Phase 5 color carry-over).
    - DOM order is headline → Δ number → micro-copy "How today compares to the 30-year average for this date." → verdict → z-score chip (PD-07); micro-copy precedes the verdict.
    - z-score chip shows `z {n.toFixed(1)}` normally and exactly "z — (too little variance to compute)" when zScore is null.
    - Empty/loading/error render the exact UI-SPEC strings; loading routes through the shared predicate — `grep -c 'isAnomalyReady' src/app/DeltaPanel.tsx` returns ≥1.
    - Opening the InfoTooltip reveals the exact two-sentence delta/z-score body.
    - No `dangerouslySetInnerHTML` (grep returns 0).
    - DeltaPanel.test.tsx passes.
  </acceptance_criteria>
  <done>DeltaPanel renders the dominant 47.6px anomaly-colored Δ with PD-07 order, the exact copy in every state, an accessible InfoTooltip, and passes its test.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire App.tsx into the two-up row + shared gate, and retire AnomalyCard</name>
  <files>src/app/App.tsx, src/app/AnomalyCard.tsx</files>
  <read_first>
    - src/app/App.tsx (target — imports lines 7-15, the inline gate literals at lines 45-70, the render composition lines 104-113 passing props to AnomalyCard + TrendRow)
    - src/app/CurrentConditionsPanel.tsx, src/app/DeltaPanel.tsx (Task 1/2 — the new panels App composes)
    - src/app/TrendRow.tsx (plan 06-02 — now requires hasSelection + currentStatus + baselineStatus props)
    - src/anomaly/anomaly.ts (isAnomalyReady — App's own anomaly/trendDays gate switches to this)
    - .planning/phases/06-panel-restructure-hierarchy/06-CONTEXT.md (PD-01/02/03/04/05 layout rules; PD-09 retire AnomalyCard; PD-10 shared gate)
    - .planning/phases/06-panel-restructure-hierarchy/06-PATTERNS.md (App.tsx section — new composition + import changes)
  </read_first>
  <action>
    In `src/app/App.tsx`: replace `import { AnomalyCard } from './AnomalyCard'` with `import { CurrentConditionsPanel } from './CurrentConditionsPanel'` and `import { DeltaPanel } from './DeltaPanel'`; import `isAnomalyReady` from '../anomaly/anomaly'. Switch the `anomaly` computation (lines 45-52) and the `trendDays` computation (lines 58-70) to start their guard with `isAnomalyReady(current.status, baseline.status) && ...` instead of the inline `current.status === 'resolved' && baseline.status === 'resolved'` literal, so the gate condition lives in exactly one place (PD-10). Replace the single `<AnomalyCard .../>` render with a wrapping two-up row `<div>` that is a 50/50 equal-width, equal-height flex row (e.g. `flex flex-row items-stretch gap-md` with each panel in a `flex-1 min-w-0` cell) containing `<CurrentConditionsPanel hasSelection={hasSelection} currentStatus={current.status} baselineStatus={baseline.status} tempC={current.tempC} units={current.units} />` and `<DeltaPanel hasSelection={hasSelection} currentStatus={current.status} baselineStatus={baseline.status} anomaly={anomaly} />` (PD-01/02/03/05; the row is always rendered so it never reflows between states — PD-04). Update the `<TrendRow>` render to pass the new props: `hasSelection={hasSelection} currentStatus={current.status} baselineStatus={baseline.status} days={trendDays} units={current.units}`. Then DELETE `src/app/AnomalyCard.tsx` (PD-09) — after removing the import, no file references it, so tsc build confirms retirement.
  </action>
  <verify>
    <automated>test ! -f src/app/AnomalyCard.tsx && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/AnomalyCard.tsx` no longer exists (`test ! -f` passes).
    - App.tsx imports CurrentConditionsPanel + DeltaPanel and no longer imports AnomalyCard; `npm run build` (tsc -b) succeeds with no dangling reference.
    - The two panels render inside a 50/50 equal-width (`flex-1`), equal-height (`items-stretch`) row that is always present regardless of hasSelection/status (PD-02/03/04).
    - App.tsx's `anomaly` and `trendDays` guards call `isAnomalyReady` (grep: `grep -c 'isAnomalyReady' src/app/App.tsx` returns ≥2 — used by both computations; the inline resolved-status literal is gone).
    - TrendRow receives `hasSelection`, `currentStatus`, `baselineStatus`, `days`, `units`.
    - Full `npm test` suite passes (all component + anomaly tests green after the split).
  </acceptance_criteria>
  <done>App renders Location → [Current Conditions | Delta] two-up row → Last 7 Days, all gates route through isAnomalyReady, AnomalyCard is deleted, and the full build + test suite pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Open-Meteo current/archive data → panel DOM | Temperature, delta, verdict, and z-score derive from external Open-Meteo responses (consumed upstream in App.tsx) and are rendered into the two new panels; a tampered response must not inject markup. |
| authored copy → InfoTooltip DOM | Static explainer prose passed into each panel's InfoTooltip; developer-authored (no dynamic/location-specific text this phase). |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | CurrentConditionsPanel / DeltaPanel dynamic text (temperature, delta, verdict, z-score) | low | mitigate | All dynamic values render as plain JSX text nodes only; no dangerouslySetInnerHTML in either panel (verified by grep gates in Tasks 1-2). Preserves the T-01-02 / T-02-07 no-raw-HTML-sink invariant across the hero split. |
| T-06-06 | Information Disclosure | InfoTooltip explainer bodies | low | accept | Bodies are static, general, developer-authored methodology copy (no secrets, no location-specific or user data). Accepted (below ASVS L1 high-block threshold). |
| T-06-07 | Spoofing | Partial-reveal of temperature before anomaly resolves (gate drift) | medium | mitigate | Both panels + App.tsx route the loading gate through the single shared `isAnomalyReady` predicate (PD-10), so the two split panels cannot drift and reveal the temperature before the anomaly is ready. Enforced by grep gates asserting no inline `=== 'resolved'` in either panel or App.tsx. |

No new dependencies are installed this phase, so no supply-chain (T-*-SC) checkpoint applies. No secrets, auth, network, or persistence surface is introduced (existing Open-Meteo data flow unchanged, consumed upstream).
</threat_model>

<verification>
- `test ! -f src/app/AnomalyCard.tsx` — AnomalyCard retired.
- `npm run build` succeeds — no dangling AnomalyCard reference; new panel props type-check.
- `npm test` — full Vitest suite (CurrentConditionsPanel, DeltaPanel, TrendRow, anomaly, and all prior tests) passes.
- `grep -c 'isAnomalyReady' src/app/App.tsx src/app/CurrentConditionsPanel.tsx src/app/DeltaPanel.tsx` returns ≥1 for each file (single gate source, PD-10 — no re-derived inline resolved-status comparison).
- Delta dominance: `grep -c 'text-\[calc(var(--text-display)\*1.7)\]' src/app/DeltaPanel.tsx` returns ≥1, while the Current Conditions temperature uses plain `text-display` (28px) — `grep -c 'text-display' src/app/CurrentConditionsPanel.tsx` returns ≥1 — keeping the Δ at ~1.7× (inside the 1.5–2× band).
</verification>

<success_criteria>
- Combined hero split into two distinct panels; AnomalyCard deleted (LAYOUT-01, PD-09).
- Every panel carries a headline; the two-up row is 50/50 equal-width, equal-height, always side-by-side (LAYOUT-02, PD-01/02/03/04).
- Delta remains the dominant focal point: 47.6px, var(--anomaly-color), Δ-glyph-led, ~1.7× the Current Conditions temperature (LAYOUT-03, PD-06).
- Each panel shows adjacent micro-copy (EXPLAIN-01) and an accessible mouse+keyboard InfoTooltip with the exact authored bodies (EXPLAIN-02).
- The combined gate lives in exactly one place (isAnomalyReady) — no partial reveal (PD-10).
</success_criteria>

<output>
Create `.planning/phases/06-panel-restructure-hierarchy/06-03-SUMMARY.md` when done.
</output>
