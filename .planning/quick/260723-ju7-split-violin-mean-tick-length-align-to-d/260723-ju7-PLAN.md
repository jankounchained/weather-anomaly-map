---
phase: quick/260723-ju7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/trend.ts
  - src/app/TrendDayChart.tsx
  - src/index.css
  - src/app/TrendLegend.tsx
  - src/app/trend.test.ts
  - src/app/TrendDayChart.test.tsx
  - src/app/TrendLegend.test.tsx
autonomous: true
requirements: [PD-07, PD-12]
must_haves:
  truths:
    - "Each per-half mean tick extends to the density-curve edge at the mean's height (length = that half's meanWidth), not a fixed 29px — the tick end lands exactly on the violin curve (PD-07)."
    - "A sparse (rug) half whose mean is non-null still renders a readable mean tick, because its meanWidth falls back to maxHalfWidth."
    - "The mean tick and its 'Period average' legend swatch both render as a neutral bold near-black (#08060d) line at strokeWidth 3, updating in lockstep via the shared --color-chart-mean token (PD-12)."
    - "Every mean mark stays native SVG — no raw-HTML sink (T-03-07)."
  artifacts:
    - "src/app/trend.ts — ViolinHalf carries meanWidth; buildViolinPaths computes it from kdeAt at each half's mean"
    - "src/app/TrendDayChart.tsx — mean-tick x2 driven by meanWidth, MEAN_TICK_LEN removed, strokeWidth 3"
    - "src/index.css — --color-chart-mean = #08060d"
    - "src/app/TrendLegend.tsx — 'Period average' swatch strokeWidth 3"
  key_links:
    - "meanWidth reuses the SAME pooled bandwidth h and sharedMax already computed in buildViolinPaths, evaluated at kdeAt(mean, samples, h) — identical to the curve-edge formula, so the tick end coincides with the curve."
    - "--color-chart-mean is the single shared token for the tile tick and the legend swatch — one edit re-colors both."
---

<objective>
Polish the Phase 8 split-violin per-half mean ticks (PD-07): (1) length-align each tick to the density-curve edge at the mean's height instead of a fixed 29px, and (2) recolor the mean mark from accent-blue to a neutral bold near-black.

Purpose: The mean tick currently reads as a floating fixed-length blue bar disconnected from the curve it summarizes. Landing its end on the curve edge ties it visually to the distribution, and a neutral bold ink weight reads as a structural "average" reference rather than competing with the accent hue.

Output: ViolinHalf gains a meanWidth field (computed identically to the curve edge, at the half's mean); TrendDayChart drives the tick x-extent from it and bumps stroke weight/color; the shared --color-chart-mean token flips to #08060d so the tile tick and TrendLegend swatch update together. Tests updated to lock the new geometry and weight.

Artifacts produced: modified src/app/trend.ts, src/app/TrendDayChart.tsx, src/index.css, src/app/TrendLegend.tsx and their three test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

@src/app/trend.ts
@src/app/TrendDayChart.tsx
@src/anomaly/kde.ts
@src/index.css
@src/app/TrendLegend.tsx

# Stay consistent with the split-violin geometry blueprint:
# Skill("spike-findings-gsd-test")
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Length-align the mean tick to the density-curve edge</name>
  <files>src/app/trend.ts, src/app/TrendDayChart.tsx, src/app/trend.test.ts, src/app/TrendDayChart.test.tsx</files>
  <behavior>
    - trend.test.ts: a curve half (n >= 20) exposes meanWidth as a number in (0, maxHalfWidth]; identical recent/prior samples yield equal meanWidth (mirror halves).
    - trend.test.ts: a rug half with a non-null mean (n = 19) exposes meanWidth === maxHalfWidth.
    - trend.test.ts: a truly empty half (n = 0, mean null) exposes meanWidth === 0.
    - TrendDayChart.test.tsx: each mean-tick line's horizontal extent |x2 - x1| is > 0 and <= 36 (MAX_HALF_WIDTH) and is no longer the old fixed 29 for both ticks; x1 stays at CX (44).
  </behavior>
  <action>
    In src/app/trend.ts: add kdeAt to the existing kde import (`import { silvermanBandwidth, kdeCurve, halfDrawsCurve, kdeAt } from '../anomaly/kde'`). Add a `meanWidth: number` field to BOTH variants of the ViolinHalf discriminated union (curve and rug). In buildViolinPaths' inner `half()` helper, reuse the already-computed pooled bandwidth `h` and `sharedMax` (do not recompute either — that shared-bandwidth invariant is load-bearing per the file's own header). For the curve branch: compute the half mean once as `m = mean(samples)`, then `meanWidth = Math.max(0, Math.min(maxHalfWidth, (kdeAt(m, samples, h) / sharedMax) * maxHalfWidth))` — this is the identical density-to-pixel formula the curve edge uses (density / sharedMax * maxHalfWidth), evaluated at x = the half's mean, so the tick end coincides with the curve. Reuse that same `m` for the existing `mean` field (compute mean once, not twice). For the rug branch: `meanWidth = samples.length ? maxHalfWidth : 0` (a non-empty sparse half falls back to full width so its tick still reads; a truly empty half is 0). Do NOT touch the violin path anchor logic (`M{bottom} L{edge} L{top} Z`) or sharedMax/h derivation.

    In src/app/TrendDayChart.tsx: delete the now-unused `MEAN_TICK_LEN` constant (line ~57). Change makeMeanTickShape's signature to accept the half's meanWidth (e.g. `makeMeanTickShape(y, side, meanWidth, tooltipText)`) and set `x2={CX + side * meanWidth}` (x1 stays at CX). At the two call sites in the JSX, pass `violinPaths.prior.meanWidth` and `violinPaths.recent.meanWidth` respectively. Keep the mark a native SVG <line> wrapping <title> — no raw-HTML sink (T-03-07). Leave strokeWidth as-is in this task (Task 2 handles weight/color).

    Update src/app/trend.test.ts and src/app/TrendDayChart.test.tsx per the behavior block above. In trend.test.ts, extend the three existing buildViolinPaths cases (n>=20 curve, n=19 rug, n=0 empty) with the meanWidth assertions rather than adding new describe blocks. In TrendDayChart.test.tsx, extend the existing "renders two per-half mean-tick lines" test to read each `line[stroke="var(--color-chart-mean)"]`'s x1/x2 attributes and assert the extent is within (0, 36] and not 29.
  </action>
  <verify>
    <automated>cd /Users/janko/Repositories/gsd-test && npm test && npm run build</automated>
  </verify>
  <done>ViolinHalf exposes meanWidth on both variants; buildViolinPaths computes it via kdeAt at the mean reusing the shared h/sharedMax; MEAN_TICK_LEN is gone; the tile's mean tick x-extent is meanWidth-driven; full test suite green and build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Recolor + embolden the mean mark (tile + legend, in lockstep)</name>
  <files>src/index.css, src/app/TrendDayChart.tsx, src/app/TrendLegend.tsx, src/app/TrendLegend.test.tsx</files>
  <action>
    In src/index.css: change `--color-chart-mean: var(--color-accent);` to `--color-chart-mean: #08060d;` (the app's existing ink/near-black, already used for body text at line ~116). Update the adjacent comment so it no longer claims the token "reuses accent" — describe it as the neutral near-black average reference instead.

    In src/app/TrendDayChart.tsx: bump the mean-tick <line> strokeWidth from 2 to 3 inside makeMeanTickShape.

    In src/app/TrendLegend.tsx: the "Period average" swatch is a native <line> already bound to var(--color-chart-mean), so its color updates automatically — bump that swatch's hardcoded strokeWidth from 2 to 3 so the legend key reads equally bold. Do not change any label copy (PD-10 legend copy is final/reviewer-locked).

    Keep all marks native SVG — no raw-HTML sink (T-03-07). Do not touch TrendYAxisColumn or the violin path anchor logic.

    In src/app/TrendLegend.test.tsx: extend the existing "renders the mean-tick swatch as a <line>" test to also assert the swatch line's strokeWidth attribute is "3", locking the bold weight.
  </action>
  <verify>
    <automated>cd /Users/janko/Repositories/gsd-test && npm test && npm run build</automated>
  </verify>
  <done>--color-chart-mean is #08060d; the tile mean tick and the 'Period average' legend swatch both render at strokeWidth 3 and pick up the near-black color via the shared token; legend copy unchanged; full test suite green and build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| computed geometry/tooltip → rendered SVG | mean-tick coordinates and title text reach the DOM as SVG attributes/text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-qu0723-01 | Tampering | mean-tick + legend swatch marks | low | mitigate | Keep every mark native SVG (`<line>`/`<title>`) — no raw-HTML sink (T-03-07); the existing TrendLegend "never uses dangerouslySetInnerHTML" test continues to guard the source. |
| T-quaz0723-02 | Denial of Service | buildViolinPaths meanWidth compute | low | accept | meanWidth clamps to [0, maxHalfWidth] and reuses the already-guarded h/sharedMax (both are `|| 1`-floored / 1e-6-floored upstream), so no new NaN/divide-by-zero path; no packages added. |
</threat_model>

<verification>
- `npm test` — full Vitest suite green, including the updated trend.test.ts (meanWidth on all three ViolinHalf cases), TrendDayChart.test.tsx (meanWidth-driven tick extent), and TrendLegend.test.tsx (bold swatch stroke).
- `npm run build` — TypeScript typecheck + Vite build succeed (confirms the ViolinHalf field addition and makeMeanTickShape signature change compile cleanly).
- Visual sanity (optional): each per-half mean tick's end sits on its violin curve edge; both ticks and the legend swatch are bold near-black.
</verification>

<success_criteria>
- Mean-tick length equals the half's meanWidth (density-curve edge at the mean), not a fixed 29px.
- Sparse (rug) half with a non-null mean still shows a full-width tick; empty half shows none.
- Mean tick and legend swatch are #08060d at strokeWidth 3, updating together via --color-chart-mean.
- No violin path anchor / TrendYAxisColumn changes; all marks native SVG.
- Full test suite and build pass.
</success_criteria>

<output>
Create `.planning/quick/260723-ju7-split-violin-mean-tick-length-align-to-d/260723-ju7-SUMMARY.md` when done
</output>
